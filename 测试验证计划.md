# IoT智慧酒店系统 - 优化功能测试验证计划

**测试日期**: 2026年4月22日  
**测试范围**: 本次优化的23项安全修复  
**测试环境**: 本地开发环境 + 远程测试服务器

---

## 一、测试环境准备

### 1.1 后端服务启动
```bash
cd backend/iot-hotel-backend
npm run dev
```

### 1.2 MQTT Broker启动
```bash
cd backend/iot-hotel-backend
docker-compose up -d mqtt
```

### 1.3 数据库准备
- 确保使用最新的 `init.sql` 初始化数据库
- 验证44张表结构完整性

### 1.4 测试账号
使用文档中提供的测试账号进行验证。

---

## 二、严重安全漏洞测试 (S-01 ~ S-05)

### S-01: MQTT Broker认证测试

#### 测试1.1: 匿名连接被拒绝
```bash
# 测试命令 - 应该连接失败
mosquitto_sub -h 8.134.166.69 -p 1883 -t "test/topic"

# 预期结果: Connection error: Connection Refused: not authorised
```

#### 测试1.2: 认证连接成功
```bash
# 测试命令 - 应该连接成功
mosquitto_sub -h 8.134.166.69 -p 1883 -t "hotel/device/status/+" -u "iot_user" -P "IotHotel2026"

# 预期结果: 连接成功，等待消息
```

#### 测试1.3: ACL权限控制
```bash
# 测试 - iot_user不应该能订阅其他用户主题
mosquitto_sub -h 8.134.166.69 -p 1883 -t "other/user/topic" -u "iot_user" -P "IotHotel2026"

# 预期结果: 订阅被拒绝或收不到消息
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### S-03: RFID门禁接口认证测试

#### 测试3.1: 无认证访问被拒绝
```bash
curl -X POST http://localhost:9000/api/v1/rfid-access/verify \
  -H "Content-Type: application/json" \
  -d '{"card_id": "123456", "device_id": "rfid_001"}'

# 预期结果: 401 Unauthorized
```

#### 测试3.2: 有效设备认证通过
```bash
# 使用有效设备密钥签名请求
curl -X POST http://localhost:9000/api/v1/rfid-access/verify \
  -H "Content-Type: application/json" \
  -H "X-Device-ID: rfid_001" \
  -H "X-Device-Signature: <valid_signature>" \
  -d '{"card_id": "123456"}'

# 预期结果: 200 OK，返回验证结果
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### S-04: MQTT主题格式测试

#### 测试4.1: 新格式主题发布/订阅
```bash
# 订阅新格式主题
mosquitto_sub -h 8.134.166.69 -p 1883 -t "hotel/device/status/light/room101" -u "iot_user" -P "IotHotel2026"

# 发布消息
mosquitto_pub -h 8.134.166.69 -p 1883 -t "hotel/device/status/light/room101" -m '{"status": "on"}' -u "iot_user" -P "IotHotel2026"

# 预期结果: 订阅端收到消息
```

#### 测试4.2: 旧格式主题向后兼容
```bash
# 订阅旧格式主题
mosquitto_sub -h 8.134.166.69 -p 1883 -t "device/status/light/room101" -u "iot_user" -P "IotHotel2026"

# 预期结果: 仍然可以订阅（如果配置了向后兼容）
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

## 三、高危安全问题测试 (H-01 ~ H-07)

### H-01: 设备注册接口认证测试

#### 测试1.1: 无认证注册被拒绝
```bash
curl -X POST http://localhost:9000/api/v1/devices/register \
  -H "Content-Type: application/json" \
  -d '{"device_id": "test_001", "device_type": "light", "hotel_id": 1}'

# 预期结果: 401 Unauthorized
```

#### 测试1.2: 管理员认证注册成功
```bash
# 先登录获取管理员Token
curl -X POST http://localhost:9000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone": "13900000001", "password": "123123"}'

# 使用Token注册设备
curl -X POST http://localhost:9000/api/v1/devices/register \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <admin_token>" \
  -d '{"device_id": "test_001", "device_type": "light", "hotel_id": 1}'

# 预期结果: 200 OK，设备注册成功
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### H-02: WebSocket CORS测试

#### 测试2.1: 开发环境允许本地连接
```javascript
// 浏览器控制台测试
const ws = new WebSocket('ws://localhost:9000');
ws.onopen = () => console.log('Connected');
ws.onerror = (e) => console.log('Error:', e);

// 预期结果: 连接成功
```

#### 测试2.2: 生产环境拒绝未授权来源
```javascript
// 从非允许域名连接（需要部署到生产环境测试）
const ws = new WebSocket('wss://your-production-domain.com');

// 预期结果: 连接被拒绝
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### H-03: 登录防暴力破解测试

#### 测试3.1: 连续失败锁定账户
```bash
# 连续10次错误密码登录
for i in {1..12}; do
  curl -X POST http://localhost:9000/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"phone": "13900000006", "password": "wrongpassword"}'
  echo "Attempt $i"
done

# 第11次尝试正确密码
curl -X POST http://localhost:9000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone": "13900000006", "password": "123123"}'

# 预期结果: 429 Too Many Requests，账户被锁定
```

#### 测试3.2: 锁定时间累进
```bash
# 继续失败登录，验证锁定时间增加
# 需要等待验证
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### H-04: 设备密钥安全存储测试

#### 测试4.1: 设备认证通过
```bash
# 使用有效设备密钥请求
curl -X POST http://localhost:9000/api/v1/devices/command \
  -H "Content-Type: application/json" \
  -H "X-Device-ID: device_001" \
  -H "X-Device-Signature: <valid_hmac_signature>" \
  -d '{"command": "turn_on", "params": {}}'

# 预期结果: 200 OK，指令执行成功
```

#### 测试4.2: 无效密钥被拒绝
```bash
curl -X POST http://localhost:9000/api/v1/devices/command \
  -H "Content-Type: application/json" \
  -H "X-Device-ID: device_001" \
  -H "X-Device-Signature: invalid_signature" \
  -d '{"command": "turn_on"}'

# 预期结果: 401 Unauthorized
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### H-06: 语音通话网关测试

#### 测试6.1: WebRTC信令交换
```bash
# 启动语音通话
# 需要前端APP配合测试
```

#### 测试6.2: MQTT音频流转发
```bash
# 硬件端发布音频数据
mosquitto_pub -h 8.134.166.69 -p 1883 -t "hotel/device/call/room101/up" -f audio_data.bin -u "iot_user" -P "IotHotel2026"

# 预期结果: 音频数据转发到WebSocket房间
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

## 四、中危安全问题测试 (M-02, M-04, M-08)

### M-02: 错误处理测试

#### 测试2.1: 生产环境错误信息
```bash
# 设置NODE_ENV=production
NODE_ENV=production npm run dev

# 触发一个错误
curl http://localhost:9000/api/v1/nonexistent-endpoint

# 预期结果: 返回 {"message": "服务器错误"}，不包含堆栈信息
```

#### 测试2.2: 开发环境详细错误
```bash
# 设置NODE_ENV=development
NODE_ENV=development npm run dev

# 触发同样错误
curl http://localhost:9000/api/v1/nonexistent-endpoint

# 预期结果: 返回包含详细错误信息和堆栈
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### M-04: 密码策略测试

#### 测试4.1: 弱密码被拒绝
```bash
# 尝试注册弱密码
curl -X POST http://localhost:9000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"phone": "13900000100", "password": "123", "name": "测试用户"}'

# 预期结果: 400 Bad Request，提示密码长度不足
```

#### 测试4.2: 无特殊字符被拒绝
```bash
curl -X POST http://localhost:9000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"phone": "13900000100", "password": "12345678", "name": "测试用户"}'

# 预期结果: 400 Bad Request，提示需要特殊字符
```

#### 测试4.3: 强密码通过
```bash
curl -X POST http://localhost:9000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"phone": "13900000100", "password": "Test@123456", "name": "测试用户"}'

# 预期结果: 200 OK，注册成功
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

### M-08: Docker MySQL测试

#### 测试8.1: 使用默认密码连接
```bash
cd backend/iot-hotel-backend
docker-compose up -d mysql

# 使用默认密码连接
mysql -h localhost -P 3306 -u root -p'IotHotel2026!Root'

# 预期结果: 连接成功
```

#### 测试8.2: 使用环境变量密码
```bash
# 设置自定义密码
export DB_ROOT_PASSWORD="MyCustomPassword123!"
docker-compose up -d mysql

# 使用自定义密码连接
mysql -h localhost -P 3306 -u root -p'MyCustomPassword123!'

# 预期结果: 连接成功
```

**测试状态**: ⬜ 未测试  
**测试结果**: 

---

## 五、回归测试

### 5.1 用户登录流程
- ⬜ 正常登录
- ⬜ 密码错误处理
- ⬜ Token生成与验证

### 5.2 设备控制流程
- ⬜ 设备列表查询
- ⬜ 设备状态获取
- ⬜ 设备指令下发
- ⬜ 设备状态上报

### 5.3 酒店管理流程
- ⬜ 酒店信息查询
- ⬜ 房间管理
- ⬜ 预订管理

### 5.4 语音通话流程
- ⬜ 通话发起
- ⬜ 通话接听
- ⬜ 通话结束

---

## 六、测试总结

| 测试类别 | 测试项 | 通过 | 失败 | 备注 |
|---------|--------|------|------|------|
| MQTT认证 | 3 | 0 | 0 | 未测试 |
| RFID认证 | 2 | 0 | 0 | 未测试 |
| 设备注册 | 2 | 0 | 0 | 未测试 |
| WebSocket | 2 | 0 | 0 | 未测试 |
| 登录安全 | 2 | 0 | 0 | 未测试 |
| 设备密钥 | 2 | 0 | 0 | 未测试 |
| 语音网关 | 2 | 0 | 0 | 未测试 |
| 错误处理 | 2 | 0 | 0 | 未测试 |
| 密码策略 | 3 | 0 | 0 | 未测试 |
| Docker配置 | 2 | 0 | 0 | 未测试 |
| **总计** | **22** | **0** | **0** | **未开始** |

---

## 七、问题记录

### 问题1: 
**描述**: 
**影响**: 
**解决方案**: 
**状态**: 

---

**测试人员**: _______________  
**测试日期**: _______________  
**审核人员**: _______________
