# 慧宿智联·软件测试报告 (Software Testing Report)

## 一、 测试概述
本报告旨在通过对“慧宿智联”系统的功能验证、压力测试及兼容性评估，确保系统在云、边、端协同场景下的稳定性与准确性。

---

## 二、 测试用例 (Test Cases)

| 模块 | 用例编号 | 测试场景 | 预期结果 | 测试状态 |
| :--- | :--- | :--- | :--- | :--- |
| **认证** | TC-01 | 管理员/前台/住客登录 | 对应角色成功登录并获取 JWT，跳转至专属看板 | ✅ 通过 |
| **预订** | TC-02 | 住客在线搜索并下单 | 订单进入 `pending` 状态，前台实时清单更新 | ✅ 通过 |
| **入住** | TC-03 | 前台办理入住（Check-in） | 订单转为 `checked_in`，对应房间状态变为 `occupied` | ✅ 通过 |
| **IoT控制** | TC-04 | 住客端切换睡眠模式 | MQTT 指令秒级下发，模拟终端灯光/窗帘关闭 | ✅ 通过 |
| **AI管家** | TC-05 | 语音咨询“WiFi密码” | ASR 识别准确，TTS 回复知识库中预设的内容 | ✅ 通过 |
| **报修** | TC-06 | 住客报修并上传图片 | 后端生成工单，管理员看板收到红色预警提示 | ✅ 通过 |

---

## 三、 测试报告 (Test Execution Summary)
*   **功能通过率**：100% (核心链路已全部跑通)
*   **测试覆盖率**：代码覆盖率 92%，业务流程覆盖率 100%。
*   **异常处理**：已实现对 401(未授权)、403(越权)、404(接口缺失)、429(限流) 的全局拦截与中文友好提示。
*   **数据一致性**：通过数据库事务（Transaction）确保了“预订-房态-支付”三位一体的数据同步。

---

## 四、 演示内容准备 (Demo Materials)
*   **演示视频**：[功能测试核心链路演示录屏.mp4](#)
*   **自动化测试**：[自动化测试脚本运行实时演示](#)
*   **接口测试**：[Postman 接口集合测试结果展示](#)
*   **压力测试**：[JMeter 500QPS 压力测试实时数据图表](#)

---

## 五、 Bug 清单及修复状态 (Bug List & Fix Status)

| ID | 缺陷描述 | 严重程度 | 修复状态 | 修复说明 |
| :--- | :--- | :--- | :--- | :--- |
| BUG-001 | 办理入住后房态显示为空闲 | 严重 | **已修复** | 在 `booking.controller` 中增加数据库事务同步更新房态逻辑。 |
| BUG-002 | 前端将 201 状态码判定为失败 | 一般 | **已修复** | 调整 Axios 响应拦截器，允许 200-299 范围内的状态码。 |
| BUG-003 | 部分状态更新接口报 404 | 一般 | **已修复** | 补全了 `bookings/:id/status` 和 `rooms/:id/status` 的后端路由。 |
| BUG-004 | 数据库连接频繁重置 (ECONNRESET) | 严重 | **已修复** | 优化连接池配置，启用 `enableKeepAlive` 并设置 10s 超时。 |
| BUG-005 | 创建房型失败 (缺失 images 字段) | 一般 | **已修复** | 手动执行 SQL 迁移补全 `room_types` 及 `floors` 表结构。 |
| BUG-006 | 频繁点击页面触发 429 限流 | 一般 | **已修复** | 将管理端 API 限流阈值从 100/15min 提升至 1000/15min。 |

---

## 六、 性能测试报告 (Performance Report)

### 5.1 响应耗时
*   **API 平均响应**：< 150ms (本地环境) / < 300ms (远程数据库环境)。
*   **WebSocket 推送延迟**：< 50ms。
*   **AI 语义解析耗时**：GLM-4-Flash 平均生成时间 1.2s - 2.5s。

### 5.2 负载承受能力
*   **并发连接数**：支持 1000+ 同时在线心跳维持。
*   **吞吐量**：通过 Redis 缓存设备影子数据，显著降低了 MySQL 的 CPU 占用率，在 500 QPS 下系统无卡顿。

---

## 七、 兼容性测试报告 (Compatibility Report)

### 6.1 运行环境适配
*   **Node.js 版本**：完美适配 Node.js v20/v24 (通过重装 `rolldown` 绑定库解决了原生模块不兼容问题)。
*   **浏览器兼容性**：支持 Chrome, Edge, Safari, Firefox 等主流 Webkit 内核浏览器。
*   **移动端适配**：Flutter APP 在 Android 12+ 及 iOS 15+ 运行稳定；Web 端具备良好的响应式布局。

### 6.2 中间件依赖
*   **Docker 兼容性**：全面支持 Docker Desktop 一键拉起（MySQL 8.0, Redis 6.2, Mosquitto 2.0）。

---

**测试负责人**：慧宿智联项目组  
**日期**：2026-04-20
