# 智慧酒店物联网控制系统 - 三层架构硬件设计-楼层控制器

## 📋 文档概述

### 文档说明
本文档详细描述智慧酒店物联网控制系统的三层架构中**楼层控制器（原楼控）**的硬件设计，包括硬件组成、接线表、功能说明、演示功能等内容。

### 设备定位
楼层控制器是智慧酒店三层架构的**中间层核心设备**，负责：
- **楼层级集中管控**：管理整层楼的所有客房终端和公共区域设备
- **环境监测与联动**：监测公共区域环境，自动控制走廊灯光、空调等
- **客房状态汇总**：收集并显示该层所有客房的状态信息
- **🚔安全报警（公安合规）**：入侵检测增强、通行日志≥90天存储、防拆报警
- **🚒消防联动（GB 50116合规）**：**烟雾探测、接收前台管理端下发的疏散指令、应急照明/疏散指示控制、防火卷帘联动**
- **能耗统计**：汇总整层楼的能耗数据
- **访客通行管理**：楼层门禁控制

### 三层架构定位

```
┌─────────────────────────────────────────────────────┐
│                   🏨 前台管理端                         │
│         酒店整体协调 / 指令下发 / 数据总汇           │
│              (1台，位于前台/机房)                    │
└──────────────────┬──────────────────────────────────┘
                   │ MQTT/云端通信
        ┌──────────┼──────────┐
        ▼          ▼          ▼
┌───────────┐ ┌───────────┐ ┌───────────┐
│ 🏢 楼层N  │ │ 🏢 楼层2  │ │ 🏢 楼层1  │  ← 楼层控制器(每层1台)
│ 控制器    │ │ 控制器    │ │ 控制器    │     (本设备)
└───┬───┬───┘ └───┬───┬───┘ └───┬───┬───┘
    │   │         │   │         │   │
    ▼   ▼         ▼   ▼         ▼   ▼
  客房1 客房2   客房3 客房4   客房5 客房6  ← 客房端(每间房1台)
```

### 适用范围
- 硬件开发、调试、维护、培训、实物演示

### 文档版本
- **v2.0.0** - 2026年4月
  - v1.0: 基础环境监测端设计
  - v2.0: 升级为楼层控制器，新增客房管理、门禁、报警等功能

---

## 📦 硬件组成

### 楼层控制器硬件清单（含实物演示功能 + 公安接口）

| 序号 | 组件名称 | 型号/规格 | 数量 | 说明 | 成本 |
|------|----------|-----------|------|------|------|
| 1 | 主控板 | ESP32-WROOM-32 | 1 | 核心控制器，双核240MHz，WiFi+蓝牙 | ¥35 |
| 2 | OLED显示屏 | 0.96寸I2C | 1 | 显示楼层状态、客房列表、报警信息 | ¥12 |
| 3 | 按键模块 | 4键独立按键 + SOS键 | 1 | 页面切换/模式选择/报警确认/演示启动/**楼层SOS报警** | ¥6 |
| 4 | 蜂鸣器 | 有源蜂鸣器，5V | 1 | 报警提示音、操作确认音 | ¥3 |
| 5 | RGB LED(状态) | WS2812B×8颗 | 1 | 楼层状态指示、客房状态可视化 | ¥10 |
| 6 | 温湿度传感器 | DHT11 | 1 | 公共区域温湿度监测 | ¥8 |
| 7 | 空气质量传感器 | MQ-135 | 1 | 公共区域空气质量监测 | ¥15 |
| **8** | **烟雾传感器** | **MQ-2 / 光电烟雾模块** | **1** | **🚒走廊烟雾探测（本地消防联动触发源）** | **¥10** |
| 9 | 光照传感器 | 光敏电阻模块 | 1 | 走廊光照强度检测（人来灯亮演示） | ¥2 |
| 10 | 人体红外传感器 | HC-SR501 | 1 | 走廊人体检测（人来灯亮人走灯灭演示） | ¥5 |
| 11 | 继电器模块 | **6路**，10A/250VAC | 1 | 走廊灯光/电梯厅空调/**应急照明**/新风系统/**疏散指示**/**消防联动主控** | ¥25 |
| 12 | RGB灯带(走廊) | WS2812B×6颗 | 1 | 走廊灯光模拟（走廊灯控演示） | ¥10 |
| 13 | 舵机(SG90) | 9g微型舵机 | 1 | 防火卷帘模拟（消防联动演示） | ¥8 |
| 14 | **网络模块** | **WiFi/以太网** | **1** | ** firefighter疏散指令接收（由主控板WiFi集成）** | **-** |
| 15 | 电源模块 | DC 5V/2A + DC 12V/0.5A | 1套 | 为所有模块供电（**含UPS备用电源接口预留**） | ¥23 |
| 16 | PCB板 | 定制PCB | 1 | 硬件载体 | ¥20 |
| **总计** | - | - | - | **¥187** |

### 版本对比
| 版本 | 成本 | 新增内容 |
|------|------|----------|
| 基础版(楼控业务) | ¥174 | 楼层管控/环境监测/门禁/基础消防联动 |
| **完整版(+公安接口)** | **¥187** | **+烟雾探测器/+6路继电器(应急照明/疏散指示)/+SOS键/+UPS接口(+¥13)** |

### 硬件功能说明

#### 1. 楼层控制器功能定位（八大核心功能）

| 功能类别 | 具体功能 | 说明 |
|----------|----------|------|
| 🏢 **楼层集中管控** | 客房状态汇总 | 收集该层所有客房的状态并在OLED上展示 |
| | 设备协调 | 协调管理该层所有客房端的运行 |
| | 能耗统计 | 汇总整层的能耗数据上报前台管理端 |
| 🌡️ **环境监测联动** | 温湿度监测 | 监测走廊/电梯厅温度湿度 |
| | 空气质量监测 | MQ-135检测空气质量，超标自动开启新风机 |
| | **🚒烟雾探测** | **MQ-2探测走廊烟雾，超标自动触发消防联动** |
| | 光照检测 | 走廊光照强度检测，联动走廊灯光 |
| 💡 **公共区域智能控制** | 走廊灯光控制 | 人体感应+光照检测 → 人来灯亮人走灯灭 |
| | 电梯厅空调控制 | 根据温度自动调节 |
| | 新风系统控制 | 空气质量差时自动开启 |
| 🔐 **楼层安全管理** | 门禁控制 | RFID刷卡进入楼层，记录通行日志 |
| | 入侵检测 | 非法闯入报警+**防拆报警(设备被拆除时触发)** |
| | **🚔安全审计日志** | **通行/入侵/SOS/操作日志≥90天存储，支持导出** |
| 🚒 **消防联动（GB 50116）** | **烟雾探测联动** | **MQ-2烟雾超标→自动触发全套消防响应** |
| | **疏散指令接收** | **接收前台管理端下发的疏散指令（MQTT）→执行消防联动** |
| | 防火卷帘控制 | 舵机模拟防火卷帘下降（两段式） |
| | **应急照明切换** | **火灾时继电器强制切换到应急照明回路** |
| | **疏散指示控制** | **火灾时继电器点亮疏散指示灯** |
| | 排烟系统联动 | 继电器控制排烟风机启动 |
| 🔔 **紧急报警处理** | 火灾报警 | 蜂鸣器长鸣+LED红闪+上报前台管理端+触发消防联动 |
| | **🚔楼层SOS报警** | **SOS按键→上报前台→前台110联动** |
| | 紧急呼叫 | 接收客房紧急呼叫请求 |
| | 异常告警 | 设备离线/故障报警 |
| 🎬 **实物演示** | 一键演示流程 | 启动预设演示场景展示所有功能 |

#### 2. 核心硬件模块与演示功能

##### 2.1 主控板（ESP32-WROOM-32）
- **功能**：楼层控制核心，数据汇聚，多设备协调
- **双核分工**：
  - Core 0：MQTT通信（与前台管理端、各客房端通信）、数据处理
  - Core 1：传感器采集、本地控制、OLED刷新
- **WiFi角色**：同时作为AP（供客房端连接）和STA（连接前台管理端/云端）

##### 2.2 OLED显示屏（0.96寸I2C）→ 📊 楼层状态可视化演示
- **功能**：显示楼层综合状态
- **多页面切换**（按键1切换）：

**页面1 - 楼层总览**
```
┌──────────────────────┐
│ 🏢 3F 楼层控制器      │
│                      │
│ 🛏️ 房间: 6/6 在线     │
│ ✅ 已入住: 4间        │
│ 🔒 空置: 2间          │
│                      │
│ ⚡ 本层能耗: 12.5kWh  │
│ 🟢 系统: 正常运行     │
└──────────────────────┘
```

**页面2 - 客房状态列表**
```
┌──────────────────────┐
│ 📋 3F 客房状态       │
│                      │
│ 301✅ 302✅ 303🔴     │
│ 304✅ 305⚪ 306⚪     │
│                      │
│ ✅=入住 🔴=维修 ⚪=空 │
│ ←→ 翻页查看详情      │
└──────────────────────┘
```

**页面3 - 环境监测**
```
┌──────────────────────┐
│ 🌡️ 3F 环境监测       │
│                      │
│ 温度: 25.2℃          │
│ 湿度: 58%RH          │
│ 光照: 320lux         │
│ 空气: 优(28ppm)      │
│                      │
│ 走廊灯: 自动模式     │
└──────────────────────┘
```

**页面4 - 报警日志**
```
┌──────────────────────┐
│ 🔔 3F 报警记录       │
│                      │
│ [14:32] 304紧急呼叫  │
│ [11:20] 烟雾预警     │
│ [09:15] 301SOS求助   │
│                      │
│ 共3条未处理          │
│ 按键3确认清除        │
└──────────────────────┘
```

##### 2.3 按键模块（4键独立按键）
- **按键1**：切换OLED显示页面（总览/客房列表/环境/报警日志）
- **按键2**：楼层模式切换（正常/节能/夜间/消防演练）
- **按键3**：报警确认/静音
- **按键4**：**启动一键演示流程**

##### 2.4 蜂鸣器（有源蜂鸣器，5V）→ 🔔 多级报警演示
- **报警等级与声音模式**：

| 报警类型 | 声音模式 | 触发条件 |
|----------|----------|----------|
| 操作确认 | 单次短"滴" | 按键操作、刷卡成功 |
| 环境预警 | 每3秒一短"滴" | 温度>30℃或空气>50ppm |
| 客房呼叫 | 双滴"滴滴"(间隔1秒) | 收到客房SOS请求 |
| 入侵警报 | 快速连续"滴滴滴" | 非法闯入检测 |
| 火灾警报 | 连续长鸣不停 | 消防按钮按下/烟雾超标 |

##### 2.5 RGB LED(状态)（WS2812B×8颗）→ 🌈 楼层状态可视化演示
- **8颗LED分区指示**：

```
LED编号:  [1] [2] [3] [4] [5] [6] [7] [8]
用途:     系统  301  302  303  304  305  306  环境
```

| LED | 含义 | 颜色含义 |
|-----|------|----------|
| LED1 (系统) | 楼层控制器自身状态 | 🟢正常 / 🔴故障 / 🔵配网中 |
| LED2~7 (客房) | 对应房间状态 | 🟢已入住 / ⚪空置 / 🔴异常/SOS / 🔵清洁中 |
| LED8 (环境) | 公共区域环境等级 | 🟢优 / 🟡良 / 🟠差 / 🔴污染 |

- **演示效果**：一眼看清整个楼层的状态，非常直观

##### 2.6 温湿度传感器(DHT11) + 空气质量传感器(MQ-135) + 光照传感器 → 🌡️ 环境监测演示
- **功能**：公共区域环境三合一监测
- **演示场景**：
  - OLED实时滚动显示三项数值
  - 用手握住DHT11 → 温度上升 → 超过28℃自动开启继电器3（模拟空调）
  - 用酒精棉靠近MQ-135 → PPM值飙升 → RGB LED第8颗变红 + 蜂鸣器预警 + 开启继电器4（新风机）
  - 遮挡光敏电阻 → 光照降低 → 低于150lux自动开启继电器1（走廊灯）

##### 2.7 人体红外传感器(HC-SR501) + RGB灯带(WS2812B×6颗) → 💡 走廊智能照明演示
- **功能**：模拟走廊人来灯亮、人走灯灭
- **演示逻辑**：
  ```
  检测到有人进入 → RGB灯带渐亮(暖白,80%亮度)
  → 人在持续移动 → 保持常亮
  → 人离开后无人移动30秒 → 渐暗至关闭
  → 同时光照传感器判断：白天亮度40%，夜晚亮度100%
  ```
- **效果**：RGB灯带模拟走廊灯带，有人经过时亮起，离开后自动熄灭

##### 2.8 RFID模块(RC522) + 继电器(1路) → 🔐 楼层门禁演示
- **功能**：模拟楼层门禁刷卡进出
- **演示场景**：
  - 刷有效卡 → 蜂鸣器"滴"一声 → OLED显示"欢迎: 张三(3F)" → 继电器吸合模拟开门（绿灯亮）
  - 刷无效卡 → 蜂鸣器"滴滴"报警声 → OLED显示"❌ 无效卡" → 上报前台管理端（红灯闪烁）
  - 支持远程下发权限：前台管理端通过MQTT发送新卡号 → 楼层控制器自动添加白名单
  - 记录通行日志：时间+卡号+结果 → OLED报警页面可查看

##### 2.9 舵机(SG90) + 继电器(1路) → 🚒 消防联动演示
- **功能**：模拟火灾时的防火卷帘下降和排烟系统
- **演示场景**：
  - 按下按键2切换到"消防演练"模式 或 手动触发
  - **第一阶段（火灾确认）**：
    - 蜂鸣器连续长鸣（火灾警报声）
    - 全部RGB LED快速红闪
    - OLED显示"🚒 火灾警报！请立即疏散！"
    - MQTT广播通知所有客房端进入紧急模式
  - **第二阶段（联动执行）**：
    - 舵机从0°转到90° → 模拟**防火卷帘半降**
    - 3秒后转到180° → 模拟**防火卷帘全降**
    - 继电器2吸合 → 模拟**排烟风机启动**
    - 继电器1强制开启 → 模拟**应急照明（走廊灯全开）**
    - 继电器4吸合 → 模拟**新风机最大转速排烟**
  - **第三阶段（恢复）**：
    - 按键3确认解除 → 所有设备复位
    - 舵机回到0°，继电器释放，报警停止

##### 2.10 继电器模块（6路）→ ⚡ 公共区域设备控制演示 + 🚒消防联动执行
- **6路继电器分配**：

| 继电器 | 控制对象 | 触发条件 | 演示效果 |
|--------|----------|----------|----------|
| 继电器1 | 走廊灯光 | 人体感应+光照不足 | RGB灯带通电/断电模拟 |
| 继电器2 | 排烟风机 | 消防联动时启动 | 可接小风扇模拟 |
| 继电器3 | 电梯厅空调 | 温度超过28℃ | 可接LED表示运行 |
| 继电器4 | 新风系统 | 空气质量>50ppm | 可接小风扇模拟 |
| **继电器5** | **🚒应急照明** | **火灾时强制切换** | **常闭→消防时吸合，切换到应急电源** |
| **继电器6** | **🚒疏散指示** | **火灾时点亮** | **常开→消防时吸合，点亮疏散指示灯/安全出口标志** |

- **手动/自动双模式**：可通过按键切换，自动模式下由传感器驱动，手动模式下按键直接控制
- **消防联动优先级**：当收到火警信号时，继电器5/6不受模式限制，**立即执行**消防动作

##### 2.11 烟雾传感器(MQ-2) → 🚒 消防探测核心
- **功能**：走廊公共区域烟雾浓度检测
- **技术参数**：
  - 检测范围：300-10000ppm
  - 工作电压：5V（需预热）
  - 输出方式：模拟量(ADC) + 数字量(可调阈值)
  - 响应时间：<10秒
- **报警联动逻辑**：
  ```
  MQ-2 ADC值 > 阈值(如800)
    → 触发一级预警：蜂鸣器"滴"间隔3秒 + LED#8黄色闪烁
    → OLED显示"⚠️ 烟雾预警"
  
  MQ-2 ADC值 > 高阈值(如1500) 或 持续30秒超阈值
    → 触发二级火警：全套消防联动自动执行
    → 上报前台管理端(MQTT)
    → 舵机转动(卷帘下降)
    → 继电器5吸合(应急照明)
    → 继电器6吸合(疏散指示)
    → 继电器2吸合(排烟风机)
    → MQTT广播所有客房端进入紧急模式
  ```

---

## 🔌 硬件接线表

### 主控板引脚定义（楼层控制器 - 含演示功能 + 公安接口）

| ESP32引脚 | 功能 | 连接设备 | 说明 |
|-----------|------|----------|------|
| 3.3V | 电源输出 | 3.3V设备 | DHT11/MQ-135/光敏电阻/RFID/OLED |
| GND | 接地 | 所有设备接地 | 公共地 |
| 5V | 电源输入 | 5V电源模块 | 输入5V电源 |
| GPIO21 | I2C SDA | OLED显示屏 | I2C数据线 |
| GPIO22 | I2C SCL | OLED显示屏 | I2C时钟线 |
| GPIO27 | I2C SDA | RFID模块(RC522) | I2C数据线（共用总线需电平转换或软件切换） |
| GPIO26 | I2C SCL | RFID模块(RC522) | I2C时钟线 |
| GPIO15 | GPIO | DHT11温湿度传感器 | 单总线数据线 |
| GPIO36 | ADC | MQ-135空气质量传感器 | 模拟输入(AIN0) |
| **GPIO34** | **ADC** | **MQ-2烟雾传感器** | **🚒烟雾浓度检测(AIN6)** |
| GPIO39 | ADC | 光照传感器(光敏电阻) | 模拟输入(AIN3) |
| GPIO4 | GPIO | 人体红外(HC-SR501) | 数字输出(高=有人) |
| GPIO16 | GPIO | 继电器1-IN | 走廊灯光控制 |
| GPIO17 | GPIO | 继电器2-IN | 排烟风机控制 |
| GPIO18 | GPIO | 继电器3-IN | 电梯厅空调控制 |
| GPIO19 | GPIO | 继电器4-IN | 新风系统控制 |
| **GPIO23** | **GPIO** | **继电器5-IN** | **🚒应急照明切换** |
| **GPIO25** | **GPIO** | **继电器6-IN** | **🚒疏散指示灯控制** |
| GPIO5 | GPIO | RGB LED(状态) | WS2812B×8数据线 |
| GPIO23 | GPIO | RGB灯带(走廊) | WS2812B×6数据线 |
| GPIO25 | GPIO | 舵机(SG90)信号线 | PWM输出控制角度 |
| GPIO13 | GPIO | 蜂鸣器 | 低电平触发 |
| GPIO32 | GPIO | 按键1 | 显示页面切换（下拉输入） |
| GPIO33 | GPIO | 按键2 | 模式切换（下拉输入） |
| GPIO0 | GPIO | 按键3 | 报警确认（下拉输入） |
| GPIO14 | GPIO | 按键4 | 演示启动（下拉输入） |
| **GPIO35** | **GPIO(输入)** | **SOS报警键** | **🚔楼层SOS→上报前台** |
| GPIO13 | UART TX | USB转TTL | 调试串口 |
| GPIO3 | UART RX | USB转TTL | 调试串口 |

#### 🚔 安全审计日志存储
```
┌───────────────────────────────────────────────┐
│           Flash日志分区(1MB)                  │
│                                               │
│  存储内容:                                    │
│  ├─ 门禁通行记录 (卡号+时间+结果) ≥90天       │
│  ├─ 入侵/防拆报警记录                         │
│  ├─ SOS报警记录                               │
│  ├─ 火警事件记录(含MQ-2读数)                  │
│  ├─ 设备操作日志                              │
│  └─ 系统事件日志                              │
│                                               │
│  导出方式: USB串口CSV / MQTT上传云端          │
│  演示命令: export_logs                        │
└───────────────────────────────────────────────┘
```

### 电源分配

| 设备 | 电压 | 电流 | 来源 |
|------|------|------|------|
| ESP32主控板 | 3.3V | ~200mA | 板载LDO |
| OLED显示屏 | 3.3V | ~20mA | ESP32 3.3V |
| DHT11传感器 | 3.3V | ~1mA | ESP32 3.3V |
| RFID模块(RC522) | 3.3V | ~100mA | ESP32 3.3V或外接LDO |
| MQ-135传感器 | 5V | ~150mA | 5V电源（需预热） |
| 光敏电阻模块 | 3.3V | <1mA | ESP32 3.3V |
| 人体红外(HC-SR501) | 5V | ~50mA | 5V电源 |
| 按键模块 | 3.3V | ~1mA | ESP32 3.3V |
| 蜂鸣器 | 5V | ~30mA | 5V电源 |
| RGB LED(8颗状态) | 5V | ~96mA | 5V电源 |
| RGB灯带(6颗走廊) | 5V | ~360mA | 5V电源 |
| 舵机(SG90) | 5V | ~200mA(堵转) | 5V电源 |
| 继电器模块(4路) | 5V | ~160mA | 5V电源 |

### 总电流估算
- **3.3V总负载**：约420mA（ESP32 + OLED + DHT11 + RFID + 光敏电阻 + 按键）
- **5V总负载**：约1146mA（MQ-135 + 人体红外 + 蜂鸣器 + RGB LED + 灯带 + 舵机 + 继电器）
- **推荐电源**：DC 5V/2A（留有余量），如需外接大功率负载可升级为5V/3A

---

## 🎯 硬件功能需求（楼层控制器）

### 1. 楼层集中管控功能

#### 1.1 客房状态汇总
- 通过MQTT收集该层所有客房端的状态（在线/离线、入住/空置、设备状态）
- OLED分页显示各客房状态
- RGB LED实时反映每个客房的状态变化
- 定时（每10秒）向前台管理端上报楼层汇总数据

#### 1.2 能耗统计
- 收集各客房端上报的能耗数据
- 计算楼层总能耗
- 支持按日/周/月统计（存储在Flash中）
- 上报前台管理端用于整体能耗分析

### 2. 环境监测与联动功能

#### 2.1 三合一环境监测
- 温度监测（DHT11）：范围0-50℃，精度±2℃
- 湿度监测（DHT11）：范围20-90%RH，精度±5%RH
- 空气质量监测（MQ-135）：范围10-1000ppm
- 光照强度监测（光敏电阻）：范围0-1000lux
- 采样频率：2秒/次

#### 2.2 智能联动规则
| 条件 | 动作 | 延迟 |
|------|------|------|
| 温度 > 28℃ | 开启继电器3（空调） | 即时 |
| 温度 < 20℃ | 关闭继电器3（空调） | 即时 |
| 空气质量 > 50ppm | 开启继电器4（新风机） | 即时 |
| 空气质量恢复 < 30ppm | 关闭继电器4（新风机） | 延迟60秒 |
| 光照 < 150lux 且 检测到人 | 开启继电器1（走廊灯） | 即时 |
| 无人 > 30秒 | 关闭继电器1（走廊灯） | 延迟30秒 |

### 3. 公共区域智能控制功能

#### 3.1 走廊智能照明
- 人体红外传感器检测人员存在
- 结合光照传感器实现白天低亮度、夜晚高亮度
- RGB灯带模拟走廊灯带，支持渐亮/渐灭过渡效果
- 支持手动覆盖（按键强制开关）

#### 3.2 楼层模式切换
| 模式 | 走廊灯 | 空调 | 新风 | 说明 |
|------|--------|------|------|------|
| 正常模式 | 自动感应 | 自动恒温 | 自动 | 默认运行模式 |
| 节能模式 | 关闭/低亮 | 调高2℃ | 降低风速 | 夜深或无客人时段 |
| 夜间模式 | 10%亮度(感应时) | 关闭 | 关闭 | 23:00-06:00 |
| 消防演练 | 强制全开 | 关闭 | 最大转速 | 演示用 |

### 4. 安全管理与报警功能

#### 4.1 楼层门禁
- RFID刷卡验证（RC522）
- 白名单存储在Flash中（最多50张卡）
- 支持前台管理端通过MQTT远程添加/删除卡片
- **通行日志记录（≥90天存储）**：卡号+时间+结果
- 无效卡尝试超过3次触发入侵警报

#### 4.2 入侵检测与防拆报警
- **非法闯入**：未授权人员触发门禁失败3次以上 → 蜂鸣器报警 + LED红闪 + MQTT上报前台
- **🚔防拆报警**（新增）：设备被从安装位置拆除时触发（可配合震动传感器或倾斜开关）
  - 触发条件：设备倾斜角度>30° 或 震动强度超阈值
  - 联动动作：蜂鸣器急促长鸣 + 全部LED红闪 + MQTT上报前台 + RS485上报安防系统
  - 复位方式：按键3确认 + 设备重新固定

#### 4.3 🚔楼层SOS报警（新增）
- **触发**：SOS键短按或长按
- **联动动作**：
  1. 蜂鸣器"滴—滴—"(两声长)提示
  2. OLED显示"🚔 SOS已发送! 等待前台响应..."
  3. MQTT消息 `hotel/floor/{floor_id}/sos` 上报前台管理端
  4. 前台收到后可通过MQTT下发指令解除或联动110
- **复位**：再次按SOS 或 前台远程解除

#### 4.4 🚔安全审计日志系统（≥90天）
- **法规依据**：《旅馆业治安管理办法》+ 公安部安全监控要求
- **日志类型与保留期**：

| 日志类型 | 数据字段 | 保留期 |
|----------|----------|--------|
| 门禁通行 | 卡号/时间/位置(楼层)/结果(通过/拒绝) | ≥90天 |
| 入侵事件 | 时间/来源(门禁/防拆)/严重级别 | ≥90天 |
| SOS报警 | 触发时间/来源/处理状态 | ≥90天 |
| 火警事件 | 时间/探测器读数(MQ-2值)/处理过程 | ≥90天 |
| 操作日志 | 操作者(按键/远程)/指令/时间 | ≥30天 |

- **存储介质**：ESP32 Flash分区（4MB中分配1MB给日志区，支持循环覆盖）
- **导出方式**：
  - USB串口CSV格式导出（`export_logs` 命令）
  - MQTT上传云端平台
- **演示场景**：串口输入 `export_logs` → 输出最近10条日志记录

### 5. 🚒 消防联动系统（符合GB 50116《火灾自动报警系统设计规范》）

#### 5.1 烟雾探测（MQ-2）
- **探测范围**：走廊公共区域（模拟整层烟雾监测）
- **工作模式**：持续ADC采样（每2秒一次），需预热30秒
- **阈值设定**：
  | 级别 | ADC阈值 | 含义 | 动作 |
  |------|---------|------|------|
  | 正常 | <800 | 无异常 | 正常运行 |
  | 一级预警 | 800-1500 | 轻度烟雾 | 黄色LED闪烁 + OLED提示 + 蜂鸣器间隔音 |
  | 二级火警 | >1500 或 持续>30s | 明显火情 | **全套消防联动自动执行** |
- **抗误报设计**：
  - 连续3次采样均超阈值才判定为有效报警
  - 支持灵敏度调节（通过OLED菜单调整阈值）

#### 5.2 消防主机通信（RS485/MODBUS）
- **通信接口**：RS485总线（MAX3485模块）
- **通信协议**：MODBUS RTU（从站模式），9600bps, 8N1
- **连接对象**：酒店消防火灾报警控制器（主机）
- **接收数据**（与前台管理端一致）：
  | 寄存器地址 | 数据含义 | 说明 |
  |------------|----------|------|
  | 0x0000 | 火警状态字 | bit0=火警 bit1=故障 bit2=屏蔽 bit3=监管 |
  | 0x0001 | 火警回路号 | 触发火警的探测器所在回路 |
  | 0x0002 | 火警地址码 | 触发火警的探测器地址 |
  | 0x0010 | 故障类型 | 0=正常 1=主电故障 2=备电故障 3=通讯故障 |
  | 0x0020 | 系统运行状态 | 0=正常 1=火警 2=故障 3=自检 4=屏蔽 |
- **心跳检测**：每30秒轮询消防主机状态
- **双重上报通道**：收到火警信号时同时通过RS485回传确认 + MQTT上报前台

#### 5.3 消防联动执行序列
```
火警触发（MQ-2超标 OR 消防主机信号 OR 前台疏散指令）
    │
    ├─→ [T+0s] 本地响应
    │     ├─ 蜂鸣器：连续长鸣（消防警报音）
    │     ├─ RGB LED：全部红闪（2Hz频率）
    │     └─ OLED："🚒 火灾警报！请立即疏散！"
    │
    ├─→ [T+1s] 继电器动作（消防优先级最高）
    │     ├─ 继电器2 吸合 → 排烟风机启动
    │     ├─ 继电器5 吸合 → 应急照明切换（常闭→吸合）
    │     └─ 继电器6 吸合 → 疏散指示点亮（常开→吸合）
    │
    ├─→ [T+2s] 卷帘控制
    │     └─ 舵机转动：0°→45°(半降,延迟10s)→90°(全降)
    │         （模拟防火卷帘两段式下降）
    │
    ├─→ [T+3s] 远程通知
    │     ├─ MQTT广播 `hotel/emergency/evacuate`
    │     │   → 所有客房端进入紧急模式
    │     │   → 前台管理端显示火警位置
    │     └─ RS485 MODBUS回传确认到消防主机
    │
    └─→ [持续] 循环执行直到复位
          └─ 复位条件：按键3手动复位 OR 消防主机复位信号
```

#### 5.4 备用电源接口预留
- **目的**：火灾时市电可能断电，楼控需继续运行至少30分钟（GB 25506要求）
- **接口设计**：电源模块预留DC 12V备用电池接入端子
- **切换逻辑**：市电正常时充电；市电中断时无缝切换到电池供电
- **演示方案**：标注接口位置，实际部署时接UPS或锂电池组

#### 5.5 多级报警体系（整合公安/消防）
| 报警级别 | 触发条件 | 声音 | LED | 上报 | 联动动作 |
|----------|----------|------|-----|------|----------|
| 提示级 | 环境参数接近阈值 | 短滴(3s间隔) | 黄色闪烁 | 是 | 无 |
| 警告级 | 环境参数超限/客房呼叫 | 双滴(1s间隔) | 橙色闪烁 | 是 | 自动调节设备 |
| 🚔紧急级 | SOS/非法闯入/防拆 | 快速连击 | 红色快闪 | 是+广播 | 通知前台管理端+记录日志≥90天 |
| 🚒灾难级 | **火灾(MQ-2/消防主机)** | 连续长鸣(消防音) | 红色全闪 | 是+广播全楼 | **执行消防联动全套(卷帘+排烟+应急照明+疏散指示)** |

### 6. 实物演示功能

#### 6.1 一键演示流程
按下按键4启动完整的楼层控制器功能演示，包含以下子演示：

**演示A - 楼层总览与环境监测**（30秒）
- OLED显示楼层总览页，RGB LED显示各房间状态
- 依次展示温湿度、空气质量、光照数据
- 用手靠近传感器观察数值变化

**演示B - 走廊智能照明**（20秒）
- 手在HC-SR501前晃动 → RGB灯带渐亮
- 移开后等待30秒 → 灯带渐暗熄灭
- 遮挡光敏电阻后再触发 → 观察亮度差异

**演示C - 楼层门禁 + 🚔安全日志**（15秒）
- 刷有效卡 → 成功提示 + 绿灯 + "欢迎"显示
- 刷无效卡/未注册卡 → 报警 + 红闪 + "拒绝"显示
- **串口显示通行日志记录（演示≥90天存储）**

**🚔 演示D - 楼层SOS报警**（15秒）【新增】
- 按下SOS键 → 两声长蜂鸣 → OLED"🚔 SOS已发送!"
- MQTT上报前台（如前台在线则收到通知）
- 再次按SOS取消

**🚒 演示E - 完整消防联动流程**（30秒）【增强】
- 方式一：手动按键触发消防模式
- 方式二：模拟MQ-2烟雾超标（串口命令 `sim_fire`）
- 方式三：模拟RS485火警信号（串口发送MODBUS包）
- 联动动作：
  1. 蜂鸣器消防警报音 + 全部LED红闪
  2. 继电器2吸合(排烟) + 继电器5吸合(应急照明) + 继电器6吸合(疏散指示)
  3. 舵机转动：0°→45°→90°（两段式卷帘下降）
  4. MQTT广播疏散指令
  5. OLED："🚒 火灾警报! 请立即疏散!"
- 按键3复位全部状态

---

## 🎬 完整演示脚本

### 场景一：日常运营演示（建议第一个展示）
```
1. 系统上电 → 启动画面 → 进入楼层总览页
2. RGB LED逐个点亮：系统绿→各房间状态→环境绿
3. OLED显示：6间房在线，4间已入住，系统正常
4. 按键1翻页 → 客房状态页 → 环境监测页 → 报警页
5. 按键2切换模式 → 观察"节能/夜间"模式下的行为变化
```

### 场景二：智能走廊照明演示
```
1. 将手放在HC-SR501前晃动
2. 观察RGB灯带(走廊)从暗到亮的渐变过程（约1秒）
3. 保持手在传感器前移动 → 灯保持常亮
4. 移开手 → 等待30秒 → 观察灯带渐暗关闭
5. 用纸遮住光敏电阻 → 再次触发 → 对比亮度差异（白天vs夜晚）
```

### 场景三：楼层门禁演示
```
1. 准备一张已录入的RFID卡
2. 刷卡 → 听到"滴"一声 → OLED显示"欢迎: XXX"
3. 继电器短暂吸合（模拟门锁弹开声音）
4. 使用未录入的卡刷 → "滴滴"报警 → OLED显示"❌ 无效卡"
5. 连续刷3次无效卡 → 触发入侵警报模式
```

### 场景四：消防联动演示（最震撼）
```
1. 按下按键2切换到"消防演练"模式（或长按按键4）
2. 【第1秒】蜂鸣器开始连续长鸣（火灾警报）
3. 【第1秒】全部RGB LED快速红闪
4. 【第1秒】OLED显示"🚒 火灾警报！请疏散！"
5. 【第3秒】舵机转到90°（防火卷帘半降）
6. 【第6秒】舵机转到180°（防火卷帘全降）
7. 【第3秒】继电器2吸合（排烟风机启动，听到"咔哒"）
8. 【第3秒】继电器1吸合（应急照明全开）
9. 【第3秒】继电器4吸合（新风机最大转速）
10. 整个过程语音解说配合
11. 按键3 → 全部复位，恢复正常
```

### 场景五：跨设备联动演示（与前台管理端/客房端配合）
```
前提：前台管理端和至少一台客房端同时运行

1. 前台管理端下发"301退房指令" → 楼层控制器接收 → 转发给301客房端
2. 301客房端执行退房流程（关灯/关空调/关门锁）
3. 301客房端上报完成 → 楼层控制器更新OLED显示（301变为⚪空置）
4. 301客房端触发SOS紧急呼叫 → 楼层控制器收到 → 蜂鸣器报警
5. 楼层控制器转发给前台管理端 → 前台管理端OLED弹出警告
6. 前台管理端回复"已处理" → 楼层控制器停止报警
```

---

## 🔧 硬件调试功能

### 1. USB串口调试
- 波特率：115200
- 日志内容：传感器原始值、继电器状态、MQTT消息、报警事件
- 支持命令调试：
  - `set_temp 35` - 模拟温度值
  - `trigger_fire` - 触发消防演练
  - `add_card ABC12345678` - 添加门禁卡
  - `list_rooms` - 显示客房状态

### 4. 网络配置（阿里云学生服务器自建MQTT）

#### 4.1 网络接入方式

| 接入方式 | 适用场景 | 优势 | 劣势 |
|---------|---------|------|------|
| **WiFi直连公网** | 公网云服务器部署 | 配置简单，成本低 | 需要公网IP或DDNS |

#### 4.2 MQTT服务器配置（阿里云学生服务器自建MQTT）

##### 配置参数

| 参数 | 说明 | 示例 | 必填 |
|------|------|------|------|
| **MQTT服务器地址** | 阿里云学生服务器公网IP | `{您的阿里云公网IP}` | 是 |
| **MQTT服务器端口** | MQTT服务端口 | `1883`(非加密) / `8883`(TLS加密) | 是 |
| **MQTT用户名** | 设备认证用户名 | `floor_3` | 是 |
| **MQTT密码** | 设备认证密码 | `device_password_123` | 是 |
| **设备ID** | 唯一设备标识 | `floor_3` | 是 |
| **TLS/SSL加密** | 是否启用加密传输 | `true`(推荐) / `false` | 否 |
| **心跳间隔** | MQTT心跳包间隔（秒） | `30` | 否 |

##### MQTT服务器部署配置示例

**方案一：自建MQTT服务器（EMQX/Mosquitto）**
```
MQTT服务器地址：`{您的阿里云公网IP}`
MQTT服务器端口：`1883` (或 `8883` TLS)
MQTT用户名：`floor_3`
MQTT密码：`device_password_123`
设备ID：`floor_3`
TLS加密：`true` (推荐)

💡 **自建方案说明**：
- 使用阿里云学生服务器自建EMQX或Mosquitto
- 成本：仅服务器费用（无额外软件费用）
- 后端已部署完成，可直接使用
```

#### 4.3 远程配置接口

##### Web配置界面（AP模式）

设备首次启动或配置失效时，自动进入AP模式：

```
热点名称：`IoT_Hotel_Config_{device_id}`
默认密码：`12345678`
配置页面：`http://192.168.4.1`
```

**配置项包括：**
1. WiFi SSID/密码（连接公网WiFi）
2. MQTT服务器地址/端口/认证信息（阿里云学生服务器自建MQTT）
3. 设备ID/名称
4. TLS加密开关

##### App配置（可选）

通过手机App扫描设备二维码，快速配置网络参数：

```
1. 扫描设备二维码（包含设备ID）
2. 选择WiFi网络
3. 输入WiFi密码
4. 选择云服务器模板（阿里云学生服务器自建MQTT）
5. 填写认证信息
6. 发送配置到设备
```

#### 4.4 网络连接状态指示

| LED状态 | 含义 | 说明 |
|---------|------|------|
| **绿色常亮** | 正常工作 | WiFi连接成功，MQTT已连接 |
| **红色常亮** | WiFi连接失败 | 无法连接WiFi网络 |
| **红色快闪(2Hz)** | MQTT连接失败 | WiFi正常但MQTT无法连接 |
| **蓝色呼吸(1Hz)** | 正在连接 | 正在尝试连接WiFi/MQTT |
| **橙色闪烁** | 配置模式 | 进入AP模式，等待配置 |

---

## 📖 相关文档

- [三层硬件架构设计说明.md](../三层硬件架构设计说明.md) - 三层硬件架构设计详细说明
- [需求说明-三层架构楼控需求.md](需求说明-三层架构楼控需求.md) - 楼控硬件需求详细说明
- [需求说明-三层架构前台管理端需求.md](需求说明-三层架构前台管理端需求.md) - 前台管理端硬件需求详细说明
- [需求说明-三层架构客房端需求.md](需求说明-三层架构客房端需求.md) - 客房端硬件需求详细说明
- [三层架构硬件设计-前台管理端.md](三层架构硬件设计-前台管理端.md) - 前台管理端硬件设计（含实物演示功能）
- [三层架构硬件设计-客房端.md](三层架构硬件设计-客房端.md) - 客房端硬件设计（含实物演示功能）
- [硬件采购清单-精简版.md](../05-项目管理/硬件采购清单-精简版.md) - 硬件采购清单（含演示组件）

---

**文档版本**：v2.0.0
**最后更新**：2026年4月4日
**适用项目**：智慧酒店物联网控制系统
**架构设计**："前台管理端-楼层控制器-客房端"三层架构
**演示功能**：楼层状态可视化/走廊智能照明/楼层门禁/消防联动/环境监测/多级报警
