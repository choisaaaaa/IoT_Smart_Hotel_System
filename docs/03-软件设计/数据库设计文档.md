# 智慧酒店物联网控制系统 - 数据库文档

## 📋 文档概述

### 文档说明
本文档描述智慧酒店物联网控制系统的数据库设计，包括数据库表结构、字段说明、索引设计、ER图等内容。

### 适用范围
- 数据库设计
- 数据库开发
- 数据库维护
- 数据库优化

### 文档版本
- **v2.0.0** - 2026年4月
  - 重构为智慧酒店三层架构
  - 新增酒店信息、房间管理、预订系统、支付系统
  - 新增会员系统、优惠券系统
  - 新增送物订单、报修工单、服务评价系统
  - 优化系统架构设计

---

## 🗄️ 数据库概览

### 数据库名称
`iot_hotel_system`

### 数据库引擎
MySQL 8.0

### 字符集
UTF8MB4

### 排序规则
utf8mb4_unicode_ci

---

## 📊 数据库表结构

### 1. 酒店信息表（hotels）

**表说明**：存储酒店基本信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| hotel_name | VARCHAR | 100 | 否 | 否 | - | 酒店名称 |
| hotel_address | VARCHAR | 255 | 否 | 是 | NULL | 酒店地址 |
| hotel_phone | VARCHAR | 20 | 否 | 是 | NULL | 酒店电话 |
| hotel_star | INT | 11 | 否 | 是 | 3 | 酒店星级 |
| total_rooms | INT | 11 | 否 | 是 | 0 | 总房间数 |
| occupied_rooms | INT | 11 | 否 | 是 | 0 | 已入住房间数 |
| occupancy_rate | DECIMAL | 5,2 | 否 | 是 | 0.00 | 入住率 |
| logo | VARCHAR | 255 | 否 | 是 | NULL | 酒店Logo |
| description | TEXT | - | 否 | 是 | NULL | 酒店描述 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

### 19. 常用入住人表 (frequent_guests)

**表说明**：存储用户的常用联系人信息，用于预订时快速填充。

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| user_id | INT | 11 | 否 | 否 | - | 所属用户ID |
| name | VARCHAR | 100 | 否 | 否 | - | 姓名 |
| phone | VARCHAR | 20 | 否 | 否 | - | 手机号 |
| id_type | ENUM | - | 否 | 否 | 'idcard' | 证件类型(idcard/passport) |
| id_number | VARCHAR | 50 | 否 | 否 | - | 证件号码 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

*****索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (hotel_name)

**示例数据**：
```sql
INSERT INTO hotels (hotel_name, hotel_address, hotel_phone, hotel_star, total_rooms, occupied_rooms, occupancy_rate) 
VALUES ('智联酒店', '北京市朝阳区XX路XX号', '010-12345678', 4, 100, 45, 45.00);
```

---

### 2. 房间信息表（rooms）

**表说明**：存储房间信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| room_number | VARCHAR | 20 | 否 | 否 | - | 房间号 |
| room_type | VARCHAR | 20 | 否 | 否 | 'standard' | 房型 |
| room_name | VARCHAR | 100 | 否 | 是 | NULL | 房间名称 |
| room_price | DECIMAL | 10,2 | 否 | 否 | 0.00 | 房间价格 |
| room_status | VARCHAR | 20 | 否 | 否 | 'available' | 房间状态 |
| floor | INT | 11 | 否 | 是 | 1 | 楼层 |
| area | DECIMAL | 6,2 | 否 | 是 | NULL | 面积（平方米） |
| bed_type | VARCHAR | 20 | 否 | 是 | 'single' | 床型 |
| max_guests | INT | 11 | 否 | 是 | 1 | 最多入住人数 |
| description | TEXT | - | 否 | 是 | NULL | 房间描述 |
| facilities | JSON | - | 否 | 是 | NULL | 设施列表 |
| images | JSON | - | 否 | 是 | NULL | 房间图片 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (room_number)
- INDEX idx_room_status (room_status)
- INDEX idx_room_type (room_type)

**房型枚举**：
- `standard` - 标准房
- `deluxe` - 豪华房
- `suite` - 套房
- `presidential` - 总统套房

**房间状态枚举**：
- `available` - 空闲
- `occupied` - 已入住
- `cleaning` - 清洁中
- `maintenance` - 维修中
- `reserved` - 已预订

**床型枚举**：
- `single` - 单人床
- `double` - 双人床
- `twin` - 双床
- `king` - 大床

**示例数据**：
```sql
INSERT INTO rooms (room_number, room_type, room_name, room_price, room_status, floor, area, bed_type, max_guests, description, facilities) 
VALUES ('101', 'standard', '标准单人房', 299.00, 'available', 1, 25.5, 'single', 1, '舒适的标准单人房', '["WiFi","电视","空调","电话"]');
```

---

### 3. 预订表（bookings）

**表说明**：存储预订信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| booking_number | VARCHAR | 50 | 否 | 否 | - | 预订编号 |
| room_id | INT | 11 | 否 | 否 | - | 房间ID |
| guest_name | VARCHAR | 100 | 否 | 否 | - | 客人姓名 |
| guest_phone | VARCHAR | 20 | 否 | 否 | - | 客人电话 |
| guest_id_number | VARCHAR | 50 | 是 | 是 | NULL | 身份证号 |
| check_in_date | DATE | - | 否 | 否 | - | 入住日期 |
| check_out_date | DATE | - | 否 | 否 | - | 退房日期 |
| guest_count | INT | 11 | 是 | 是 | 1 | 入住人数 |
| special_requests | VARCHAR | 255 | 是 | 是 | NULL | 特殊要求 |
| payment_method | VARCHAR | 20 | 是 | 是 | 'balance' | 支付方式 |
| total_price | DECIMAL | 10,2 | 否 | 否 | 0.00 | 总金额 |
| deposit | DECIMAL | 10,2 | 是 | 是 | 0.00 | 定金 |
| status | VARCHAR | 20 | 否 | 否 | 'pending' | 预订状态 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| check_in_time | DATETIME | - | 是 | 是 | NULL | 入住时间 |
| check_out_time | DATETIME | - | 是 | 是 | NULL | 退房时间 |
| cancelled_at | DATETIME | - | 是 | 是 | NULL | 取消时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (booking_number)
- INDEX idx_room_id (room_id)
- INDEX idx_status (status)
- INDEX idx_check_in_date (check_in_date)
- INDEX idx_check_out_date (check_out_date)

**支付方式枚举**：
- `balance` - 账户余额
- `wechat` - 微信支付
- `alipay` - 支付宝支付
- `cash` - 现金
- `credit_card` - 信用卡

**预订状态枚举**：
- `pending` - 待确认
- `confirmed` - 已确认
- `checked_in` - 已入住
- `checked_out` - 已退房
- `cancelled` - 已取消

**示例数据**：
```sql
INSERT INTO bookings (booking_number, room_id, guest_name, guest_phone, guest_id_number, check_in_date, check_out_date, guest_count, special_requests, payment_method, total_price, deposit, status) 
VALUES ('BK20240118001', 1, '张三', '13800138000', '110101199001011234', '2024-01-20', '2024-01-22', 2, '需要安静的房间', 'balance', 598.00, 0.00, 'confirmed');
```

---

### 4. 支付表（payments）

**表说明**：存储支付订单信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| payment_no | VARCHAR | 50 | 否 | 否 | - | 支付单号 |
| order_type | VARCHAR | 20 | 否 | 否 | 'booking' | 订单类型 |
| order_id | INT | 11 | 否 | 否 | - | 订单ID |
| amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 支付金额 |
| payment_method | VARCHAR | 20 | 否 | 否 | 'wechat' | 支付方式 |
| status | VARCHAR | 20 | 否 | 否 | 'pending' | 支付状态 |
| transaction_no | VARCHAR | 100 | 是 | 是 | NULL | 交易单号 |
| paid_at | DATETIME | - | 是 | 是 | NULL | 支付时间 |
| description | VARCHAR | 255 | 是 | 是 | NULL | 支付描述 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (payment_no)
- INDEX idx_order (order_type, order_id)
- INDEX idx_status (status)
- INDEX idx_created_at (created_at)

**订单类型枚举**：
- `booking` - 预订
- `delivery` - 送物
- `maintenance` - 报修
- `other` - 其他

**支付方式枚举**：
- `wechat` - 微信支付
- `alipay` - 支付宝支付
- `balance` - 账户余额
- `cash` - 现金
- `credit_card` - 信用卡

**支付状态枚举**：
- `pending` - 待支付
- `paid` - 已支付
- `refunded` - 已退款
- `failed` - 支付失败

**示例数据**：
```sql
INSERT INTO payments (payment_no, order_type, order_id, amount, payment_method, status, transaction_no, paid_at, description) 
VALUES ('PAY20240118001', 'booking', 1, 598.00, 'wechat', 'paid', '100000000020240118123456', '2024-01-18 10:05:00', '预订101房支付');
```

---

### 5. 会员表（members）

**表说明**：存储会员信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| phone | VARCHAR | 20 | 否 | 否 | - | 手机号 |
| password | VARCHAR | 255 | 否 | 否 | - | 密码（加密存储） |
| name | VARCHAR | 100 | 是 | 是 | NULL | 姓名 |
| id_number | VARCHAR | 50 | 是 | 是 | NULL | 身份证号 |
| member_level | VARCHAR | 20 | 否 | 否 | 'standard' | 会员等级 |
| points | INT | 11 | 否 | 否 | 0 | 积分 |
| balance | DECIMAL | 10,2 | 否 | 否 | 0.00 | 账户余额 |
| total_spent | DECIMAL | 12,2 | 否 | 否 | 0.00 | 累计消费 |
| total_stays | INT | 11 | 否 | 否 | 0 | 累计入住次数 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (phone)
- INDEX idx_member_level (member_level)

**会员等级枚举**：
- `standard` - 普通会员
- `gold` - 金卡会员
- `platinum` - 白金卡会员
- `diamond` - 钻石卡会员

**示例数据**：
```sql
INSERT INTO members (phone, password, name, id_number, member_level, points, balance, total_spent, total_stays) 
VALUES ('13800138000', '$2a$10$...', '张三', '110101199001011234', 'standard', 100, 50.00, 1000.00, 5);
```

---

### 6. 优惠券表（coupons）

**表说明**：存储优惠券信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| coupon_name | VARCHAR | 100 | 否 | 否 | - | 优惠券名称 |
| coupon_type | VARCHAR | 20 | 否 | 否 | 'discount' | 优惠券类型 |
| discount_value | DECIMAL | 10,2 | 否 | 否 | 0.00 | 优惠值 |
| min_amount | DECIMAL | 10,2 | 是 | 是 | 0.00 | 最低消费 |
| total_count | INT | 11 | 否 | 否 | 0 | 总发行量 |
| received_count | INT | 11 | 否 | 否 | 0 | 已领取数量 |
| valid_from | DATE | - | 否 | 否 | - | 有效期开始 |
| valid_to | DATE | - | 否 | 否 | - | 有效期结束 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_valid (valid_from, valid_to)

**优惠券类型枚举**：
- `discount` - 满减券
- `percentage` - 折扣券
- `free` - 免费券

**示例数据**：
```sql
INSERT INTO coupons (coupon_name, coupon_type, discount_value, min_amount, total_count, received_count, valid_from, valid_to) 
VALUES ('满200减50优惠券', 'discount', 50.00, 200.00, 1000, 500, '2024-01-18', '2024-02-18');
```

---

### 7. 会员优惠券关联表（member_coupons）

**表说明**：存储会员领取的优惠券

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| member_id | INT | 11 | 否 | 否 | - | 会员ID |
| coupon_id | INT | 11 | 否 | 否 | - | 优惠券ID |
| status | VARCHAR | 20 | 否 | 否 | 'unused' | 优惠券状态 |
| used_at | DATETIME | - | 是 | 是 | NULL | 使用时间 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_member (member_id)
- INDEX idx_coupon (coupon_id)
- INDEX idx_status (status)

**优惠券状态枚举**：
- `unused` - 未使用
- `used` - 已使用
- `expired` - 已过期

**示例数据**：
```sql
INSERT INTO member_coupons (member_id, coupon_id, status) 
VALUES (1, 1, 'unused');
```

---

### 8. 送物订单表（delivery_orders）

**表说明**：存储送物订单信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| order_no | VARCHAR | 50 | 否 | 否 | - | 订单号 |
| booking_id | INT | 11 | 是 | 是 | NULL | 预订ID（关联booking表，用于退房结算） |
| guest_id | INT | 11 | 是 | 是 | NULL | 住客ID（关联guest表，用于服务归属） |
| room_id | INT | 11 | 否 | 否 | - | 房间ID |
| item_category | VARCHAR | 50 | 否 | 否 | 'food' | 物品类别 |
| item_name | VARCHAR | 100 | 否 | 否 | - | 物品名称 |
| quantity | INT | 11 | 否 | 否 | 1 | 数量 |
| note | VARCHAR | 255 | 是 | 是 | NULL | 备注 |
| status | VARCHAR | 20 | 否 | 否 | 'pending' | 订单状态 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| completed_at | DATETIME | - | 是 | 是 | NULL | 完成时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (order_no)
- INDEX idx_booking_id (booking_id)
- INDEX idx_guest_id (guest_id)
- INDEX idx_room_id (room_id)
- INDEX idx_status (status)
- INDEX idx_created_at (created_at)

**物品类别枚举**：
- `food` - 食品
- `beverage` - 饮料
- `towel` - 毛巾
- `toilet_paper` - 卫生纸
- `other` - 其他

**订单状态枚举**：
- `pending` - 待接单
- `processing` - 配送中
- `completed` - 已完成
- `cancelled` - 已取消

**示例数据**：
```sql
INSERT INTO delivery_orders (order_no, room_id, item_category, item_name, quantity, note, status) 
VALUES ('DLV20240118001', 1, 'food', '矿泉水', 2, '请送到房间', 'pending');
```

---

### 9. 报修工单表（maintenance_tickets）

**表说明**：存储报修工单信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| ticket_no | VARCHAR | 50 | 否 | 否 | - | 工单号 |
| booking_id | INT | 11 | 是 | 是 | NULL | 预订ID（关联booking表，用于退房结算） |
| guest_id | INT | 11 | 是 | 是 | NULL | 住客ID（关联guest表，用于服务归属） |
| room_id | INT | 11 | 否 | 否 | - | 房间ID |
| fault_type | VARCHAR | 50 | 否 | 否 | 'other' | 故障类型 |
| fault_description | TEXT | - | 是 | 是 | NULL | 故障描述 |
| photos | JSON | - | 是 | 是 | NULL | 故障照片 |
| priority | VARCHAR | 20 | 否 | 否 | 'medium' | 优先级 |
| status | VARCHAR | 20 | 否 | 否 | 'pending' | 工单状态 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| completed_at | DATETIME | - | 是 | 是 | NULL | 完成时间 |
| repair_description | TEXT | - | 是 | 是 | NULL | 维修描述 |
| repair_cost | DECIMAL | 10,2 | 是 | 是 | 0.00 | 维修费用 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (ticket_no)
- INDEX idx_booking_id (booking_id)
- INDEX idx_guest_id (guest_id)
- INDEX idx_room_id (room_id)
- INDEX idx_status (status)
- INDEX idx_priority (priority)
- INDEX idx_created_at (created_at)

**故障类型枚举**：
- `air_conditioner` - 空调
- `lighting` - 灯光
- `plumbing` - 水电
- `furniture` - 家具
- `electronics` - 电器
- `other` - 其他

**优先级枚举**：
- `low` - 低
- `medium` - 中
- `high` - 高
- `urgent` - 紧急

**工单状态枚举**：
- `pending` - 待处理
- `processing` - 处理中
- `completed` - 已完成
- `cancelled` - 已取消

**示例数据**：
```sql
INSERT INTO maintenance_tickets (ticket_no, room_id, fault_type, fault_description, photos, priority, status) 
VALUES ('MT20240118001', 1, 'air_conditioner', '空调不制冷', '["http://example.com/photo1.jpg"]', 'high', 'pending');
```

---

### 10. 服务评价表（reviews）

**表说明**：存储服务评价信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| order_id | INT | 11 | 否 | 否 | - | 订单ID |
| order_type | VARCHAR | 20 | 否 | 否 | 'booking' | 订单类型 |
| member_id | INT | 11 | 是 | 是 | NULL | 会员ID |
| score | INT | 11 | 否 | 否 | 5 | 评分（1-5星） |
| content | TEXT | - | 是 | 是 | NULL | 评价内容 |
| photos | JSON | - | 是 | 是 | NULL | 评价图片 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_order (order_id, order_type)
- INDEX idx_member_id (member_id)
- INDEX idx_created_at (created_at)

**订单类型枚举**：
- `booking` - 预订
- `delivery` - 送物
- `maintenance` - 报修

**示例数据**：
```sql
INSERT INTO reviews (order_id, order_type, member_id, score, content, photos) 
VALUES (1, 'booking', 1, 5, '房间很干净，服务很好', '["http://example.com/photo1.jpg"]');
```

---

### 11. 设备表（devices）

**表说明**：存储所有设备信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| device_id | VARCHAR | 50 | 否 | 否 | - | 设备唯一标识 |
| device_type | VARCHAR | 20 | 否 | 否 | - | 设备类型 |
| device_name | VARCHAR | 50 | 否 | 否 | - | 设备名称 |
| device_key | VARCHAR | 50 | 否 | 否 | - | 设备密钥 |
| device_status | VARCHAR | 20 | 否 | 否 | 'offline' | 设备状态 |
| firmware_version | VARCHAR | 20 | 是 | 是 | NULL | 固件版本 |
| last_seen | DATETIME | - | 是 | 是 | NULL | 最后上线时间 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (device_id)
- INDEX idx_device_id (device_id)
- INDEX idx_device_status (device_status)

**设备类型枚举**：
- `main` - 前台管理端
- `sub1` - 楼控（环境监测）
- `sub2` - 客房端（设备控制）

**设备状态枚举**：
- `online` - 在线
- `offline` - 离线
- `error` - 异常

**示例数据**：
```sql
INSERT INTO devices (device_id, device_type, device_name, device_key, device_status, firmware_version) 
VALUES ('MAIN001', 'main', '前台管理端', 'key_main_001', 'online', 'v1.0.0');
```

---

### 12. 传感器数据表（sensor_data）

**表说明**：存储传感器采集的数据

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| device_id | VARCHAR | 50 | 否 | 否 | - | 设备ID |
| sensor_type | VARCHAR | 20 | 否 | 否 | - | 传感器类型 |
| sensor_value | VARCHAR | 50 | 否 | 否 | - | 传感器值 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_device_id (device_id)
- INDEX idx_sensor_type (sensor_type)
- INDEX idx_created_at (created_at)
- INDEX idx_device_sensor (device_id, sensor_type)

**传感器类型枚举**：
- `temperature` - 温度
- `humidity` - 湿度
- `air_quality` - 空气质量
- `light_intensity` - 光照强度
- `door_status` - 门磁状态
- `motion` - 人体检测

**分区策略**（高性能场景）：

**按时间范围分区**（推荐用于高频数据）：

```sql
-- 按月分区
ALTER TABLE sensor_data PARTITION BY RANGE (TO_DAYS(created_at)) (
    PARTITION p202401 VALUES LESS THAN (TO_DAYS('2024-02-01')),
    PARTITION p202402 VALUES LESS THAN (TO_DAYS('2024-03-01')),
    PARTITION p202403 VALUES LESS THAN (TO_DAYS('2024-04-01')),
    PARTITION p202404 VALUES LESS THAN (TO_DAYS('2024-05-01')),
    PARTITION p202405 VALUES LESS THAN (TO_DAYS('2024-06-01')),
    PARTITION p202406 VALUES LESS THAN (TO_DAYS('2024-07-01')),
    PARTITION p202407 VALUES LESS THAN (TO_DAYS('2024-08-01')),
    PARTITION p202408 VALUES LESS THAN (TO_DAYS('2024-09-01')),
    PARTITION p202409 VALUES LESS THAN (TO_DAYS('2024-10-01')),
    PARTITION p202410 VALUES LESS THAN (TO_DAYS('2024-11-01')),
    PARTITION p202411 VALUES LESS THAN (TO_DAYS('2024-12-01')),
    PARTITION p202412 VALUES LESS THAN (TO_DAYS('2025-01-01')),
    PARTITION p_max VALUES LESS THAN MAXVALUE
);
```

**按设备ID哈希分区**（推荐用于多设备并发）：

```sql
-- 按设备ID哈希分区（10个分区）
ALTER TABLE sensor_data PARTITION BY HASH (device_id) PARTITIONS 10;
```

**组合分区策略**（推荐用于超大规模）：

```sql
-- 先按时间范围分区，再按设备ID哈希子分区
ALTER TABLE sensor_data 
PARTITION BY RANGE (TO_DAYS(created_at))
SUBPARTITION BY HASH (device_id) 
SUBPARTITIONS 5 (
    PARTITION p2024q1 VALUES LESS THAN (TO_DAYS('2024-04-01')),
    PARTITION p2024q2 VALUES LESS THAN (TO_DAYS('2024-07-01')),
    PARTITION p2024q3 VALUES LESS THAN (TO_DAYS('2024-10-01')),
    PARTITION p2024q4 VALUES LESS THAN (TO_DAYS('2025-01-01')),
    PARTITION p_max VALUES LESS THAN MAXVALUE
);
```

**分区维护脚本**：

```sql
-- 添加新分区（每月执行）
ALTER TABLE sensor_data ADD PARTITION (
    PARTITION p202412 VALUES LESS THAN (TO_DAYS('2025-01-01'))
);

-- 删除旧分区（保留最近3个月数据）
ALTER TABLE sensor_data DROP PARTITION p202401;
```

**性能优化建议**：
- **高频数据**（1-10秒采集频率）：使用时间范围分区，按月或季度分区
- **多设备并发**：使用哈希分区，分区数建议为设备数的1/10
- **查询优化**：分区键必须出现在WHERE条件中才能触发分区裁剪
- **数据保留**：建议保留最近3个月数据，旧数据归档到历史表

**示例数据**：
```sql
INSERT INTO sensor_data (device_id, sensor_type, sensor_value) 
VALUES ('SUB1001', 'temperature', '25.5');
```

---

### 13. 控制指令表（control_commands）

**表说明**：存储设备控制指令

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| device_id | VARCHAR | 50 | 否 | 否 | - | 设备ID |
| command_type | VARCHAR | 20 | 否 | 否 | - | 指令类型 |
| command_value | VARCHAR | 50 | 否 | 否 | - | 指令值 |
| command_status | VARCHAR | 20 | 否 | 否 | 'pending' | 指令状态 |
| created_by | VARCHAR | 50 | 是 | 是 | NULL | 创建者 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| executed_at | DATETIME | - | 是 | 是 | NULL | 执行时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_device_id (device_id)
- INDEX idx_created_at (created_at)
- INDEX idx_command_status (command_status)

**指令类型枚举**：
- `light_on` - 开灯
- `light_off` - 关灯
- `ac_on` - 开空调
- `ac_off` - 关空调
- `lock_open` - 开锁
- `lock_close` - 上锁
- `relay_on` - 继电器开
- `relay_off` - 继电器关

**指令状态枚举**：
- `pending` - 待执行
- `executing` - 执行中
- `success` - 成功
- `failed` - 失败
- `timeout` - 超时

**示例数据**：
```sql
INSERT INTO control_commands (device_id, command_type, command_value, command_status, created_by) 
VALUES ('SUB1001', 'light_on', '1', 'pending', 'user001');
```

---

### 14. 语音通话表（calls）

**表说明**：存储语音通话记录（支持住客发起和前台主动发起双向通话）

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| call_id | VARCHAR | 64 | 否 | 否 | - | 唯一通话ID (UUID) |
| caller_type | ENUM | - | 否 | 否 | - | 主叫方类型 |
| caller_id | VARCHAR | 64 | 否 | 否 | - | 主叫标识(房间号/员工ID) |
| callee_type | ENUM | - | 否 | 否 | - | 被叫方类型 |
| callee_id | VARCHAR | 64 | 否 | 否 | - | 被叫标识 |
| status | ENUM | - | 否 | 否 | 'calling' | 通话状态 |
| started_at | DATETIME | - | 否 | 否 | - | 通话开始时间 |
| answered_at | DATETIME | - | 是 | 是 | NULL | 接听时间 |
| ended_at | DATETIME | - | 是 | 是 | NULL | 结束时间 |
| duration_sec | INT | 11 | 否 | 否 | 0 | 通话时长(秒) |
| recording_url | VARCHAR | 512 | 是 | 是 | NULL | 录音文件URL(可选) |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (call_id)
- INDEX idx_caller (caller_type, caller_id)
- INDEX idx_callee (callee_type, callee_id)
- INDEX idx_status (status)
- INDEX idx_started_at (started_at)
- INDEX idx_caller_time (caller_type, caller_id, started_at)
- INDEX idx_callee_time (callee_type, callee_id, started_at)

**主叫/被叫类型枚举**：
- `room` - 客房端（房间号）
- `front_desk` - 前台管理端（员工ID）

**通话状态枚举**：
- `calling` - 呼叫中（住客发起）
- `outgoing` - 呼出中（前台发起）
- `ringing` - 响铃中
- `connected` - 通话中
- `ended` - 通话结束
- `rejected` - 已拒接
- `missed` - 未接来电
- `busy` - 对方正忙

**示例数据**：
```sql
-- 住客发起通话
INSERT INTO calls (call_id, caller_type, caller_id, callee_type, callee_id, status, started_at) 
VALUES ('CALL20260404001', 'room', '301', 'front_desk', 'FD001', 'calling', NOW());

-- 前台主动发起通话
INSERT INTO calls (call_id, caller_type, caller_id, callee_type, callee_id, status, started_at) 
VALUES ('CALL20260404002', 'front_desk', 'FD001', 'room', '301', 'outgoing', NOW());

-- 通话结束
UPDATE calls 
SET status = 'ended', answered_at = '2026-04-04 15:30:00', ended_at = '2026-04-04 15:35:00', duration_sec = 300 
WHERE call_id = 'CALL20260404001';
```

---

### 15. 用户表（users）

**表说明**：存储系统用户信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| username | VARCHAR | 50 | 否 | 否 | - | 用户名 |
| password | VARCHAR | 255 | 否 | 否 | - | 密码（加密存储） |
| email | VARCHAR | 100 | 是 | 是 | NULL | 邮箱 |
| role | VARCHAR | 20 | 否 | 否 | 'user' | 用户角色 |
| permissions | JSON | - | 是 | 是 | NULL | 权限列表 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (username)
- INDEX idx_username (username)

**用户角色枚举**：
- `admin` - 管理员
- `staff` - 员工
- `guest` - 访客

**示例数据**：
```sql
INSERT INTO users (username, password, email, role, permissions) 
VALUES ('admin', '$2a$10$...', 'admin@example.com', 'admin', '["read","write","delete"]');
```

---

### 15. 安防事件表（security_events）

**表说明**：存储安防相关事件

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| device_id | VARCHAR | 50 | 否 | 否 | - | 设备ID |
| event_type | VARCHAR | 20 | 否 | 否 | - | 事件类型 |
| event_data | TEXT | - | 是 | 是 | NULL | 事件数据 |
| event_level | VARCHAR | 20 | 否 | 否 | 'info' | 事件级别 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_device_id (device_id)
- INDEX idx_created_at (created_at)
- INDEX idx_event_type (event_type)

**事件类型枚举**：
- `door_open` - 门被打开
- `motion_detected` - 检测到人体
- `intrusion` - 入侵报警
- `door_broken` - 门磁被破坏
- `card_swipe` - 刷卡开门
- `remote_open` - 远程开门

**事件级别枚举**：
- `info` - 信息
- `warning` - 警告
- `critical` - 严重
- `emergency` - 紧急

**示例数据**：
```sql
INSERT INTO security_events (device_id, event_type, event_data, event_level) 
VALUES ('SUB2001', 'intrusion', '{"sensor": "motion", "location": "living_room"}', 'critical');
```

---

### 16. 设备认证表（device_auth）

**表说明**：存储设备认证信息

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| device_id | VARCHAR | 50 | 否 | 否 | - | 设备ID |
| token | VARCHAR | 255 | 否 | 否 | - | 认证Token |
| expires_at | DATETIME | - | 否 | 否 | - | 过期时间 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE KEY (device_id)
- INDEX idx_token (token)
- INDEX idx_expires_at (expires_at)

**示例数据**：
```sql
INSERT INTO device_auth (device_id, token, expires_at) 
VALUES ('MAIN001', 'eyJhbGciOiJIUzI1NiIs...', '2024-01-19 00:00:00');
```

---

### 17. 系统日志表（system_logs）

**表说明**：存储系统操作日志

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| user_id | INT | 11 | 是 | 是 | NULL | 用户ID |
| device_id | VARCHAR | 50 | 是 | 是 | NULL | 设备ID |
| operation | VARCHAR | 50 | 否 | 否 | - | 操作类型 |
| operation_data | TEXT | - | 是 | 是 | NULL | 操作数据 |
| ip_address | VARCHAR | 45 | 是 | 是 | NULL | IP地址 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_user_id (user_id)
- INDEX idx_device_id (device_id)
- INDEX idx_created_at (created_at)
- INDEX idx_operation (operation)

**操作类型枚举**：
- `login` - 登录
- `logout` - 登出
- `device_register` - 设备注册
- `device_control` - 设备控制
- `data_query` - 数据查询
- `user_create` - 用户创建
- `user_update` - 用户更新
- `user_delete` - 用户删除

**示例数据**：
```sql
INSERT INTO system_logs (user_id, operation, operation_data, ip_address) 
VALUES (1, 'login', '{"username": "admin"}', '192.168.1.100');
```

---

### 18. 配网记录表（network_config）

**表说明**：存储设备配网记录

**表结构**：

| 字段名 | 类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|----------|--------|------|
| id | INT | 11 | 是 | 否 | - | 自增主键 |
| device_id | VARCHAR | 50 | 否 | 否 | - | 设备ID |
| ssid | VARCHAR | 100 | 否 | 否 | - | WiFi名称 |
| signal_strength | INT | 11 | 是 | 是 | NULL | 信号强度 |
| config_method | VARCHAR | 20 | 否 | 否 | - | 配网方式 |
| config_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 配网时间 |
| status | VARCHAR | 20 | 否 | 否 | 'success' | 配网状态 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_device_id (device_id)
- INDEX idx_config_time (config_time)

**配网方式枚举**：
- `bluetooth` - 蓝牙配网
- `usb` - USB串口配网
- `ap` - AP模式配网

**配网状态枚举**：
- `success` - 成功
- `failed` - 失败
- `timeout` - 超时

**示例数据**：
```sql
INSERT INTO network_config (device_id, ssid, signal_strength, config_method, status) 
VALUES ('MAIN001', 'MyWiFi', -65, 'bluetooth', 'success');
```

---

## 🔗 ER图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              数据库ER图                                       │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│    hotels       │
├─────────────────┤
│ id (PK)         │
│ hotel_name (UK) │
│ hotel_address   │
│ hotel_phone     │
│ hotel_star      │
│ total_rooms     │
│ occupancy_rate  │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│    rooms        │
├─────────────────┤
│ id (PK)         │
│ room_number (UK)│
│ room_type       │
│ room_price      │
│ room_status     │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│   bookings      │
├─────────────────┤
│ id (PK)         │
│ booking_number  │
│ room_id (FK)    │
│ guest_name      │
│ check_in_date   │
│ check_out_date  │
│ status          │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│   payments      │
├─────────────────┤
│ id (PK)         │
│ payment_no      │
│ order_id        │
│ order_type      │
│ amount          │
│ status          │
│ created_at      │
│ updated_at      │
└─────────────────┘

┌─────────────────┐
│   members       │
├─────────────────┤
│ id (PK)         │
│ phone (UK)      │
│ name            │
│ member_level    │
│ points          │
│ balance         │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│ member_coupons  │
├─────────────────┤
│ id (PK)         │
│ member_id (FK)  │
│ coupon_id (FK)  │
│ status          │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│   coupons       │
├─────────────────┤
│ id (PK)         │
│ coupon_name     │
│ coupon_type     │
│ discount_value  │
│ valid_from      │
│ valid_to        │
│ created_at      │
│ updated_at      │
└─────────────────┘

         ▼
┌─────────────────┐
│ delivery_orders │
├─────────────────┤
│ id (PK)         │
│ order_no        │
│ room_id (FK)    │
│ item_category   │
│ status          │
│ created_at      │
│ updated_at      │
└─────────────────┘

         ▼
┌─────────────────┐
│maintenance_     │
│   tickets       │
├─────────────────┤
│ id (PK)         │
│ ticket_no       │
│ room_id (FK)    │
│ fault_type      │
│ priority        │
│ status          │
│ created_at      │
│ updated_at      │
└─────────────────┘

         ▼
┌─────────────────┐
│    reviews      │
├─────────────────┤
│ id (PK)         │
│ order_id        │
│ order_type      │
│ score           │
│ content         │
│ created_at      │
└─────────────────┘

┌─────────────────┐
│    devices      │
├─────────────────┤
│ id (PK)         │
│ device_id (UK)  │
│ device_type     │
│ device_name     │
│ device_key      │
│ device_status   │
│ firmware_version│
│ last_seen       │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│ sensor_data     │
├─────────────────┤
│ id (PK)         │
│ device_id (FK)  │
│ sensor_type     │
│ sensor_value    │
│ created_at      │
└─────────────────┘

         ▼
┌─────────────────┐
│ control_commands│
├─────────────────┤
│ id (PK)         │
│ device_id (FK)  │
│ command_type    │
│ command_value   │
│ command_status  │
│ created_by (FK) │
│ created_at      │
│ executed_at     │
└─────────────────┘

         ▼
┌─────────────────┐
│ security_events │
├─────────────────┤
│ id (PK)         │
│ device_id (FK)  │
│ event_type      │
│ event_data      │
│ event_level     │
│ created_at      │
└─────────────────┘

┌─────────────────┐
│ device_auth     │
├─────────────────┤
│ id (PK)         │
│ device_id (FK)  │
│ token           │
│ expires_at      │
│ created_at      │
│ updated_at      │
└─────────────────┘

┌─────────────────┐
│ network_config  │
├─────────────────┤
│ id (PK)         │
│ device_id (FK)  │
│ ssid            │
│ signal_strength │
│ config_method   │
│ config_time     │
│ status          │
└─────────────────┘

┌─────────────────┐
│    users        │
├─────────────────┤
│ id (PK)         │
│ username (UK)   │
│ password        │
│ email           │
│ role            │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│ system_logs     │
├─────────────────┤
│ id (PK)         │
│ user_id (FK)    │
│ device_id (FK)  │
│ operation       │
│ operation_data  │
│ ip_address      │
│ created_at      │
└─────────────────┘
```

---

## 📝 数据库索引优化

### 常用查询索引

#### 1. 房间状态查询
```sql
CREATE INDEX idx_rooms_status ON rooms(room_status);
CREATE INDEX idx_rooms_type ON rooms(room_type);
```

#### 2. 预订查询
```sql
CREATE INDEX idx_bookings_status ON bookings(status);
CREATE INDEX idx_bookings_date ON bookings(check_in_date, check_out_date);
CREATE INDEX idx_bookings_room ON bookings(room_id);
```

#### 3. 支付查询
```sql
CREATE INDEX idx_payments_status ON payments(status);
CREATE INDEX idx_payments_order ON payments(order_type, order_id);
```

#### 4. 会员查询
```sql
CREATE INDEX idx_members_level ON members(member_level);
CREATE INDEX idx_members_phone ON members(phone);
```

#### 5. 优惠券查询
```sql
CREATE INDEX idx_coupons_valid ON coupons(valid_from, valid_to);
CREATE INDEX idx_member_coupons_status ON member_coupons(status);
```

#### 6. 送物订单查询
```sql
CREATE INDEX idx_delivery_orders_status ON delivery_orders(status);
CREATE INDEX idx_delivery_orders_room ON delivery_orders(room_id);
```

#### 7. 报修工单查询
```sql
CREATE INDEX idx_maintenance_tickets_status ON maintenance_tickets(status);
CREATE INDEX idx_maintenance_tickets_priority ON maintenance_tickets(priority);
CREATE INDEX idx_maintenance_tickets_room ON maintenance_tickets(room_id);
```

#### 8. 传感器数据查询
```sql
CREATE INDEX idx_sensor_data_device_type ON sensor_data(device_id, sensor_type);
CREATE INDEX idx_sensor_data_time ON sensor_data(created_at);
```

#### 9. 控制指令查询
```sql
CREATE INDEX idx_commands_status ON control_commands(command_status);
CREATE INDEX idx_commands_device ON control_commands(device_id);
```

#### 10. 安防事件查询
```sql
CREATE INDEX idx_security_events_device ON security_events(device_id);
CREATE INDEX idx_security_events_time ON security_events(created_at);
```

---

## 🔐 数据库安全

### 用户权限
```sql
-- 创建数据库用户
CREATE USER 'hotel_user'@'localhost' IDENTIFIED BY 'password';

-- 授权
GRANT SELECT, INSERT, UPDATE, DELETE ON iot_hotel_system.* TO 'hotel_user'@'localhost';
GRANT EXECUTE ON iot_hotel_system.* TO 'hotel_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;
```

### 数据加密
- 密码字段使用bcrypt加密
- 敏感数据使用AES加密
- 传输使用SSL/TLS加密
- 身份证号等敏感信息加密存储

---

## 📊 数据库备份

### 备份脚本
```bash
#!/bin/bash

# 备份数据库
mysqldump -u root -p<password> iot_hotel_system > /backup/iot_hotel_system_$(date +%Y%m%d).sql

# 压缩备份文件
gzip /backup/iot_hotel_system_$(date +%Y%m%d).sql

# 删除7天前的备份
find /backup -name "iot_hotel_system_*.sql.gz" -mtime +7 -delete
```

### 恢复脚本
```bash
#!/bin/bash

# 恢复数据库
gunzip -c /backup/iot_hotel_system_20240118.sql.gz | mysql -u root -p<password> iot_hotel_system
```

---

## 🚀 数据库迁移

### 迁移脚本
```sql
-- 版本2.0.0
-- 智慧酒店三层架构升级

-- 创建新表
CREATE TABLE hotels (...);
CREATE TABLE rooms (...);
CREATE TABLE bookings (...);
CREATE TABLE payments (...);
CREATE TABLE members (...);
CREATE TABLE coupons (...);
CREATE TABLE member_coupons (...);
CREATE TABLE delivery_orders (...);
CREATE TABLE maintenance_tickets (...);
CREATE TABLE reviews (...);

-- 更新现有表结构
ALTER TABLE devices ADD COLUMN device_type ENUM('main', 'sub1', 'sub2') NOT NULL DEFAULT 'main';
ALTER TABLE devices CHANGE device_type device_type VARCHAR(20) NOT NULL DEFAULT 'main';
```

---

## 📚 相关文档

- [系统架构文档](./01-系统架构/README.md)
- [API接口文档](./API接口文档.md)
- [部署运维文档](./04-部署运维/README.md)

---

**文档版本**：v2.0.0  
**最后更新**：2026年4月4日  
**适用项目**：智慧酒店物联网控制系统  
**架构设计**："前台管理端-楼控-客房端"三层架构 + 云端协同
