# 智慧酒店物联网控制系统 - 软件系统需求

## 📋 文档概述

### 文档说明
本文档描述智慧酒店物联网控制系统的软件系统需求，包括云端服务器、Web管理后台、手机端H5/小程序、前端界面等各个软件模块的功能需求、技术实现和接口设计。

### 适用范围
- 云端服务器开发
- Web管理后台开发（Vue.js 3）
- 手机端H5/小程序开发（住客/员工端）
- 嵌入式固件开发（前台管理端/楼控/客房端）
- 前端Web界面开发

### 文档版本
- **v6.0.0** - 2026年4月
  - **新增客房端语音通话功能**：与前台实时双向语音通信
  - **新增 WebRTC/MQTT 音频传输适配需求**
  - **新增前端语音通话 UI 组件选型**
  - v5.0.0 基础上重构为智慧酒店三层架构
  - 新增预订系统、支付接口、会员机制、优惠券机制
  - 移除桌面应用，统一使用Vue Web管理后台
  - 保留AI管家系统，前端语音识别（APP端ASR），统一后端处理文字指令

---

## 🏨 系统架构

### 1.1 架构设计

智慧酒店系统采用**三层架构 + 云端协同**设计：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         智慧酒店三层架构                                │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                  🖥️ 前台电脑端（管理员）                            │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │ │
│  │  │ GUI工作台     │  │ 🤖酒店管家   │  │ MQTT客户端   │             │ │
│  │  │ (酒店管理)    │  │ (AI语音/工单)│  │ (WiFi/有线)  │             │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘             │ │
│  └─────────┼────────────────┼────────────────┼─────────────────────────┘ │
│            │                │                │                           │
│            └────────────────┼────────────────┘                           │
│                             │ USB串口                                    │
│  ┌──────────────────────────┼──────────────────────────────────────────┐ │
│  │                 🔌 前台管理端硬件（USB外设）                         │ │
│  │  ┌──────┐ ┌────┐ ┌──────┐ ┌────┐ ┌─────┐ ┌──────┐                 │ │
│  │  │RFID  │ │RGB │ │ 按键  │ │蜂鸣 │ │RS485│ │ UART │                 │ │
│  │  │RC522 │ │灯带 │ │+SOS  │ │器   │ │模块 │ │扩展口│                 │ │
│  │  └──┬───┘ └────┘ └──────┘ └────┘ └──┬──┘ └──┬───┘                 │ │
│  └─────┼────┴────┘ └──────┘ └────┘ └──┬──┘ └──┬───┘                 │ │
│        │                               │        │                       │ │
│  ┌─────▼───────┐  ┌───────────────────▼────────▼───────┐             │ │
│  │  🏢 楼控     │  │  🛏️ 客房端                          │             │ │
│  │  楼层管理    │  │  客房控制                            │             │ │
│  └─────────────┘  └─────────────────────────────────────┘             │ │
│                                                                         │ │
└─────────────────────────────────────────────────────────────────────────┘ │
                                    ↕ MQTT/API                            │
┌─────────────────────────────────────────────────────────────────────────┐ │
│                    📱 手机端（住客/员工）                               │ │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                 │ │
│  │ 客房服务      │  │ 联系前台     │  │ 设备控制      │                 │ │
│  │ (送物/报修)   │  │ (通话/消息)  │  │ (灯光/空调)   │                 │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                 │ │
│         │                  │                  │                          │ │
│         └──────────────────┼──────────────────┘                          │ │
│                            │                                             │ │
│                   ┌────────▼────────┐                                   │ │
│                   │   Node.js 后端   │                                   │ │
│                   │   (Express.js)   │                                   │ │
│                   └────────┬─────────┘                                   │ │
│                            │                                              │ │
│              ┌─────────────┼─────────────┐                               │ │
│              ▼             ▼             ▼                               │ │
│        ┌──────────┐  ┌──────────┐  ┌──────────┐                         │ │
│        │ 🤖 AI管家  │  │  MySQL   │  │  Redis   │                         │ │
│        │ (统一服务) │  │  (主库)   │  │  (缓存)   │                         │ │
│        └──────────┘  └──────────┘  └──────────┘                         │ │
│              │                                                           │ │
│              ▼                                                           │ │
│        ┌──────────┐                                                      │ │
│        │  MQTT    │                                                      │ │
│        │  (消息)   │                                                      │ │
│        └──────────┘                                                      │ │
│                                                                          │ │
│  ┌───────────────────────────────────────────────────────────────────┐ │ │
│  │                    数据存储层                                       │ │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐                         │ │ │
│  │  │  MySQL   │  │  Redis   │  │  MQTT    │                         │ │ │
│  │  │  (主库)   │  │  (缓存)   │  │  (消息)   │                         │ │ │
│  │  └──────────┘  └──────────┘  └──────────┘                         │ │ │
│  └───────────────────────────────────────────────────────────────────┘ │ │
└─────────────────────────────────────────────────────────────────────────┘ │
```

### 1.2 技术栈

**前端技术栈（统一Vue框架，多角色视图）**：
- Web前端：Vue.js 3 + Ant Design Vue + ECharts
- 状态管理：Pinia
- HTTP客户端：Axios
- WebSocket客户端：Socket.io-client
- 路由管理：Vue Router 4
- 组件库：Ant Design Vue (UI组件) + Chart.js (图表)
- 多角色视图架构：
  - **管理员视图**（Web管理后台）：入住/退房管理、房卡管理、客房状态监控、预订管理、数据分析报表
  - **住客视图**（手机端H5/小程序）：客房服务请求、设备控制、酒店信息、个人中心
  - **员工视图**（服务员/维修工移动端）：客房状态查看、服务记录

**后端技术栈**：
  - 后端框架：Node.js + Express
  - 数据库：MySQL（主数据库）+ Redis（缓存）
  - 消息队列：MQTT Broker（Mosquitto）
  - 实时通信：WebSocket（Socket.io）

**部署架构**：
  - 前端部署：Nginx 反向代理，静态资源托管
  - 后端部署：Node.js 服务（PM2 进程管理）
  - 数据库部署：MySQL 主从复制 + Redis 集群


**后端技术栈**：
- 后端框架：Node.js + Express
- 数据库：MySQL（主数据库）+ Redis（缓存）
- 消息队列：MQTT Broker（Mosquitto）
- 实时通信：WebSocket（Socket.io）

### 1.3 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         智慧酒店三层架构                                │
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │
│  │  📱 手机客户端  │  │  💻 Web管理后台   │  │  🖥️ 楼控/客房端 │                  │
│  │  Vue.js PWA   │  │  Vue.js 3       │  │  ESP32终端   │                  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                  │
│         │                  │                  │                          │
│         └──────────────────┼──────────────────┘                          │
│                            │                                             │
│                   ┌────────▼────────┐                                   │
│                   │   Node.js 后端   │                                   │
│                   │   (Express.js)   │                                   │
│                   └────────┬─────────┘                                   │
│                            │                                              │
│              ┌─────────────┼─────────────┐                               │
│              ▼             ▼             ▼                               │
│        ┌──────────┐  ┌──────────┐  ┌──────────┐                         │
│        │  MySQL   │  │  Redis   │  │  MQTT    │                         │
│        │  (主库)   │  │  (缓存)   │  │  (消息)   │                         │
│        └──────────┘  └──────────┘  └──────────┘                         │
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                    数据存储层                                       │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐                         │ │
│  │  │  MySQL   │  │  Redis   │  │  MQTT    │                         │ │
│  │  │  (主库)   │  │  (缓存)   │  │  (消息)   │                         │ │
│  │  └──────────┘  └──────────┘  └──────────┘                         │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

### 1.4 模块划分

**Web管理后台模块**：
- GUI工作台（酒店管理界面）
- MQTT客户端（设备通信）

**手机端模块**：
- 客房服务（送物、报修）
- 设备控制（灯光、空调、窗帘）
- 酒店信息（设施、周边推荐）
- 个人中心（个人信息、订单记录、评价记录）

**后端模块**：
- API服务模块（RESTful API）
- MQTT服务模块（设备通信）
- WebSocket服务模块（实时通信）
- 数据处理模块（数据存储和分析）
- 设备管理模块（设备注册、认证、管理）
- 指令调度模块（指令下发、路由）
- 用户管理模块（用户认证、权限管理）
- 预订管理模块（预订创建、确认、入住、退房）
- 支付管理模块（支付创建、微信支付、支付宝支付、支付结果确认）
- 会员管理模块（会员注册、升级、积分管理）
- 优惠券管理模块（优惠券发放、使用、核销）

---

## ☁️ 云端服务器需求

### 2.1 数据管理

#### 2.1.1 数据存储

**功能描述**：存储所有酒店数据、用户数据、系统日志

**存储内容**：
- **酒店信息**：酒店基本信息、房间信息、设施信息、周边推荐
- **房间信息**：房号、房型、价格、状态（空闲/入住/维修/SOS）
- **住客信息**：姓名、身份证号、房号、入住时间、退房时间
- **房卡信息**：房卡号、房号、有效期、权限等级
- **预订信息**：预订号、房号、入住日期、退房日期、状态、支付状态
- **支付信息**：支付单号、订单ID、金额、支付方式、交易ID、状态
- **会员信息**：会员ID、姓名、手机号、会员等级、积分、有效期
- **优惠券信息**：优惠券码、优惠券名称、优惠类型、折扣值、有效期、使用状态
- **设备信息**：设备ID、设备类型、设备状态、固件版本
- **传感器数据**：温度、湿度、空气质量、光照等
- **控制指令**：开灯、关灯、开门、空调控制等
- **送物订单**：订单号、房号、物品类别、物品名称、数量、备注、状态、时间
- **报修工单**：工单号、房号、故障类型、故障描述、照片、优先级、状态、处理人、时间
- **服务评价**：评价ID、房号、订单/工单ID、评分（1-5星）、评价内容、时间
- **用户信息**：用户名、密码、角色（管理员/服务员/维修工/住客）、权限
- **系统日志**：操作日志、错误日志、报警日志、入住/退房日志、支付日志、会员日志

**存储方式**：MySQL（主数据库）+ Redis（缓存）

**数据备份**：每日自动备份，保留90天

#### 2.1.2 数据查询

**功能描述**：提供数据查询接口

**查询功能**：
- 实时数据查询（房间状态、设备状态）
- 历史数据查询（入住记录、订单记录、工单记录）
- 统计数据查询（入住率、订单量、工单量、满意度）
- 设备状态查询（在线/离线、健康状态）
- 送物订单查询（按房号/订单号/状态查询）
- 报修工单查询（按房号/工单号/优先级/状态查询）
- 预订订单查询（按预订号/房号/日期查询）
- 支付记录查询（按支付单号/订单号/状态查询）
- 会员信息查询（按会员ID/手机号查询）
- 优惠券查询（按优惠券码/会员查询）

**查询方式**：RESTful API

**查询限制**：单次查询最多1000条记录

#### 2.1.3 数据分析

**功能描述**：对采集的数据进行分析

**分析功能**：
- 环境数据趋势分析（温度、湿度、空气质量）
- 设备使用频率统计（灯光、空调、窗帘使用频率）
- 异常数据检测（设备离线、传感器异常）
- 能耗分析（各房间能耗统计）
- 酒店运营分析（入住率、订单量、工单量、满意度）
- 送物订单分析（热门物品、配送时效）
- 报修工单分析（故障类型分布、处理时效）
- 预订分析（预订量、预订率、预订取消率、平均预订周期）
- 支付分析（支付方式分布、支付成功率、平均支付金额）
- 会员分析（会员数量、会员等级分布、会员活跃度、会员复购率）
- 优惠券分析（优惠券发放量、使用率、优惠券核销金额）

**分析结果**：生成报表和图表

### 2.2 设备管理

#### 2.2.1 设备注册

**功能描述**：设备启动时自动注册到后端

**注册信息**：
- 设备ID（唯一标识）
- 设备类型（前台管理端/楼控/客房端）
- 设备名称
- 设备能力（支持的传感器和执行器）
- 固件版本
- 注册时间

**注册时机**：设备启动时自动注册

**注册方式**：MQTT或HTTP

#### 2.2.2 设备认证

**功能描述**：验证设备身份，防止未授权设备接入

**认证方式**：设备ID + 设备密钥

**认证流程**：
1. 设备发送认证请求（设备ID + 设备密钥）
2. 后端验证设备身份
3. 验证通过，返回认证Token
4. 设备使用Token进行后续通信

**Token有效期**：24小时

#### 2.2.3 设备状态监控

**功能描述**：实时监控设备状态

**监控内容**：
- 在线/离线状态
- 设备健康状态
- 设备运行时间
- 设备错误信息

**监控方式**：心跳检测（30秒）

**离线报警**：设备离线超过5分钟，发送报警通知

#### 2.2.4 设备控制

**功能描述**：向设备发送控制指令

**控制方式**：MQTT消息

**控制指令**：
- 继电器控制（开/关）
- LED控制（颜色/亮度）
- 电控锁控制（开锁/上锁）
- 安防模式控制（布防/撤防）
- 空调控制（开关、温度、模式）
- 窗帘控制（开/关/暂停）
- 灯光控制（开关、亮度、颜色）

**控制响应**：设备返回执行结果

### 2.3 指令调度

#### 2.3.1 指令下发

**功能描述**：接收前端指令并下发给对应设备

**指令来源**：
- 前端用户操作（前台电脑/手机端）
- 定时任务
- 场景模式
- 语音指令

**指令处理**：
- 指令解析与路由
- 指令优先级管理
- 指令执行结果反馈
- 超时重试机制（3次）
- 指令日志记录

#### 2.3.2 指令路由

**功能描述**：根据设备类型和ID路由指令

**路由规则**：
- 前台管理端指令 → 前台管理端设备
- 楼控指令 → 楼控设备
- 客房端指令 → 客房端设备

**路由方式**：MQTT Topic路由

### 2.4 MQTT服务

#### 2.4.1 消息路由

**功能描述**：路由设备之间的消息

**MQTT Broker**：Mosquitto

**Topic设计**：
- 设备上行：`device/{device_id}/data`
- 设备下行：`device/{device_id}/command`
- 设备状态：`device/{device_id}/status`
- 广播消息：`broadcast/{message_type}`

**QoS等级**：QoS 1（至少一次）

**保留消息**：支持

#### 2.4.2 消息处理

**功能描述**：处理设备发送的MQTT消息

**处理流程**：
1. 接收MQTT消息
2. 解析消息内容
3. 验证消息格式
4. 存储到数据库
5. 转发到WebSocket客户端

**消息格式**：JSON

### 2.5 WebSocket服务

#### 2.5.1 实时通信

**功能描述**：提供实时通信能力

**WebSocket库**：Socket.io

**通信内容**：
- 传感器数据实时推送
- 设备状态实时推送
- 报警信息实时推送
- 控制指令实时推送
- 送物订单状态推送
- 报修工单状态推送

**推送频率**：
- 传感器数据：1-10秒
- 报警信息：实时
- 送物订单：实时
- 报修工单：实时

#### 2.5.2 客户端连接

**功能描述**：管理客户端WebSocket连接

**连接管理**：
- 客户端认证
- 连接状态管理
- 心跳检测
- 异常断开重连

**最大连接数**：100个并发连接

### 2.6 📞 语音通话服务（v6.0新增）

#### 2.6.1 功能概述
**功能描述**：提供客房端与前台管理端之间的实时双向语音通信能力

**业务场景**：
- **住客发起**：住客按客房终端"呼叫前台键" → 发起语音通话请求
- **前台发起**：前台主动呼叫指定房间（退房提醒、紧急通知、服务回访等）
- 前台Web界面收到来电通知 → 接听/拒接/转接
- 通话中双方实时语音交互（咨询、报修、送物请求等）
- SOS求助时自动升级为紧急语音通道

#### 2.6.2 技术方案选型

| 方案 | 架构 | 延迟 | 带宽 | 复杂度 | 推荐场景 |
|------|------|------|------|--------|----------|
| **A: WebRTC + Socket.io信令**（推荐） | P2P直连+STUN/TURN | **≤150ms** | 自适应(Opus) | 中等 | 生产环境，支持NAT穿透 |
| B: MQTT + Opus编码 | 中心转发 | ~200-300ms | ~32kbps | 低 | 纯局域网，复用MQTT基础设施 |
| C: WebSocket + PCM原始音频 | 中心转发 | ~150ms | ~256kbps | 最低 | 快速原型/MVP验证 |

**推荐方案 A（WebRTC）理由**：
1. **低延迟**：P2P直连避免服务器中转瓶颈
2. **自适应码率**：Opus 编码自动适应网络状况（8kbps~128kbps）
3. **回声消除(AEC)内置**：浏览器原生支持，无需额外开发
4. **NAT穿透**：STUN/TURN 解决酒店内网/公网互通问题
5. **前端生态成熟**：Vue 组件库丰富（vue-webrtc、simple-peer）

#### 2.6.3 后端适配需求（Node.js + Express）

##### 信令服务器（Signaling Server）
```
┌─────────────── 信令流程 ───────────────────────────┐
│                                                    │
│  [客房端]                    [后端]        [前台端] │
│     │                          │              │    │
│     │  ① POST /call/request    │              │    │
│     │ ───────────────────────▶ │              │    │
│     │                          │  ② WS emit   │    │
│     │                          │ ────────────▶│    │
│     │                          │              │    │
│     │                          │  ③ WS answer │    │
│     │                          │ ◀────────────│    │
│     │  ④ POST /call/answer     │              │    │
│     │ ◀─────────────────────── │              │    │
│     │                          │              │    │
│     │ ═════ WebRTC P2P 直连开始 ══════════════ │    │
│     │ ═════ (音频流不再经过后端) ══════════════ │    │
│                                                    │
└────────────────────────────────────────────────────┘
```

**新增 API 接口**：

```
# 语音通话 - 信令接口
POST   /api/v1/calls/initiate          # 客房端发起呼叫 {room_id, type:"voice"}
POST   /api/v1/calls/:id/answer         # 前台接听
POST   /api/v1/calls/:id/reject         # 前台拒接
POST   /api/v1/calls/:id/hangup         # 任方挂断
POST   /api/v1/calls/outbound           # 前台主动呼叫房间 {room_id}
GET    /api/v1/calls/:id/status          # 查询通话状态
GET    /api/v1/calls/active              # 获取当前活跃通话列表

# 通话记录
GET    /api/v1/calls/history             # 通话记录查询（分页）
GET    /api/v1/calls/stats               # 通话统计（时长/次数/接听率）
```

**WebSocket 事件扩展**（在现有 Socket.io 基础上）：

| 事件名 | 方向 | Payload | 说明 |
|--------|------|---------|------|
| `call:incoming` | 后台→前台 | `{from, room_id, timestamp}` | 来电通知（住客发起） |
| `call:outgoing` | 后台→客房端 | `{to, room_id, timestamp}` | 呼出通知（前台发起） |
| `call:answered` | 双向 | `{call_id, sdp_answer}` | 接听确认 |
| `call:ended` | 双向 | `{call_id, reason, duration}` | 通话结束 |
| `call:busy` | 后台→客房端 | `{room_id}` | 对方正忙 |
| `call:missed` | 后台→客房端 | `{room_id, caller_type}` | 未接来电通知 |

**数据库扩展**（新增 `calls` 表）：

```sql
CREATE TABLE calls (
  id INT PRIMARY KEY AUTO_INCREMENT,
  call_id VARCHAR(64) UNIQUE NOT NULL,       -- 唯一通话ID (UUID)
  caller_type ENUM('room','front_desk') NOT NULL, -- 主叫方类型
  caller_id VARCHAR(64) NOT NULL,            -- 主叫标识(房间号/员工ID)
  callee_type ENUM('room','front_desk') NOT NULL, -- 被叫方类型
  callee_id VARCHAR(64) NOT NULL,            -- 被叫标识
  status ENUM('calling','ringing','connected','ended','rejected','missed') DEFAULT ' ringing',
  started_at DATETIME NOT NULL,
  answered_at DATETIME NULL,
  ended_at DATETIME NULL,
  duration_sec INT DEFAULT 0,                -- 通话时长(秒)
  recording_url VARCHAR(512) NULL,           -- 录音文件URL(可选)
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_caller(caller_type, caller_id),
  INDEX idx_callee(callee_type, callee_id),
  INDEX idx_status(status)
);
```

#### 2.6.4 前端 Web 应用选型（语音通话 UI）

##### 管理端（前台端）- 通话组件选型

| 库名 | npm 包 | 特点 | 成熟度 | 推荐 |
|------|--------|------|--------|------|
| **vue-webrtc** | `vue-webrtc` | Vue3封装，开箱即用 | ⭐⭐⭐ | ✅ 首选 |
| simple-peer | `simple-peer` | 轻量WebRTC封装，需自行写UI | ⭐⭐⭐⭐ | ✅ 高度定制时选用 |
| @livekit/components-vue | `@livekit/components-vue` | LiveKit SFU架构，功能完整 | ⭐⭐⭐⭐⭐ | 企业级备选 |
| janus-gateway | `janus-gateway` | 服务端MCU架构 | ⭐⭐⭐ | 大规模并发备选 |

**推荐：`vue-webrtc` + `simple-peer` 组合**
- MVP阶段使用 `vue-webrtc` 快速实现通话面板
- 后续若需自定义UI（通话状态动画、DTMF按键等），切换至 `simple-peer`

##### 前台端通话 UI 组件设计

```
┌─────────────────────────────────────────────┐
│  📞 语音通话面板（嵌入前台端侧边栏/浮动窗）    │
├─────────────────────────────────────────────┤
│                                             │
│   ┌───────────────────────────────────┐     │
│   │                                   │     │
│   │      👤 301客房                   │     │
│   │                                   │     │
│   │      📞 通话中  ⏱ 02:35          │     │
│   │                                   │     │
│   └───────────────────────────────────┘     │
│                                             │
│   ┌─────┐ ┌─────┐ ┌─────┐                 │
│   │ 🎤  │ │ 🔇  │ │ 📞  │                 │
│   │静音 │ │免提 │ │挂断 │                 │
│   └─────┘ └─────┘ └─────┘                 │
│                                             │
│   [📋 创建工单] [📦 送物请求] [💰 账单查询]  │
│                                             │
└─────────────────────────────────────────────┘
```

##### 客户端（住客视图）- 通话入口

在现有 [GuestRoom.vue](../../frontend/iot-hotel-web/src/views/guest/GuestRoom.vue) 中增加"呼叫前台"按钮：

```vue
<!-- 客房服务页面增加语音通话入口 -->
<a-button type="primary" size="large" icon="phone" @click="startVoiceCall">
  📞 呼叫前台
</a-button>
```

来电弹窗组件（全局 Toast/Modal）：
```
┌──────────────────────────────┐
│  📞 前台来电                  │
│                              │
│  "您好，这里是智联酒店前台"    │
│                              │
│  ┌────────┐  ┌────────┐     │
│  │  接听  │  │  拒接  │     │
│  └────────┘  └────────┘     │
└──────────────────────────────┘
```

#### 2.6.5 ESP32 固件适配需求

##### 音频采集与播放（FreeRTOS 任务分配）

```
ESP32 双核任务分配:
┌─────────────────────────────────────────────────────┐
│ Core 0 (PRO_CPU)                                    │
│   ├── Task_MQTT        : MQTT通信 + 设备控制指令处理 │
│   ├── Task_GPIO        : 按键扫描 + LED控制          │
│   ├── Task_OLED        : OLED显示刷新               │
│   └── Task_MainLoop    : 主循环/心跳                 │
│                                                     │
│ Core 1 (APP_CPU)                                     │
│   ├── Task_AudioRecord  : I2S读取INMP441 → PCM缓冲区 │
│   ├── Task_AudioEncode  : PCM → Opus编码            │
│   ├── Task_AudioDecode  : Opus解码 → PCM缓冲区       │
│   ├── Task_AudioPlay    : PCM → DAC输出 → PAM8403   │
│   └── Task_WebRTC/WebSocket : 音频数据帧传输         │
└─────────────────────────────────────────────────────┘
```

##### 关键固件依赖库

| 库名 | 用途 | 许可证 |
|------|------|--------|
| `esp-opus` | Opus编解码（16kHz单声道） | BSD-3-Clause |
| `i2s_microphone` | INMP441 I2S驱动 | MIT |
| `WebSocketsClient` | WiFi音频传输（备用方案） | LGPL-2.1 |

### 2.7 API服务

#### 2.7.1 RESTful API

**功能描述**：提供RESTful API接口

**API框架**：Express.js

**API版本**：v1

**API基路径**：`/api/v1`

#### 2.7.2 API接口列表

**酒店信息接口**
```
GET    /api/v1/hotels/info              # 获取酒店基本信息
GET    /api/v1/hotels/rooms             # 获取房间列表
GET    /api/v1/hotels/rooms/:id         # 获取房间详情
PUT    /api/v1/hotels/rooms/:id/status  # 更新房间状态
```

**住客管理接口**
```
POST   /api/v1/guests/checkin           # 办理入住
POST   /api/v1/guests/checkout          # 办理退房
GET    /api/v1/guests/list              # 获取住客列表
GET    /api/v1/guests/:id               # 获取住客详情
```

**房卡管理接口**
```
POST   /api/v1/cards/issue              # 发放房卡
POST   /api/v1/cards/revoke             # 注销房卡
GET    /api/v1/cards/list               # 获取房卡列表
```

**设备管理接口**
```
GET    /api/v1/devices                  # 获取设备列表
GET    /api/v1/devices/:id              # 获取设备详情
POST   /api/v1/devices/:id/control      # 控制设备
DELETE /api/v1/devices/:id              # 删除设备
```

**传感器数据接口**
```
GET    /api/v1/sensors/data             # 获取传感器数据
GET    /api/v1/sensors/data/latest      # 获取最新数据
GET    /api/v1/sensors/data/history     # 获取历史数据
```

**送物订单接口**
```
POST   /api/v1/orders/delivery          # 创建送物订单
GET    /api/v1/orders/list              # 获取订单列表
GET    /api/v1/orders/:id               # 获取订单详情
PUT    /api/v1/orders/:id/status        # 更新订单状态
```

**报修工单接口**
```
POST   /api/v1/tickets/repair           # 创建报修工单
GET    /api/v1/tickets/list             # 获取工单列表
GET    /api/v1/tickets/:id              # 获取工单详情
PUT    /api/v1/tickets/:id/status       # 更新工单状态
PUT    /api/v1/tickets/:id/assign       # 分配工单
```

**服务评价接口**
```
POST   /api/v1/reviews/submit           # 提交服务评价
GET    /api/v1/reviews/list             # 获取评价列表
GET    /api/v1/reviews/:id              # 获取评价详情
```

**用户管理接口**
```
POST   /api/v1/auth/login               # 用户登录
POST   /api/v1/auth/logout              # 用户登出
GET    /api/v1/users/profile            # 获取用户信息
PUT    /api/v1/users/profile            # 更新用户信息
```

**安防管理接口**
```
GET    /api/v1/security/status          # 获取安防状态
POST   /api/v1/security/arm             # 启动布防
POST   /api/v1/security/disarm          # 解除布防
GET    /api/v1/security/events          # 获取安防事件
```

**酒店信息接口**
```
GET    /api/v1/hotel/info               # 获取酒店设施信息
GET    /api/v1/hotel/recommendations   # 获取周边推荐
```

**预订系统接口**
```
POST   /api/v1/bookings/create          # 创建预订订单
GET    /api/v1/bookings/list            # 获取预订列表
GET    /api/v1/bookings/:id             # 获取预订详情
PUT    /api/v1/bookings/:id/confirm     # 确认预订
PUT    /api/v1/bookings/:id/checkin     # 办理入住
PUT    /api/v1/bookings/:id/checkout    # 办理退房
PUT    /api/v1/bookings/:id/cancel      # 取消预订
GET    /api/v1/bookings/available       # 查询可预订房间
```

**支付接口**
```
POST   /api/v1/payments/create          # 创建支付订单
POST   /api/v1/payments/wechat          # 微信支付
POST   /api/v1/payments/alipay          # 支付宝支付
POST   /api/v1/payments/confirm         # 支付结果确认
GET    /api/v1/payments/list            # 获取支付记录列表
GET    /api/v1/payments/:id             # 获取支付记录详情
```

**会员管理接口**
```
POST   /api/v1/members/register         # 会员注册
GET    /api/v1/members/list             # 获取会员列表
GET    /api/v1/members/:id              # 获取会员详情
PUT    /api/v1/members/:id/upgrade      # 会员升级
GET    /api/v1/members/:id/points       # 获取会员积分
POST   /api/v1/members/:id/points/add   # 添加积分
POST   /api/v1/members/:id/points/deduct# 扣除积分
```

**优惠券接口**
```
POST   /api/v1/coupons/issue            # 发放优惠券
GET    /api/v1/coupons/list             # 获取优惠券列表
GET    /api/v1/coupons/:id              # 获取优惠券详情
PUT    /api/v1/coupons/:id/use          # 使用优惠券
GET    /api/v1/coupons/available        # 获取可用优惠券
```

##### 2.6.3 API认证

**认证方式**：JWT（JSON Web Token）

**Token有效期**：7天

**认证流程**：
1. 用户登录，获取Token
2. 请求时在Header中携带Token
3. 服务端验证Token
4. 验证通过，返回数据

---

## 🖥️ Web管理后台需求

### 1. 系统架构

- **开发环境**：Vue.js 3 + Ant Design Vue
- **编程语言**：JavaScript/TypeScript
- **通信协议**：HTTP、WebSocket、MQTT
- **状态管理**：Pinia
- **路由管理**：Vue Router 4
- **操作系统**：跨平台（Windows/Mac/Linux）

### 2. 功能需求

#### 2.1 GUI工作台

**功能描述**：酒店管理主界面

**界面布局**：
- 顶部导航栏（系统标题、用户信息、退出登录）
- 左侧菜单栏（房务管理、客房状态、预订管理、数据分析、系统设置）
- 右侧内容区（动态显示各模块内容）
- 底部状态栏（系统状态、时间、网络连接）

**房务管理页面**：
- 入住办理（客户信息核验、房卡制作、信息录入）
- 退房办理（房卡读取、费用结算、信息注销）
- 房卡管理（房卡发放、注销、挂失）
- 住客信息查询

**客房状态页面**：
- 房间状态墙（实时显示各房间状态）
- 房间状态列表（房号、状态、住客、入住时间）
- 房间状态筛选（空闲/入住/维修/SOS）
- 房间状态统计（入住率、空房数）

**预订管理页面**：
- 预订订单列表（预订号、房号、入住日期、退房日期、状态、支付状态）
- 预订订单处理（确认预订、办理入住、办理退房、取消预订）
- 预订订单查询（按预订号/房号/日期查询）
- 可预订房间查询

**数据分析页面**：
- 入住率图表（折线图）
- 预订量图表（柱状图）
- 支付记录图表（饼图）
- 会员增长图表（折线图）
- 优惠券使用图表（饼图）
- 能耗分析图表

**系统设置页面**：
- 用户设置（用户名、密码、权限）
- 设备设置（设备ID、设备名称）
- 系统信息（版本号、硬件信息）

---

## 📱 手机端H5/小程序需求

### 1. 系统架构

- **开发环境**：Vue.js 3（PWA）
- **编程语言**：JavaScript/TypeScript
- **通信协议**：4G/WiFi、HTTP、WebSocket
- **操作系统**：Android/iOS/H5

**多端输入统一处理**：
- **电脑端**（Web后台）：文字输入 → 后端AI管家处理
- **手机端**（H5/小程序）：语音输入（前端ASR）→ 文字发送 → 后端AI管家处理
- **统一后端**：只有一个AI管家服务，统一处理所有端的文字指令

**前端语音识别**：
- 手机端使用浏览器Web Speech API或第三方ASR服务进行语音识别
- 识别后的文字发送给后端AI管家
- 后端只处理文字指令，不进行语音识别

### 2. 功能需求

#### 2.1 客房服务模块

**送物服务**：
- 物品选择（卫浴用品/餐饮服务/床上用品/日用品/特殊需求）
- 物品数量选择
- 备注填写
- 订单提交
- 订单状态查询（已接单/配送中/已送达）
- 订单评价

**报修服务**：
- 故障类型选择（紧急/重要/一般/轻微）
- 故障描述填写
- 照片上传（可选）
- 工单提交
- 工单状态查询（已提交/已受理/维修中/待确认/已完成）
- 工单评价

**联系前台**：
- 一键呼叫（文字聊天/VoIP通话）
- 文字消息发送
- 语音消息发送
- 消息记录查询

#### 2.2 设备控制模块

**功能描述**：控制客房设备

**控制功能**：
- 灯光控制（开关、亮度、颜色）
- 空调控制（开关、温度、模式）
- 窗帘控制（开/关/暂停）
- 电视控制（开关、输入源）

**控制方式**：
- 触控控制（APP界面）
- 场景模式（离家/回家/睡眠）

#### 2.3 酒店信息模块

**功能描述**：查询酒店信息

**信息内容**：
- 酒店设施介绍（餐厅/健身房/游泳池/商务中心）
- 周边推荐（餐厅/景点/商场/交通）
- 酒店服务介绍（送物/报修/洗衣/叫车）

**推荐方式**：
- 人气排行榜
- 价格筛选
- 距离筛选

#### 2.4 服务评价模块

**功能描述**：提交服务评价

**评价内容**：
- 送物服务评价（1-5星 + 评价内容）
- 报修服务评价（1-5星 + 评价内容）
- 前台服务评价（1-5星 + 评价内容）
- 整体住宿评价（1-5星 + 评价内容）

**评价提交**：
- 评价表单
- 星级评分
- 文字评价
- 匿名/实名选择

#### 2.5 个人中心模块

**功能描述**：用户个人信息管理

**功能模块**：
- 个人信息（姓名、房号、入住时间）
- 房卡管理（房卡号、有效期）
- 订单记录（送物订单、报修工单）
- 评价记录
- 消息通知
- 设置（语言、主题、通知）

---

## 📊 数据库设计

### 3.1 数据库表结构

#### 3.1.1 酒店信息表（hotels）

```sql
CREATE TABLE hotels (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    hotel_name VARCHAR(100) NOT NULL,
    hotel_address VARCHAR(200),
    hotel_phone VARCHAR(20),
    hotel_star INTEGER DEFAULT 3,
    room_count INTEGER DEFAULT 100,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 3.1.2 房间信息表（rooms）

```sql
CREATE TABLE rooms (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    room_number VARCHAR(10) UNIQUE NOT NULL,
    room_type VARCHAR(20) NOT NULL,
    room_price DECIMAL(10,2) NOT NULL,
    room_status VARCHAR(20) DEFAULT 'empty',
    floor VARCHAR(10),
    description TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_room_number (room_number),
    INDEX idx_room_status (room_status)
);
```

#### 3.1.3 住客信息表（guests）

```sql
CREATE TABLE guests (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    guest_name VARCHAR(50) NOT NULL,
    guest_id_card VARCHAR(18) UNIQUE NOT NULL,
    guest_phone VARCHAR(20),
    room_id INTEGER NOT NULL,
    checkin_time DATETIME NOT NULL,
    checkout_time DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (room_id) REFERENCES rooms(id),
    INDEX idx_guest_id_card (guest_id_card),
    INDEX idx_room_id (room_id)
);
```

#### 3.1.4 房卡信息表（cards）

```sql
CREATE TABLE cards (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    card_number VARCHAR(20) UNIQUE NOT NULL,
    room_id INTEGER NOT NULL,
    card_level VARCHAR(20) DEFAULT 'guest',
    expire_time DATETIME NOT NULL,
    card_status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (room_id) REFERENCES rooms(id),
    INDEX idx_card_number (card_number),
    INDEX idx_card_status (card_status)
);
```

#### 3.1.5 设备表（devices）

```sql
CREATE TABLE devices (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id VARCHAR(50) UNIQUE NOT NULL,
    device_type VARCHAR(20) NOT NULL,
    device_name VARCHAR(50) NOT NULL,
    device_key VARCHAR(50) NOT NULL,
    device_status VARCHAR(20) DEFAULT 'offline',
    firmware_version VARCHAR(20),
    last_seen DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_device_id (device_id),
    INDEX idx_device_status (device_status)
);
```

#### 3.1.6 传感器数据表（sensor_data）

```sql
CREATE TABLE sensor_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id VARCHAR(50) NOT NULL,
    sensor_type VARCHAR(20) NOT NULL,
    sensor_value VARCHAR(50) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_device_id (device_id),
    INDEX idx_sensor_type (sensor_type),
    INDEX idx_created_at (created_at)
);
```

#### 3.1.7 控制指令表（control_commands）

```sql
CREATE TABLE control_commands (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id VARCHAR(50) NOT NULL,
    command_type VARCHAR(20) NOT NULL,
    command_value VARCHAR(50) NOT NULL,
    command_status VARCHAR(20) DEFAULT 'pending',
    created_by VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    executed_at DATETIME,
    INDEX idx_device_id (device_id),
    INDEX idx_created_at (created_at)
);
```

#### 3.1.8 送物订单表（delivery_orders）

```sql
CREATE TABLE delivery_orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_number VARCHAR(20) UNIQUE NOT NULL,
    room_id INTEGER NOT NULL,
    category VARCHAR(20) NOT NULL,
    item_name VARCHAR(50) NOT NULL,
    quantity INTEGER NOT NULL,
    remarks TEXT,
    order_status VARCHAR(20) DEFAULT 'pending',
    created_by VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    accepted_at DATETIME,
    delivered_at DATETIME,
    completed_at DATETIME,
    rating INTEGER,
    review TEXT,
    FOREIGN KEY (room_id) REFERENCES rooms(id),
    INDEX idx_order_number (order_number),
    INDEX idx_order_status (order_status),
    INDEX idx_room_id (room_id)
);
```

#### 3.1.9 报修工单表（repair_tickets）

```sql
CREATE TABLE repair_tickets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ticket_number VARCHAR(20) UNIQUE NOT NULL,
    room_id INTEGER NOT NULL,
    fault_type VARCHAR(20) NOT NULL,
    fault_description TEXT,
    photo_url VARCHAR(200),
    priority VARCHAR(20) DEFAULT 'normal',
    ticket_status VARCHAR(20) DEFAULT 'pending',
    created_by VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    assigned_to VARCHAR(50),
    assigned_at DATETIME,
    started_at DATETIME,
    completed_at DATETIME,
    confirmed_at DATETIME,
    rating INTEGER,
    review TEXT,
    FOREIGN KEY (room_id) REFERENCES rooms(id),
    INDEX idx_ticket_number (ticket_number),
    INDEX idx_ticket_status (ticket_status),
    INDEX idx_priority (priority),
    INDEX idx_room_id (room_id)
);
```

#### 3.1.10 服务评价表（reviews）

```sql
CREATE TABLE reviews (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    review_type VARCHAR(20) NOT NULL,
    review_target_id INTEGER NOT NULL,
    rating INTEGER NOT NULL CHECK(rating >= 1 AND rating <= 5),
    review_content TEXT,
    is_anonymous BOOLEAN DEFAULT FALSE,
    created_by VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_review_type (review_type),
    INDEX idx_created_at (created_at)
);
```

#### 3.1.11 用户表（users）

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    role VARCHAR(20) DEFAULT 'user',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username)
);
```

#### 3.1.12 安防事件表（security_events）

```sql
CREATE TABLE security_events (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id VARCHAR(50) NOT NULL,
    event_type VARCHAR(20) NOT NULL,
    event_data TEXT,
    event_level VARCHAR(20) DEFAULT 'info',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_device_id (device_id),
    INDEX idx_created_at (created_at)
);
```

#### 3.1.13 操作日志表（operation_logs）

```sql
CREATE TABLE operation_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    operation_type VARCHAR(50) NOT NULL,
    operation_content TEXT,
    operation_result VARCHAR(20) DEFAULT 'success',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    INDEX idx_operation_type (operation_type),
    INDEX idx_created_at (created_at)
);
```

#### 3.1.14 预订订单表（bookings）

```sql
CREATE TABLE bookings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    booking_number VARCHAR(20) UNIQUE NOT NULL,
    guest_name VARCHAR(50) NOT NULL,
    guest_phone VARCHAR(20) NOT NULL,
    guest_id_card VARCHAR(18),
    room_id INTEGER NOT NULL,
    room_type VARCHAR(20) NOT NULL,
    checkin_date DATE NOT NULL,
    checkout_date DATE NOT NULL,
    booking_status VARCHAR(20) DEFAULT 'pending',
    payment_status VARCHAR(20) DEFAULT 'unpaid',
    total_price DECIMAL(10,2),
    deposit DECIMAL(10,2),
    remarks TEXT,
    created_by VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    confirmed_at DATETIME,
    cancelled_at DATETIME,
    FOREIGN KEY (room_id) REFERENCES rooms(id),
    INDEX idx_booking_number (booking_number),
    INDEX idx_booking_status (booking_status),
    INDEX idx_checkin_date (checkin_date),
    INDEX idx_checkout_date (checkout_date)
);
```

#### 3.1.15 支付记录表（payments）

```sql
CREATE TABLE payments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    payment_number VARCHAR(20) UNIQUE NOT NULL,
    order_id INTEGER NOT NULL,
    order_type VARCHAR(20) NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    payment_method VARCHAR(20) NOT NULL,
    payment_status VARCHAR(20) DEFAULT 'pending',
    transaction_id VARCHAR(50),
    paid_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (order_id) REFERENCES bookings(id),
    INDEX idx_payment_number (payment_number),
    INDEX idx_payment_status (payment_status),
    INDEX idx_order_id (order_id),
    INDEX idx_created_at (created_at)
);
```

#### 3.1.16 会员表（members）

```sql
CREATE TABLE members (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    member_id VARCHAR(20) UNIQUE NOT NULL,
    guest_name VARCHAR(50) NOT NULL,
    guest_phone VARCHAR(20) UNIQUE NOT NULL,
    guest_id_card VARCHAR(18) UNIQUE,
    member_level VARCHAR(20) DEFAULT 'normal',
    total_spent DECIMAL(10,2) DEFAULT 0,
    points INTEGER DEFAULT 0,
    membership_start DATE,
    membership_end DATE,
    member_status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_member_id (member_id),
    INDEX idx_member_phone (guest_phone),
    INDEX idx_member_level (member_level)
);
```

#### 3.1.17 会员积分记录表（member_points）

```sql
CREATE TABLE member_points (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    member_id INTEGER NOT NULL,
    points_change INTEGER NOT NULL,
    points_type VARCHAR(20) NOT NULL,
    related_order_id INTEGER,
    remarks TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (member_id) REFERENCES members(id),
    INDEX idx_member_id (member_id),
    INDEX idx_created_at (created_at)
);
```

#### 3.1.18 优惠券表（coupons）

```sql
CREATE TABLE coupons (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    coupon_code VARCHAR(20) UNIQUE NOT NULL,
    coupon_name VARCHAR(50) NOT NULL,
    coupon_type VARCHAR(20) NOT NULL,
    discount_value DECIMAL(10,2) NOT NULL,
    min_order_amount DECIMAL(10,2),
    max_discount DECIMAL(10,2),
    valid_start DATE NOT NULL,
    valid_end DATE NOT NULL,
    total_count INTEGER DEFAULT 1000,
    issued_count INTEGER DEFAULT 0,
    remaining_count INTEGER DEFAULT 1000,
    coupon_status VARCHAR(20) DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_coupon_code (coupon_code),
    INDEX idx_coupon_status (coupon_status),
    INDEX idx_valid_end (valid_end)
);
```

#### 3.1.19 优惠券发放记录表（coupon_issues）

```sql
CREATE TABLE coupon_issues (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    coupon_id INTEGER NOT NULL,
    member_id INTEGER,
    guest_phone VARCHAR(20),
    issue_method VARCHAR(20) NOT NULL,
    issued_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    used_at DATETIME,
    used_order_id INTEGER,
    FOREIGN KEY (coupon_id) REFERENCES coupons(id),
    FOREIGN KEY (member_id) REFERENCES members(id),
    INDEX idx_coupon_id (coupon_id),
    INDEX idx_member_id (member_id),
    INDEX idx_issued_at (issued_at)
);
```

#### 3.1.20 优惠券使用记录表（coupon_usages）

```sql
CREATE TABLE coupon_usages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    coupon_id INTEGER NOT NULL,
    order_id INTEGER NOT NULL,
    discount_amount DECIMAL(10,2) NOT NULL,
    used_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (coupon_id) REFERENCES coupons(id),
    FOREIGN KEY (order_id) REFERENCES bookings(id),
    INDEX idx_coupon_id (coupon_id),
    INDEX idx_order_id (order_id),
    INDEX idx_used_at (used_at)
);
```

### 3.2 数据库索引

```sql
CREATE INDEX idx_sensor_data_device_id ON sensor_data(device_id);
CREATE INDEX idx_sensor_data_sensor_type ON sensor_data(sensor_type);
CREATE INDEX idx_sensor_data_created_at ON sensor_data(created_at);
CREATE INDEX idx_control_commands_device_id ON control_commands(device_id);
CREATE INDEX idx_security_events_device_id ON security_events(device_id);
CREATE INDEX idx_security_events_created_at ON security_events(created_at);
CREATE INDEX idx_delivery_orders_room_id ON delivery_orders(room_id);
CREATE INDEX idx_repair_tickets_room_id ON repair_tickets(room_id);
CREATE INDEX idx_reviews_review_type ON reviews(review_type);
```

---

## 🚀 部署指南

### 1. 系统部署架构

#### 1.1 本地开发环境

```
┌─────────────────────────────────────────────────────────────┐
│                      本地开发环境                             │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  成员A本地   │  │  成员B本地   │  │  成员C本地   │          │
│  │  VS Code    │  │  VS Code    │  │  VS Code    │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
│         │                │                │                  │
│         └────────────────┼────────────────┘                  │
│                          │                                   │
│                  ┌───────▼───────┐                           │
│                  │  GitHub 远程仓库 │                           │
│                  └───────────────┘                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 1.2 云服务器部署环境

```
┌─────────────────────────────────────────────────────────────┐
│                      云服务器部署环境                          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                   后端服务器                           │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │   │
│  │  │ RESTful API  │  │ WebSocket    │  │ MQTT Broker│ │   │
│  │  │ 业务逻辑     │  │ 实时通信     │  │ 消息路由   │ │   │
│  │  └──────────────┘  └──────────────┘  └────────────┘ │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │   │
│  │  │ 数据存储     │  │ 设备管理     │  │ 指令调度   │ │   │
│  │  │ MySQL/Redis  │  │ 设备认证     │  │ 数据处理   │ │   │
│  │  └──────────────┘  └──────────────┘  └────────────┘ │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │   │
│  │  │ AI管家       │  │ 用户管理     │  │ 安防管理   │ │   │
│  │  │ (统一服务)   │  │ 权限管理     │  │ 事件记录   │ │   │
│  │  └──────────────┘  └──────────────┘  └────────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
│                              │                               │
│                        HTTP/WebSocket                        │
│                              │                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                   前端服务器                           │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │   │
│  │  │ Vue Web      │  │ Vue.js PWA   │  │ ESP32终端  │ │   │
│  │  │ 管理后台     │  │ 手机端APP     │  │ 楼控/客房端 │ │   │
│  │  └──────────────┘  └──────────────┘  └────────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2. 环境准备

#### 2.1 开发环境准备（本地）

**方案A: 完全本地开发**

```bash
# 1. 安装 Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 2. 安装 Docker（用于数据库等服务）
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

**方案B: 云服务器开发**

```bash
# 在云服务器上直接安装所有依赖
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# 安装 PM2
npm install -g pm2
```

#### 2.2 服务器环境要求（云服务器）

**推荐配置（开发/演示）**

| 组件 | 最低配置 | 推荐配置 | 说明 |
|------|---------|---------|------|
| CPU | 1核 | 2核 | 处理AI推理和并发请求 |
| 内存 | 2GB | 4GB | 缓存和数据库运行 |
| 存储 | 50GB SSD | 100GB SSD | 系统+数据库+日志 |
| 带宽 | 10Mbps | 50Mbps | 音频传输和API调用 |
| 系统 | Ubuntu 20.04+ | Ubuntu 22.04 LTS | 推荐使用Ubuntu |

**云服务提供商选择**

| 平台 | 优势 | 推荐实例 | 预估费用 |
|------|------|---------|---------|
| **阿里云** | 稳定可靠，API丰富，学生优惠 | 1核2GB ECS | ¥9.9/月（学生版） |
| **华为云** | 配套鸿蒙生态 | 1核2GB ECS | ¥9.9-140/月 |
| **学生优惠** | 9.9元/月 | 1核2GB | 仅限学生认证 |

**推荐方案**：使用阿里云学生服务器（9.9元/月），部署自建MQTT服务（EMQX/Mosquitto）+ Node.js后端 + MySQL + Redis，完全满足系统需求且成本最低。

### 3. 快速部署指南

#### 方案1: 本地开发 + Docker部署

**适合场景**: 开发阶段，环境隔离，易于调试

```bash
# 1. 克隆项目
git clone https://github.com/choisaaaaa/IoT_Smart_Home_Control_System.git
cd IoT_Smart_Hotel_System

# 2. 配置环境变量
cd backend
cp .env.example .env
# 编辑 .env 文件，填写 API Keys

# 3. 启动所有服务
cd ..
docker-compose up -d

# 4. 查看服务状态
docker-compose ps

# 5. 访问后端 API
curl http://localhost:3000/api/v1/health
```

#### 方案2: 云服务器部署

**适合场景**: 比赛演示，远程访问，环境一致

```bash
# 1. 在云服务器上执行
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# 2. 克隆项目
git clone https://github.com/choisaaaaa/IoT_Smart_Home_Control_System.git
cd IoT_Smart_Hotel_System

# 3. 配置环境变量
cd backend
cp .env.example .env
# 编辑 .env 文件，填写 API Keys

# 4. 启动服务
cd ..
docker-compose up -d

# 5. 配置 Nginx 反向代理（可选）
# 参考部署指南第 2.6 节

# 6. 获取服务器公网 IP
curl ifconfig.me
```

### 4. 后端服务部署

#### 4.1 项目结构

```
backend/
├── src/
│   ├── index.js                  # 入口文件
│   ├── config/
│   │   ├── index.js              # 配置管理
│   │   └── .env.example          # 环境变量模板
│   ├── mqtt/
│   │   ├── client.js             # MQTT Broker
│   │   └── handler.js            # 消息路由
│   ├── agent/
│   │   ├── orchestrator.js       # AI管家编排器
│   │   ├── context-builder.js    # 上下文构建
│   │   ├── tools/                # Function Calling 工具
│   │   │   ├── device-control.js # 设备控制
│   │   │   ├── sensor-query.js   # 传感器查询
│   │   │   ├── delivery-order.js # 送物订单管理
│   │   │   ├── repair-ticket.js  # 报修工单管理
│   │   │   └── hotel-info.js     # 酒店信息查询
│   │   └── prompts/
│   │       └── system-prompt.js  # System Prompt
│   ├── routes/
│   │   ├── chat.js               # AI管家路由
│   │   ├── devices.js            # 设备路由
│   │   ├── sensors.js            # 传感器路由
│   │   ├── hotels.js             # 酒店路由
│   │   ├── guests.js             # 住客路由
│   │   ├── cards.js              # 房卡路由
│   │   ├── orders.js             # 订单路由
│   │   ├── tickets.js            # 工单路由
│   │   └── reviews.js            # 评价路由
│   └── services/
│       ├── weather-service.js    # 天气服务
│       └── device-service.js     # 设备服务
├── package.json
├── .env                          # 环境变量（实际配置）
├── .env.example                  # 环境变量模板
├── Dockerfile                    # Docker部署文件
├── docker-compose.yml            # Docker Compose编排
├── pm2.config.js                 # PM2进程管理配置
└── README.md
```

#### 4.2 环境变量配置

```bash
cd backend
cp .env.example .env
```

**`.env` 配置内容**：

```env
# 服务器配置
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=smart_hotel
DB_POOL=10

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# MQTT Broker 配置
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USER=
MQTT_PASSWORD=

# 智谱AI大模型（AI管家）
ZHIPUAI_API_KEY=your_zhipuai_api_key
ZHIPUAI_MODEL=glm-4-flash

# 和风天气 API
HEFENG_KEY=your_hefeng_key

# JWT 认证
JWT_SECRET=your_jwt_secret_key_2024
JWT_EXPIRES_IN=7d

# 日志配置
LOG_LEVEL=info
LOG_DIR=./logs

# 安全配置
CORS_ORIGINS=http://localhost:8080,http://localhost:3000
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000
```

#### 4.3 依赖安装

```bash
cd backend
npm install
npm install -D nodemon pm2 eslint
```

#### 4.4 本地运行

```bash
# 开发模式
npm run dev

# 生产模式
npm start

# 使用 PM2
pm2 start pm2.config.js --name smart-hotel
```

#### 4.5 生产环境部署

**Docker 部署**：

```bash
# 构建并启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f backend

# 停止服务
docker-compose down
```

**Nginx 反向代理**：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}
```

### 5. 小组协作开发指南

#### 5.1 Git 工作流

**分支策略**：

```
main        # 生产环境分支（受保护）
develop     # 开发分支
feature/*   # 功能分支
bugfix/*    # 修复分支
```

**开发流程**：

```bash
# 1. 创建功能分支
git checkout -b feature/add-new-api

# 2. 开发并提交
git add .
git commit -m "feat: add new API endpoint"

# 3. 推送到远程
git push origin feature/add-new-api

# 4. 提交 Pull Request
# 5. 代码审查后合并到 develop
```

#### 5.2 环境配置协作

**规范**：
- `.env.example` 提交到 Git（模板）
- `.env` 不提交到 Git（本地配置）
- 所有成员使用相同的 `.env.example`

**初始化流程**：

```bash
# 1. 克隆项目
git clone https://github.com/choisaaaaa/IoT_Smart_Home_Control_System.git
cd IoT_Smart_Hotel_System/backend

# 2. 复制环境变量模板
cp .env.example .env

# 3. 编辑 .env，填写团队的 API Keys
# 4. 启动服务
cd ..
docker-compose up -d
```

#### 5.3 云服务器部署流程

**自动化部署**：

```bash
# 1. 提交代码到 GitHub
git push origin main

# 2. 云服务器拉取最新代码
cd /opt/IoT_Smart_Hotel_System
git pull

# 3. 重启服务
docker-compose down
docker-compose up -d
```

### 6. 监控与运维

#### 6.1 日志管理

```bash
# 后端日志
pm2 logs smart-hotel

# Nginx 日志
tail -f /var/log/nginx/smart-hotel-access.log

# MySQL 慢查询日志
tail -f /var/log/mysql/slow.log
```

#### 6.2 健康检查

```bash
# 后端健康检查
curl http://localhost:3000/api/v1/health

# MySQL 健康检查
docker exec -it smart-hotel-db mysqladmin -u root -p"password" ping

# Redis 健康检查
docker exec -it smart-hotel-redis redis-cli ping

# MQTT 健康检查
docker exec -it smart-hotel-mqtt mosquitto_sub -h localhost -t test -C 1
```

#### 6.3 备份策略

```bash
# 创建备份脚本
cat > /backup/db_backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/backup/mysql"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

docker exec smart-hotel-db mysqldump -u root -p"password" smart_hotel > $BACKUP_DIR/smart_hotel_$DATE.sql

# 保留最近7天的备份
find $BACKUP_DIR -name "smart_hotel_*.sql" -mtime +7 -delete
EOF

chmod +x /backup/db_backup.sh

# 添加定时任务（每天凌晨2点备份）
crontab -e
0 2 * * * /backup/db_backup.sh
```

---

## 📁 前端项目结构与多角色视图设计

### 1. 项目结构

```
frontend/
├── public/
│   ├── favicon.ico
│   └── index.html
├── src/
│   ├── assets/              # 静态资源
│   │   ├── styles/          # 全局样式
│   │   │   ├── variables.css
│   │   │   └── themes/
│   │   └── images/          # 图片资源
│   ├── components/          # 公共组件
│   │   ├── layout/          # 布局组件
│   │   │   ├── Header.vue
│   │   │   ├── Sidebar.vue
│   │   │   └── Footer.vue
│   │   ├── common/          # 通用组件
│   │   │   ├── Button.vue
│   │   │   ├── Card.vue
│   │   │   └── Table.vue
│   │   └── ui/              # UI组件
│   │       ├── Form.vue
│   │       └── Modal.vue
│   ├── views/               # 视图页面（按角色划分）
│   │   ├── admin/           # 管理员视图（前台管理后台）
│   │   │   ├── Dashboard.vue          # 仪表盘
│   │   │   ├── CheckIn.vue            # 入住管理
│   │   │   ├── CheckOut.vue           # 退房管理
│   │   │   ├── RoomCard.vue           # 房卡管理
│   │   │   ├── RoomStatus.vue         # 客房状态监控
│   │   │   ├── WorkOrder.vue          # 工单处理
│   │   │   ├── DataReport.vue         # 数据分析报表
│   │   │   └── Settings.vue           # 系统设置
│   │   ├── guest/           # 住客视图（手机端H5/小程序）
│   │   │   ├── Home.vue               # 首页
│   │   │   ├── RoomService.vue        # 客房服务（送物/报修）
│   │   │   ├── ContactFrontDesk.vue   # 联系前台
│   │   │   ├── DeviceControl.vue      # 设备控制
│   │   │   ├── HotelInfo.vue          # 酒店信息
│   │   │   └── Review.vue             # 服务评价
│   │   └── staff/           # 员工视图（服务员/维修工移动端）
│   │       ├── WorkOrderList.vue      # 工单列表
│   │       ├── WorkOrderDetail.vue    # 工单详情
│   │       ├── RoomStatusView.vue     # 客房状态查看
│   │       └── ServiceRecord.vue      # 服务记录
│   ├── router/              # 路由配置
│   │   ├── index.js
│   │   └── routes.js        # 路由定义（按角色划分）
│   ├── store/               # 状态管理
│   │   ├── index.js
│   │   ├── modules/         # 模块化状态
│   │   │   ├── user.js
│   │   │   ├── room.js
│   │   │   ├── order.js
│   │   │   └── workOrder.js
│   │   └── actions.js       # 全局Action
│   ├── services/            # API服务
│   │   ├── api.js           # 基础API配置
│   │   ├── auth.js          # 认证API
│   │   ├── room.js          # 客房API
│   │   ├── order.js         # 订单API
│   │   └── workOrder.js     # 工单API
│   ├── utils/               # 工具函数
│   │   ├── request.js       # HTTP请求封装
│   │   ├── websocket.js     # WebSocket封装
│   │   ├── auth.js          # 认证工具
│   │   └── format.js        # 格式化工具
│   ├── directives/          # 自定义指令
│   │   └── permission.js    # 权限指令
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
├── tests/                   # 测试文件
│   ├── unit/                # 单元测试
│   └── e2e/                 # 端到端测试
├── .env.example             # 环境变量模板
├── vue.config.js            # Vue配置
├── babel.config.js          # Babel配置
├── jsconfig.json            # JS配置
├── package.json             # 项目依赖
└── README.md                # 项目说明
```

### 2. 多角色视图架构

#### 2.1 角色定义

| 角色 | 访问端 | 主要功能 | 权限等级 |
|------|--------|----------|----------|
| **管理员**（前台工作人员） | 前台电脑（Web后台） | 入住/退房管理、房卡管理、客房状态监控、工单处理、数据分析报表 | 最高权限 |
| **住客**（酒店客人） | 手机端（H5/小程序） | 客房服务请求、联系前台、设备控制、酒店信息、服务评价 | 仅限本人房间 |
| **员工**（服务员/维修工） | 移动端（H5） | 工单处理、客房状态查看、服务记录 | 有限权限 |

#### 2.2 路由权限控制

```javascript
// router/routes.js
const routes = [
  {
    path: '/admin',
    component: Layout,
    meta: { role: 'admin' },
    children: [
      { path: 'dashboard', component: AdminDashboard },
      { path: 'check-in', component: CheckIn },
      { path: 'check-out', component: CheckOut },
      { path: 'room-card', component: RoomCard },
      { path: 'room-status', component: RoomStatus },
      { path: 'work-order', component: WorkOrder },
      { path: 'data-report', component: DataReport },
      { path: 'settings', component: Settings }
    ]
  },
  {
    path: '/guest',
    component: GuestLayout,
    meta: { role: 'guest' },
    children: [
      { path: '', component: GuestHome },
      { path: 'room-service', component: RoomService },
      { path: 'contact', component: ContactFrontDesk },
      { path: 'device-control', component: DeviceControl },
      { path: 'hotel-info', component: HotelInfo },
      { path: 'review', component: Review }
    ]
  },
  {
    path: '/staff',
    component: StaffLayout,
    meta: { role: 'staff' },
    children: [
      { path: 'work-order', component: WorkOrderList },
      { path: 'work-order/:id', component: WorkOrderDetail },
      { path: 'room-status', component: RoomStatusView },
      { path: 'service-record', component: ServiceRecord }
    ]
  },
  {
    path: '/login',
    component: Login,
    meta: { public: true }
  }
]
```

#### 2.3 权限指令

```javascript
// directives/permission.js
export default {
  mounted(el, binding) {
    const { value } = binding
    const userRole = localStorage.getItem('role')
    
    if (value && value.includes(userRole)) {
      el.style.display = 'block'
    } else {
      el.style.display = 'none'
    }
  }
}
```

使用示例：

```vue
<!-- 只有管理员可见 -->
<button v-permission="['admin']">新增房卡</button>

<!-- 管理员和员工可见 -->
<button v-permission="['admin', 'staff']">处理工单</button>
```

#### 2.4 视图组件划分

**管理员视图（admin/）**：
- **Dashboard.vue**：仪表盘，显示酒店运营数据（入住率、订单量、工单量、满意度）
- **CheckIn.vue**：入住管理，客户信息核验、身份证读取、房卡发放
- **CheckOut.vue**：退房管理，结账、房卡回收、客房状态更新
- **RoomCard.vue**：房卡管理，房卡发放、挂失、补办
- **RoomStatus.vue**：客房状态监控，实时显示各房间状态（空闲/入住/维修/SOS）
- **WorkOrder.vue**：工单处理，接收客房服务请求，分配任务给服务员/维修工
- **DataReport.vue**：数据分析报表，生成入住率、订单量、工单量、满意度等统计报表
- **Settings.vue**：系统设置，用户管理、设备管理、系统参数配置

**住客视图（guest/）**：
- **Home.vue**：首页，显示房间信息、酒店设施、周边推荐
- **RoomService.vue**：客房服务，送物服务（物品选择、订单提交）、报修申请（故障描述、照片上传）
- **ContactFrontDesk.vue**：联系前台，一键呼叫、文字聊天、语音消息
- **DeviceControl.vue**：设备控制，灯光、空调、窗帘、电视控制
- **HotelInfo.vue**：酒店信息，设施介绍、周边推荐
- **Review.vue**：服务评价，满意度评分、评价内容提交

**员工视图（staff/）**：
- **WorkOrderList.vue**：工单列表，显示待处理工单（新工单、处理中、已完成）
- **WorkOrderDetail.vue**：工单详情，查看工单信息、更新处理进度
- **RoomStatusView.vue**：客房状态查看，查看各房间状态、入住信息
- **ServiceRecord.vue**：服务记录，查看历史服务记录、评价反馈

### 3. 状态管理设计

```javascript
// store/modules/user.js
export default {
  state: {
    user: null,
    token: localStorage.getItem('token') || '',
    role: localStorage.getItem('role') || '',
    permissions: []
  },
  mutations: {
    SET_USER(state, user) {
      state.user = user
    },
    SET_TOKEN(state, token) {
      state.token = token
      localStorage.setItem('token', token)
    },
    SET_ROLE(state, role) {
      state.role = role
      localStorage.setItem('role', role)
    },
    SET_PERMISSIONS(state, permissions) {
      state.permissions = permissions
    }
  },
  actions: {
    async login({ commit }, payload) {
      const response = await api.login(payload)
      commit('SET_TOKEN', response.token)
      commit('SET_USER', response.user)
      commit('SET_ROLE', response.user.role)
      commit('SET_PERMISSIONS', response.user.permissions)
    },
    async logout({ commit }) {
      await api.logout()
      commit('SET_USER', null)
      commit('SET_TOKEN', '')
      commit('SET_ROLE', '')
      commit('SET_PERMISSIONS', [])
    }
  }
}
```

### 4. API服务设计

```javascript
// services/api.js
import axios from 'axios'
import { getToken } from '@/utils/auth'

const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API,
  timeout: 5000
})

service.interceptors.request.use(
  config => {
    if (getToken()) {
      config.headers['Authorization'] = 'Bearer ' + getToken()
    }
    return config
  },
  error => {
    console.log(error)
    return Promise.reject(error)
  }
)

service.interceptors.response.use(
  response => {
    return response.data
  },
  error => {
    console.log(error)
    return Promise.reject(error)
  }
)

export default service
```

```javascript
// services/auth.js
import api from './api'

export function login(data) {
  return api({
    url: '/auth/login',
    method: 'post',
    data
  })
}

export function logout() {
  return api({
    url: '/auth/logout',
    method: 'post'
  })
}

export function getInfo() {
  return api({
    url: '/auth/info',
    method: 'get'
  })
}
```

### 5. WebSocket实时通信

```javascript
// utils/websocket.js
import io from 'socket.io-client'

class WebSocketClient {
  constructor() {
    this.socket = null
    this.connected = false
  }

  connect() {
    this.socket = io(process.env.VUE_APP_WS_URL, {
      transports: ['websocket'],
      reconnection: true,
      reconnectionDelay: 1000,
      reconnectionAttempts: 5
    })

    this.socket.on('connect', () => {
      this.connected = true
      console.log('WebSocket connected')
    })

    this.socket.on('disconnect', () => {
      this.connected = false
      console.log('WebSocket disconnected')
    })

    this.socket.on('error', (error) => {
      console.error('WebSocket error:', error)
    })

    this.socket.on('message', (data) => {
      this.handleMessage(data)
    })
  }

  disconnect() {
    if (this.socket) {
      this.socket.disconnect()
      this.socket = null
      this.connected = false
    }
  }

  sendMessage(type, data) {
    if (this.connected && this.socket) {
      this.socket.emit('message', { type, data })
    }
  }

  handleMessage(data) {
    // 根据消息类型分发到不同的处理函数
    switch (data.type) {
      case 'room_status_update':
        this.handleRoomStatusUpdate(data.payload)
        break
      case 'work_order_update':
        this.handleWorkOrderUpdate(data.payload)
        break
      case 'notification':
        this.handleNotification(data.payload)
        break
      default:
        console.log('Unknown message type:', data.type)
    }
  }

  handleRoomStatusUpdate(payload) {
    // 更新客房状态
    console.log('Room status updated:', payload)
  }

  handleWorkOrderUpdate(payload) {
    // 更新工单状态
    console.log('Work order updated:', payload)
  }

  handleNotification(payload) {
    // 显示通知
    console.log('Notification:', payload)
  }
}

export default new WebSocketClient()
```

### 6. 部署配置

```javascript
// vue.config.js
module.exports = {
  publicPath: process.env.NODE_ENV === 'production' ? '/smart-hotel/' : '/',
  outputDir: 'dist',
  assetsDir: 'static',
  lintOnSave: process.env.NODE_ENV === 'development',
  runtimeCompiler: true,
  productionSourceMap: false,
  devServer: {
    port: 8080,
    proxy: {
      '/api': {
        target: process.env.VUE_APP_BASE_API,
        changeOrigin: true,
        pathRewrite: {
          '^/api': ''
        }
      },
      '/ws': {
        target: process.env.VUE_APP_WS_URL,
        ws: true
      }
    }
  }
}
```

---

## 📞 技术支持

### 1. 资源链接

- **ESP32 文档**: https://docs.espressif.com/
- **Node.js 文档**: https://nodejs.org/en/docs/
- **Vue.js 文档**: https://vuejs.org/
- **智谱AI**: https://bigmodel.cn/
- **MQTT 协议**: https://mqtt.org/

### 2. 常见问题

**Q: 智谱 GLM-4 免费吗？**  
A: 是的，GLM-4-Flash 完全免费，无限制。

**Q: ESP32 可以离线运行吗？**  
A: 可以，但需要在 ESP32 上运行轻量级模型（如 ESP-SR），性能会受限。

**Q: 如何降低延迟？**  
A: 使用阿里云 ECS，延迟可控制在 1-2秒内。

**Q: 小组成员如何协作开发？**  
A: 使用 GitHub 远程仓库，每人本地开发，提交代码后云服务器自动部署。

**Q: 手机端APP如何连接后端？**  
A: 手机端通过4G/WiFi连接后端服务器，使用WebSocket实现实时通信。

**Q: 前台电脑端如何连接硬件？**  
A: 前台电脑端通过USB串口连接前台管理端硬件，使用自定义JSON协议通信。

**Q: 移动端语音识别在哪里进行？**  
A: 移动端语音识别在前端进行，使用浏览器Web Speech API或第三方ASR服务，识别后的文字发送给后端AI管家处理。

---

## 📄 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2024-01-XX | 初始版本，包含基本功能需求 |
| v2.0.0 | 2024-01-XX | 整合三端语音智能体架构，优化系统设计，完善部署指南 |
| v3.0.0 | 2026-04-XX | 重构为智慧酒店三层架构，新增酒店管家AI系统、手机端APP、送物服务、报修工单系统 |
| v4.0.0 | 2026-04-XX | 优化AI管家架构，前端语音识别，统一后端处理文字指令 |
| v5.0.0 | 2026-04-XX | 简化移动端功能，保留AI管家系统，前端语音识别（APP端ASR），统一后端处理文字指令 |

---

**文档结束**
