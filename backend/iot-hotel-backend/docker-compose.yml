version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: iot-hotel-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-IotHotel2026!Root}
      MYSQL_DATABASE: ${DB_NAME:-iot_hotel_system}
      MYSQL_USER: ${DB_USER:-iot_user}
      MYSQL_PASSWORD: ${DB_PASSWORD:-IotHotel2026}
      MYSQL_CHARSET: utf8mb4
      MYSQL_COLLATION: utf8mb4_unicode_ci
      TZ: Asia/Shanghai
    ports:
      - "${DB_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --default-authentication-plugin=mysql_native_password
      --max_connections=200
      --innodb-buffer-pool-size=256M
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - iot-hotel-net

  mqtt:
    image: eclipse-mosquitto:2
    container_name: iot-hotel-mqtt
    restart: unless-stopped
    ports:
      - "${MQTT_PORT:-1883}:1883"
      - "${MQTT_WS_PORT:-9001}:9001"
    volumes:
      - ./config/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./config/mosquitto.passwd:/mosquitto/config/mosquitto.passwd:ro
      - ./config/acl:/mosquitto/config/acl:ro
      - mqtt_data:/mosquitto/data
      - mqtt_log:/mosquitto/log
    networks:
      - iot-hotel-net

networks:
  iot-hotel-net:
    driver: bridge

volumes:
  mysql_data:
    driver: local
  mqtt_data:
    driver: local
  mqtt_log:
    driver: local