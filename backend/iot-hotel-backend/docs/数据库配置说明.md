# 智慧酒店物联网控制系统 - 数据库配置说明

## 📋 数据库配置

本项目使用 MySQL 8.0 作为主数据库，存储所有酒店运营数据。

## 🔧 配置文件

### 1. 环境变量配置

文件位置: `backend/iot-hotel-backend/.env`

```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=iot_hotel_system
```

### 2. 数据库连接配置

文件位置: `backend/iot-hotel-backend/src/config/database.ts`

```typescript
import mysql, { Pool, PoolOptions } from 'mysql2/promise';
import config from '../config';

const poolOptions: PoolOptions = {
  host: config.database.host,
  port: config.database.port,
  user: config.database.user,
  password: config.database.password,
  database: config.database.database,
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
  charset: 'utf8mb4_unicode_ci'
};

const pool: Pool = mysql.createPool(poolOptions);

export default pool;
```

## 🗄️ 数据库结构

### 对齐说明（2026-04-09）

已完成数据库结构优化与对齐，重点解决了多酒店隔离、房型管理、楼层管理及报修分配逻辑。  
本次更新执行的脚本包含：

- `database/migrations/add_room_types_v2.sql` (房型管理支持)
- `database/migrations/add_floors_table.sql` (楼层平面图支持)
- `database/migrations/fix_rooms_unique_constraint.sql` (房间号按酒店隔离)
- `database/migrations/update_maintenance_tickets.sql` (维修人员分配支持)

关键变更点：

- **房间号隔离**：`rooms` 表的唯一索引由全局改为 `(hotel_id, room_number)` 联合唯一。
- **房型独立化**：新增 `room_types` 表，`rooms.room_type_id` 与之关联。
- **楼层管理**：新增 `floors` 表，支持楼层名称和平面图。
- **报修增强**：`maintenance_tickets` 新增 `repairer` 和 `assigned_at` 字段。

### 主要表结构

#### 1. hotels (酒店信息表)
(未变更，保持原样)

#### 2. room_types (房型信息表) - [2026-04-09 新增]

存储房型的通用配置信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| name | VARCHAR(50) | 房型名称 |
| code | VARCHAR(50) | 房型编码 (唯一) |
| base_price | DECIMAL(10,2)| 基础房价 |
| area | INT | 面积 |
| bed_type | VARCHAR(50) | 床型 |
| max_guests | INT | 最大人数 |
| facilities | JSON | 设施 |
| description | TEXT | 描述 |
| images | JSON | 房型图片 |

#### 3. floors (楼层信息表) - [2026-04-09 新增]

存储楼层平面图及名称信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| floor_number | INT | 楼层号 (唯一) |
| floor_name | VARCHAR(50) | 楼层显示名称 |
| floor_plan_image | VARCHAR(255) | 平面图路径 |
| description | TEXT | 楼层描述 |

#### 4. rooms (房间信息表)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| hotel_id | INT | 所属酒店ID |
| room_number | VARCHAR(20) | 房间号 (与 hotel_id 联合唯一) |
| room_type_id | INT | 关联 room_types.id |
| room_status | VARCHAR(20) | 状态 (available, occupied, reserved, cleaning, maintenance) |
| ... | ... | ... |

#### 5. maintenance_tickets (报修工单表)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| status | VARCHAR(20) | 状态 (pending, assigned, completed, cancelled) |
| repairer | VARCHAR(50) | 维修人员 [新增] |
| assigned_at | DATETIME | 分配时间 [新增] |
| ... | ... | ... |

## 👥 测试账号 (2026-04-09 初始化)

| 用户名 | 初始密码 | 角色 | 权限说明 |
| :--- | :--- | :--- | :--- |
| `manager01` | `123456` | `admin` | 超级管理员，拥有全部权限 |
| `staff02` | `123456` | `staff` | 酒店员工，负责办理入住、房态管理 |
| `guest01` | `123456` | `user` | 普通客房，负责预订和个人订单 |

#### 10. reviews (服务评价表)

存储服务评价信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| order_id | INT | 订单ID |
| order_type | VARCHAR(20) | 订单类型 |
| member_id | INT | 会员ID |
| score | INT | 评分 |
| content | TEXT | 评价内容 |
| photos | JSON | 评价图片 |
| created_at | DATETIME | 创建时间 |

## 🚀 数据库初始化

### 1. 使用Docker初始化

```bash
cd docker
docker-compose up -d
```

数据库会自动执行 `docker/mysql/init/schema.sql` 中的初始化脚本。

### 2. 手动初始化

```bash
# 连接到MySQL
mysql -u root -p

# 执行初始化脚本
source /path/to/schema.sql
```

### 3. 使用MySQL客户端

1. 创建数据库: `iot_hotel_system`
2. 创建用户: `iot_user`
3. 执行 `docker/mysql/init/schema.sql` 中的SQL语句

## 🔌 数据库连接

### 1. Node.js 连接

```typescript
import pool from './config/database';

// 查询
const [rows] = await pool.query('SELECT * FROM hotels');

// 插入
const [result] = await pool.query(
  'INSERT INTO hotels (hotel_name) VALUES (?)',
  ['智联酒店']
);

// 更新
const [result] = await pool.query(
  'UPDATE hotels SET hotel_name = ? WHERE id = ?',
  ['智联酒店', 1]
);

// 删除
const [result] = await pool.query(
  'DELETE FROM hotels WHERE id = ?',
  [1]
);
```

### 2. 连接池配置

```typescript
const poolOptions = {
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: 'your_password',
  database: 'iot_hotel_system',
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
  charset: 'utf8mb4_unicode_ci'
};
```

## 🛠️ 常用SQL语句

### 1. 查询酒店信息

```sql
SELECT * FROM hotels LIMIT 1;
```

### 2. 查询房间列表

```sql
SELECT * FROM rooms 
WHERE room_status = 'available' 
  AND room_type = 'standard' 
ORDER BY id DESC 
LIMIT 10 OFFSET 0;
```

### 3. 查询预订列表

```sql
SELECT b.*, r.room_number, r.room_type 
FROM bookings b 
LEFT JOIN rooms r ON b.room_id = r.id 
WHERE b.status = 'pending' 
ORDER BY b.id DESC 
LIMIT 10 OFFSET 0;
```

### 4. 查询支付记录

```sql
SELECT * FROM payments 
WHERE status = 'paid' 
ORDER BY paid_at DESC 
LIMIT 10 OFFSET 0;
```

### 5. 查询会员列表

```sql
SELECT * FROM members 
WHERE member_level = 'gold' 
ORDER BY points DESC 
LIMIT 10 OFFSET 0;
```

### 6. 查询优惠券

```sql
SELECT * FROM coupons 
WHERE valid_from <= CURDATE() 
  AND valid_to >= CURDATE() 
ORDER BY id DESC 
LIMIT 10 OFFSET 0;
```

### 7. 查询送物订单

```sql
SELECT d.*, r.room_number 
FROM delivery_orders d 
LEFT JOIN rooms r ON d.room_id = r.id 
WHERE d.status = 'pending' 
ORDER BY d.id DESC 
LIMIT 10 OFFSET 0;
```

### 8. 查询报修工单

```sql
SELECT m.*, r.room_number 
FROM maintenance_tickets m 
LEFT JOIN rooms r ON m.room_id = r.id 
WHERE m.status = 'pending' 
ORDER BY m.id DESC 
LIMIT 10 OFFSET 0;
```

### 9. 查询服务评价

```sql
SELECT r.*, m.name as member_name, m.phone as member_phone 
FROM reviews r 
LEFT JOIN members m ON r.member_id = m.id 
ORDER BY r.id DESC 
LIMIT 10 OFFSET 0;
```

## 🔐 安全配置

### 1. 数据库用户权限

```sql
CREATE USER 'iot_user'@'%' IDENTIFIED BY 'iot_password';
GRANT SELECT, INSERT, UPDATE, DELETE ON iot_hotel_system.* TO 'iot_user'@'%';
FLUSH PRIVILEGES;
```

### 2. SSL连接

```typescript
const poolOptions = {
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: 'your_password',
  database: 'iot_hotel_system',
  ssl: {
    ca: fs.readFileSync('/path/to/ca.pem'),
    cert: fs.readFileSync('/path/to/client-cert.pem'),
    key: fs.readFileSync('/path/to/client-key.pem')
  }
};
```

## 📊 数据库优化

### 1. 索引优化

```sql
-- 房间状态索引
CREATE INDEX idx_room_status ON rooms(room_status);

-- 预订状态索引
CREATE INDEX idx_booking_status ON bookings(status);

-- 支付状态索引
CREATE INDEX idx_payment_status ON payments(status);

-- 会员等级索引
CREATE INDEX idx_member_level ON members(member_level);

-- 优惠券有效期索引
CREATE INDEX idx_coupon_valid ON coupons(valid_from, valid_to);
```

### 2. 查询优化

```sql
-- 使用EXPLAIN分析查询
EXPLAIN SELECT * FROM bookings WHERE status = 'pending';

-- 使用覆盖索引
SELECT id, room_number, room_status FROM rooms WHERE room_status = 'available';
```

## 🔄 数据库备份

### 1. 使用mysqldump

```bash
mysqldump -u root -p iot_hotel_system > backup.sql
```

### 2. 使用Docker

```bash
docker-compose exec mysql mysqldump -u root -p iot_hotel_system > backup.sql
```

### 3. 恢复数据库

```bash
mysql -u root -p iot_hotel_system < backup.sql
```

---

**版本**: v1.0.0  
**最后更新**: 2026年4月4日
