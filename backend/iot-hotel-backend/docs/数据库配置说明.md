# 智慧酒店物联网控制系统 - 数据库配置说明

## 📋 数据库配置

本项目使用 MySQL 8.0 作为主数据库，存储所有酒店运营数据。

## 🔧 配置文件

### 1. 环境变量配置

文件位置: `backend/iot-hotel-backend/.env`

```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=iot_hotel_system
```

### 2. 数据库连接配置

文件位置: `backend/iot-hotel-backend/src/config/database.ts`

```typescript
import mysql, { Pool, PoolOptions } from 'mysql2/promise';
import config from '../config';

const poolOptions: PoolOptions = {
  host: config.database.host,
  port: config.database.port,
  user: config.database.user,
  password: config.database.password,
  database: config.database.database,
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
  charset: 'utf8mb4_unicode_ci'
};

const pool: Pool = mysql.createPool(poolOptions);

export default pool;
```

## 🗄️ 数据库结构

### 对齐说明（2026-04）

已完成数据库对齐与验证，核心是兼容 `hotel_id/hotel_code/room_id` 相关历史 SQL 与新接口逻辑。  
本次对齐执行脚本包含：

- `database/migrations/add_auth_tables.sql`
- `database/migrations/add_room_types_v2.sql`
- `database/migrations/update_devices_table.sql`
- `database/migrations/align_api_schema_v260.sql`

关键字段已确认存在：

- `hotels.hotel_code`, `hotels.location`
- `rooms.hotel_id`, `rooms.room_id`
- `bookings.hotel_id`
- `users.hotel_id`
- `devices.hotel_id`

### 主要表结构

#### 1. hotels (酒店信息表)

存储酒店基本信息和运营数据。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| hotel_name | VARCHAR(100) | 酒店名称 |
| hotel_address | VARCHAR(255) | 酒店地址 |
| hotel_phone | VARCHAR(20) | 酒店电话 |
| hotel_star | INT | 酒店星级 |
| total_rooms | INT | 总房间数 |
| occupied_rooms | INT | 已入住房间数 |
| occupancy_rate | DECIMAL(5,2) | 入住率 |
| logo | VARCHAR(255) | 酒店Logo |
| description | TEXT | 酒店描述 |
| hotel_id | INT | 兼容字段（旧查询使用） |
| hotel_code | VARCHAR(50) | 酒店编码 |
| city | VARCHAR(100) | 城市 |
| location | VARCHAR(255) | 位置（兼容字段） |
| star_rating | INT | 星级（兼容字段） |
| rating | DECIMAL(3,2) | 综合评分 |
| review_count | INT | 评价数量 |
| image_url | VARCHAR(255) | 主图 |
| promotion | VARCHAR(255) | 活动文案 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 2. rooms (房间信息表)

存储房间基本信息和状态。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| room_number | VARCHAR(20) | 房间号 |
| room_type | VARCHAR(20) | 房间类型 |
| room_name | VARCHAR(100) | 房间名称 |
| room_price | DECIMAL(10,2) | 房间价格 |
| room_status | VARCHAR(20) | 房间状态 |
| floor | INT | 楼层 |
| area | DECIMAL(6,2) | 面积 |
| bed_type | VARCHAR(20) | 床型 |
| max_guests | INT | 最多入住人数 |
| description | TEXT | 房间描述 |
| facilities | JSON | 设施列表 |
| images | JSON | 房间图片 |
| hotel_id | INT | 所属酒店ID |
| room_id | INT | 兼容字段（旧查询使用） |
| image_url | VARCHAR(255) | 主图 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 3. bookings (预订表)

存储客户预订信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| booking_number | VARCHAR(50) | 预订编号 |
| hotel_id | INT | 所属酒店ID |
| room_id | INT | 房间ID |
| guest_name | VARCHAR(100) | 客户姓名 |
| guest_phone | VARCHAR(20) | 客户电话 |
| guest_id_number | VARCHAR(50) | 身份证号 |
| check_in_date | DATE | 入住日期 |
| check_out_date | DATE | 退房日期 |
| guest_count | INT | 入住人数 |
| special_requests | VARCHAR(255) | 特殊要求 |
| payment_method | VARCHAR(20) | 支付方式 |
| total_price | DECIMAL(10,2) | 总价 |
| deposit | DECIMAL(10,2) | 定金 |
| status | VARCHAR(20) | 预订状态 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| check_in_time | DATETIME | 入住时间 |
| check_out_time | DATETIME | 退房时间 |
| cancelled_at | DATETIME | 取消时间 |

#### 4. payments (支付表)

存储支付信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| payment_no | VARCHAR(50) | 支付单号 |
| order_type | VARCHAR(20) | 订单类型 |
| order_id | INT | 订单ID |
| amount | DECIMAL(10,2) | 支付金额 |
| payment_method | VARCHAR(20) | 支付方式 |
| status | VARCHAR(20) | 支付状态 |
| transaction_no | VARCHAR(100) | 交易号 |
| paid_at | DATETIME | 支付时间 |
| description | VARCHAR(255) | 支付描述 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 5. members (会员表)

存储会员信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| phone | VARCHAR(20) | 手机号 |
| password | VARCHAR(255) | 密码 |
| name | VARCHAR(100) | 姓名 |
| id_number | VARCHAR(50) | 身份证号 |
| member_level | VARCHAR(20) | 会员等级 |
| points | INT | 积分 |
| balance | DECIMAL(10,2) | 余额 |
| total_spent | DECIMAL(12,2) | 总消费 |
| total_stays | INT | 总入住次数 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 6. coupons (优惠券表)

存储优惠券信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| coupon_name | VARCHAR(100) | 优惠券名称 |
| coupon_type | VARCHAR(20) | 优惠券类型 |
| discount_value | DECIMAL(10,2) | 优惠金额 |
| min_amount | DECIMAL(10,2) | 最低消费 |
| total_count | INT | 总数量 |
| received_count | INT | 已领取数量 |
| valid_from | DATE | 有效期开始 |
| valid_to | DATE | 有效期结束 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 7. member_coupons (会员优惠券关联表)

存储会员领取的优惠券。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| member_id | INT | 会员ID |
| coupon_id | INT | 优惠券ID |
| status | VARCHAR(20) | 状态 |
| used_at | DATETIME | 使用时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 8. delivery_orders (送物订单表)

存储送物订单信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| order_no | VARCHAR(50) | 订单号 |
| room_id | INT | 房间ID |
| item_category | VARCHAR(50) | 物品分类 |
| item_name | VARCHAR(100) | 物品名称 |
| quantity | INT | 数量 |
| note | VARCHAR(255) | 备注 |
| status | VARCHAR(20) | 订单状态 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| completed_at | DATETIME | 完成时间 |

#### 9. maintenance_tickets (报修工单表)

存储报修工单信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| ticket_no | VARCHAR(50) | 工单号 |
| room_id | INT | 房间ID |
| fault_type | VARCHAR(50) | 故障类型 |
| fault_description | TEXT | 故障描述 |
| photos | JSON | 故障照片 |
| priority | VARCHAR(20) | 优先级 |
| status | VARCHAR(20) | 工单状态 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| completed_at | DATETIME | 完成时间 |
| repair_description | TEXT | 维修描述 |
| repair_cost | DECIMAL(10,2) | 维修费用 |

#### 10. reviews (服务评价表)

存储服务评价信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| order_id | INT | 订单ID |
| order_type | VARCHAR(20) | 订单类型 |
| member_id | INT | 会员ID |
| score | INT | 评分 |
| content | TEXT | 评价内容 |
| photos | JSON | 评价图片 |
| created_at | DATETIME | 创建时间 |

## 🚀 数据库初始化

### 1. 使用Docker初始化

```bash
cd docker
docker-compose up -d
```

数据库会自动执行 `docker/mysql/init/schema.sql` 中的初始化脚本。

### 2. 手动初始化

```bash
# 连接到MySQL
mysql -u root -p

# 执行初始化脚本
source /path/to/schema.sql
```

### 3. 使用MySQL客户端

1. 创建数据库: `iot_hotel_system`
2. 创建用户: `iot_user`
3. 执行 `docker/mysql/init/schema.sql` 中的SQL语句

## 🔌 数据库连接

### 1. Node.js 连接

```typescript
import pool from './config/database';

// 查询
const [rows] = await pool.query('SELECT * FROM hotels');

// 插入
const [result] = await pool.query(
  'INSERT INTO hotels (hotel_name) VALUES (?)',
  ['智联酒店']
);

// 更新
const [result] = await pool.query(
  'UPDATE hotels SET hotel_name = ? WHERE id = ?',
  ['智联酒店', 1]
);

// 删除
const [result] = await pool.query(
  'DELETE FROM hotels WHERE id = ?',
  [1]
);
```

### 2. 连接池配置

```typescript
const poolOptions = {
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: 'your_password',
  database: 'iot_hotel_system',
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
  charset: 'utf8mb4_unicode_ci'
};
```

## 🛠️ 常用SQL语句

### 1. 查询酒店信息

```sql
SELECT * FROM hotels LIMIT 1;
```

### 2. 查询房间列表

```sql
SELECT * FROM rooms 
WHERE room_status = 'available' 
  AND room_type = 'standard' 
ORDER BY id DESC 
LIMIT 10 OFFSET 0;
```

### 3. 查询预订列表

```sql
SELECT b.*, r.room_number, r.room_type 
FROM bookings b 
LEFT JOIN rooms r ON b.room_id = r.id 
WHERE b.status = 'pending' 
ORDER BY b.id DESC 
LIMIT 10 OFFSET 0;
```

### 4. 查询支付记录

```sql
SELECT * FROM payments 
WHERE status = 'paid' 
ORDER BY paid_at DESC 
LIMIT 10 OFFSET 0;
```

### 5. 查询会员列表

```sql
SELECT * FROM members 
WHERE member_level = 'gold' 
ORDER BY points DESC 
LIMIT 10 OFFSET 0;
```

### 6. 查询优惠券

```sql
SELECT * FROM coupons 
WHERE valid_from <= CURDATE() 
  AND valid_to >= CURDATE() 
ORDER BY id DESC 
LIMIT 10 OFFSET 0;
```

### 7. 查询送物订单

```sql
SELECT d.*, r.room_number 
FROM delivery_orders d 
LEFT JOIN rooms r ON d.room_id = r.id 
WHERE d.status = 'pending' 
ORDER BY d.id DESC 
LIMIT 10 OFFSET 0;
```

### 8. 查询报修工单

```sql
SELECT m.*, r.room_number 
FROM maintenance_tickets m 
LEFT JOIN rooms r ON m.room_id = r.id 
WHERE m.status = 'pending' 
ORDER BY m.id DESC 
LIMIT 10 OFFSET 0;
```

### 9. 查询服务评价

```sql
SELECT r.*, m.name as member_name, m.phone as member_phone 
FROM reviews r 
LEFT JOIN members m ON r.member_id = m.id 
ORDER BY r.id DESC 
LIMIT 10 OFFSET 0;
```

## 🔐 安全配置

### 1. 数据库用户权限

```sql
CREATE USER 'iot_user'@'%' IDENTIFIED BY 'iot_password';
GRANT SELECT, INSERT, UPDATE, DELETE ON iot_hotel_system.* TO 'iot_user'@'%';
FLUSH PRIVILEGES;
```

### 2. SSL连接

```typescript
const poolOptions = {
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: 'your_password',
  database: 'iot_hotel_system',
  ssl: {
    ca: fs.readFileSync('/path/to/ca.pem'),
    cert: fs.readFileSync('/path/to/client-cert.pem'),
    key: fs.readFileSync('/path/to/client-key.pem')
  }
};
```

## 📊 数据库优化

### 1. 索引优化

```sql
-- 房间状态索引
CREATE INDEX idx_room_status ON rooms(room_status);

-- 预订状态索引
CREATE INDEX idx_booking_status ON bookings(status);

-- 支付状态索引
CREATE INDEX idx_payment_status ON payments(status);

-- 会员等级索引
CREATE INDEX idx_member_level ON members(member_level);

-- 优惠券有效期索引
CREATE INDEX idx_coupon_valid ON coupons(valid_from, valid_to);
```

### 2. 查询优化

```sql
-- 使用EXPLAIN分析查询
EXPLAIN SELECT * FROM bookings WHERE status = 'pending';

-- 使用覆盖索引
SELECT id, room_number, room_status FROM rooms WHERE room_status = 'available';
```

## 🔄 数据库备份

### 1. 使用mysqldump

```bash
mysqldump -u root -p iot_hotel_system > backup.sql
```

### 2. 使用Docker

```bash
docker-compose exec mysql mysqldump -u root -p iot_hotel_system > backup.sql
```

### 3. 恢复数据库

```bash
mysql -u root -p iot_hotel_system < backup.sql
```

---

**版本**: v1.0.0  
**最后更新**: 2026年4月4日
