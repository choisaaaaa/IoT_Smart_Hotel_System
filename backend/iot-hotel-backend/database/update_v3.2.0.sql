-- 智慧酒店物联网控制系统 - 数据库更新脚本 (v3.2.0)
-- 评价系统增加用户头像显示功能

USE iot_hotel_system;

-- -----------------------------------------------------------------------------
-- 1. 检查并确保 users 表有 avatar 字段
-- -----------------------------------------------------------------------------
-- 注意: users 表已存在 avatar 字段，无需修改
-- 字段定义: avatar VARCHAR(255) DEFAULT NULL

-- -----------------------------------------------------------------------------
-- 2. 评价查询视图优化说明
-- -----------------------------------------------------------------------------
-- 为了支持评价列表返回用户头像，需要在查询时关联 users 表
-- 
-- 关联查询示例:
-- SELECT r.*, m.name as member_name, m.phone as member_phone, u.avatar as user_avatar
-- FROM reviews r
-- LEFT JOIN members m ON r.member_id = m.id
-- LEFT JOIN users u ON r.user_id = u.id
-- WHERE r.is_deleted = 0
-- ORDER BY r.id DESC;
--
-- 字段说明:
-- - user_avatar: 用户头像URL，可能为NULL
-- - 头像优先级: member.avatar > user.avatar > 默认头像

-- -----------------------------------------------------------------------------
-- 3. 索引优化建议 (可选)
-- -----------------------------------------------------------------------------
-- 如果评价数据量大，建议添加以下索引优化查询性能:
-- CREATE INDEX idx_reviews_user_id ON reviews(user_id);
-- CREATE INDEX idx_reviews_member_id ON reviews(member_id);

-- -----------------------------------------------------------------------------
-- 4. 数据库文档更新完毕
-- -----------------------------------------------------------------------------
