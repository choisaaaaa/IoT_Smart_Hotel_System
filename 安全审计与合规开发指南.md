# 🔒 慧宿智联·IoT智慧酒店系统 — 安全审计与合规开发指南

## 📋 文档概述

本文档基于对慧宿智联IoT智慧酒店系统的全面安全审计，提供了详细的安全问题清单、整改任务表以及针对酒店行业的合规开发建议。旨在帮助项目团队在行业应用赛道中构建安全可靠的智慧酒店解决方案。

**审计时间**: 2026年4月22日  
**审计范围**: 后端API、数据库、IoT通信、移动端、WebSocket、部署配置  
**审计状态**: 已完成全面审计

---

## 🚨 安全风险概览

| 风险等级 | 数量 | 状态 |
|---------|------|------|
| 🔴 严重 (Critical) | 5个 | 必须立即修复 |
| 🟠 高危 (High) | 7个 | 尽快修复 |
| 🟡 中危 (Medium) | 8个 | 计划修复 |
| 🟢 低危 (Low) | 5个 | 建议改进 |

**总体安全评分**: 45/100 (需要重大改进)

---

## 🔴 严重问题 (Critical) - 必须立即修复

### C-01: MQTT Broker 允许匿名连接
**风险等级**: 🔴 严重  
**影响范围**: 所有IoT设备通信  
**合规违反**: 《网络安全法》第21条、GB/T 35273-2020

**问题描述**: 
- MQTT Broker配置为 `allow_anonymous true`
- 任何人都可以连接到MQTT Broker，订阅所有酒店设备消息
- 可发送伪造指令控制房间设备（灯光、空调、门锁）

**涉及文件**: 
- `backend/iot-hotel-backend/config/mosquitto.conf`

**修复建议**: 
```conf
# 禁用匿名连接
allow_anonymous false

# 启用密码文件认证
password_file /mosquitto/config/passwd

# 或启用TLS客户端证书认证
require_certificate true
```

### C-02: MQTT 通信未加密
**风险等级**: 🔴 严重  
**影响范围**: 所有设备通信数据  
**合规违反**: 《数据安全法》第27条

**问题描述**: 
- 使用1883端口明文传输所有MQTT通信
- 设备控制指令、传感器数据、房卡操作均可在网络中被嗅探

**涉及文件**: 
- `mobile/iot_hotel_app/lib/core/constants/mqtt_constants.dart`
- `backend/iot-hotel-backend/src/services/mqtt.service.ts`

**修复建议**: 
```dart
// 移动端使用TLS端口
static const int brokerPort = 8883; // TLS端口
```

```typescript
// 后端启用TLS
const useTLS = process.env.MQTT_USE_TLS === 'true' || isProduction;
const protocol = useTLS ? 'mqtts' : 'mqtt';
const port = useTLS ? 8883 : 1883;
```

### C-03: 服务器IP和数据库密码硬编码
**风险等级**: 🔴 严重  
**影响范围**: 整个系统安全  
**合规违反**: 《数据安全法》第27条

**问题描述**: 
- 生产服务器IP `8.134.166.69` 硬编码在源码中
- 数据库密码 `Iot2026.` 硬编码在迁移脚本中
- 源码泄露即导致生产环境完全失控

**涉及文件**: 
- `mobile/iot_hotel_app/lib/core/constants/api_constants.dart`
- `mobile/iot_hotel_app/lib/core/constants/mqtt_constants.dart`
- `backend/iot-hotel-backend/scripts/run-migration.ts`

**修复建议**: 
```dart
// 使用环境变量配置
static String get baseUrl {
  const String? envUrl = const String.fromEnvironment('API_BASE_URL');
  return envUrl ?? 'http://localhost:3000/api/v1/';
}
```

### C-04: 密码重置接口无身份验证
**风险等级**: 🔴 严重  
**影响范围**: 所有用户账户  
**合规违反**: 《个人信息保护法》第28条

**问题描述**: 
- 只需提供手机号和新密码即可重置密码
- 无需短信验证码、旧密码或任何身份验证
- 任何人只要知道手机号就能重置他人密码

**涉及文件**: 
- `backend/iot-hotel-backend/src/routes/v1/auth.ts`

**修复建议**: 
```typescript
// 增加短信验证码验证
router.post('/reset-password', async (req, res) => {
  const { phone, new_password, verification_code } = req.body;
  
  // 验证短信验证码
  const isValidCode = await verifySMSCode(phone, verification_code);
  if (!isValidCode) {
    return sendError(res, errorResponse('验证码错误或已过期', 400));
  }
  
  // 继续密码重置逻辑
});
```

### C-05: RFID门禁验证接口无有效认证
**风险等级**: 🔴 严重  
**影响范围**: 酒店门禁系统  
**合规违反**: 物理安全基本要求

**问题描述**: 
- `POST /rfid-access/verify` 接口仅在body中传递 `device_key`
- 等同于API Key明文传输，无签名验证
- 攻击者可伪造门禁验证请求，绕过门禁系统

**涉及文件**: 
- `backend/iot-hotel-backend/src/routes/v1/rfid-access.ts`

**修复建议**: 
```typescript
// 使用设备认证中间件
import { deviceAuthMiddleware } from '../../middleware/auth';

router.post('/verify', deviceAuthMiddleware, rfidAccessController.verify);
```

---

## 🟠 高危问题 (High) - 尽快修复

### H-01: 设备注册接口无认证
**问题**: `POST /devices/register` 无需认证即可注册新设备
**风险**: 恶意设备注入虚假数据、发送伪造指令
**修复**: 要求管理员权限或设备预注册Token

### H-02: WebSocket CORS 配置为 `origin: '*'`
**问题**: WebSocket允许任何来源连接
**风险**: 跨站WebSocket劫持，可监听实时数据
**修复**: 限制origin为已知的前端域名列表

### H-03: 登录接口防暴力破解不足
**问题**: 限流基于IP，可被代理绕过
**风险**: 暴力破解用户密码
**修复**: 实现账户锁定机制，增加验证码

### H-04: JWT Token 泄露在日志中
**问题**: 日志输出了JWT Token的前10个字符
**风险**: Token可能被重建
**修复**: 不在日志中输出任何Token片段

### H-05: 设备密钥明文存储和传输
**问题**: 设备密钥在数据库中明文存储
**风险**: 数据库泄露即导致所有设备密钥泄露
**修复**: 设备密钥应哈希存储

### H-06: 移动端Token明文存储
**问题**: Token存储在SharedPreferences中（Android上为XML明文文件）
**风险**: root设备可直接读取Token
**修复**: 使用 `flutter_secure_storage` 加密存储

### H-07: 移动端API通信使用HTTP
**问题**: 使用明文HTTP协议
**风险**: 所有API请求可被中间人截获
**修复**: 使用HTTPS，配置SSL证书

---

## 🟡 中危问题 (Medium) - 计划修复

### M-01: 缺少CSRF防护
**问题**: API未实现CSRF Token机制
**修复**: 对状态变更操作添加CSRF Token

### M-02: 错误处理泄露内部信息
**问题**: 将JWT验证错误详情返回给客户端
**修复**: 生产环境返回通用错误消息

### M-03: SQL注入防护依赖不完整
**问题**: SQL注入检测仅基于关键词黑名单
**修复**: 确保所有数据库操作使用参数化查询

### M-04: 密码策略过弱
**问题**: 密码最小长度仅6位，不要求特殊字符
**修复**: 最小长度提升至8位，要求包含特殊字符

### M-05: 缺少Refresh Token机制
**问题**: JWT过期后需要重新登录
**修复**: 实现Refresh Token轮换机制

### M-06: 上传文件缺少内容验证
**问题**: 仅验证MIME类型和扩展名
**修复**: 添加文件Magic Number验证

### M-07: 缺少审计日志
**问题**: 关键操作缺少专门的审计日志记录
**修复**: 实现完整的操作审计日志系统

### M-08: Docker MySQL Root密码为空
**问题**: 数据库无密码保护
**修复**: 设置强默认密码

---

## 🟢 低危问题 (Low) - 建议改进

### L-01: 缺少安全响应头 X-Content-Type-Options
**修复**: 验证Helmet配置完整性

### L-02: 缺少请求ID追踪
**修复**: 实现请求ID中间件，便于日志追踪

### L-03: 数据库连接池配置偏小
**问题**: `connectionLimit: 10` 生产环境可能不足
**修复**: 根据并发量调整，建议20-50

### L-04: MQTT通信日志记录payload
**问题**: 通信日志记录了完整的payload（最多5000字符）
**修复**: 仅记录元数据，不记录完整payload

### L-05: 缺少依赖安全扫描
**问题**: 未配置 `npm audit` 或 Snyk 等依赖安全扫描工具
**修复**: 在CI/CD中集成依赖安全扫描

---

## 📋 合规性检查结果

| 法规/标准 | 合规状态 | 不合规项 |
|-----------|---------|---------|
| 《网络安全法》 | ❌ 不合规 | 缺少审计日志留存6个月、明文传输敏感数据 |
| 《数据安全法》 | ❌ 不合规 | 数据库凭据硬编码、未进行数据分类分级 |
| 《个人信息保护法》 | ❌ 不合规 | 密码重置无身份验证、缺少隐私政策、用户同意机制 |
| GB/T 35273-2020 | ❌ 不合规 | 个人信息明文传输、未加密存储 |
| GB/T 22239-2019 (等保2.0) | ❌ 不合规 | 通信完整性、身份鉴别不满足要求 |
| GDPR (如涉及欧盟用户) | ❌ 不合规 | 缺少数据主体权利实现、DPO指定 |

---

## 🎯 优先级整改任务表

| 优先级 | 编号 | 任务 | 涉及文件 | 预估工作量 |
|-------|------|------|---------|-----------|
| P0 | C-01 | MQTT启用认证，禁用匿名连接 | mosquitto.conf, mqtt.service.ts | 中 |
| P0 | C-02 | MQTT启用TLS加密通信 | mosquitto.conf, mqtt_constants.dart, mqtt.service.ts | 中 |
| P0 | C-03 | 移除源码中硬编码的服务器IP和凭据 | api_constants.dart, mqtt_constants.dart, run-migration.ts | 小 |
| P0 | C-04 | 密码重置增加短信验证码验证 | auth.ts | 中 |
| P0 | C-05 | RFID门禁接口增加设备认证 | rfid-access.ts | 中 |
| P1 | H-01 | 设备注册接口增加认证 | devices.ts | 小 |
| P1 | H-02 | WebSocket限制CORS来源 | websocket.service.ts | 小 |
| P1 | H-03 | 登录增加账户锁定+验证码 | auth.ts, app.ts | 中 |
| P1 | H-04 | 移除日志中的Token输出 | auth.ts | 小 |
| P1 | H-05 | 设备密钥哈希存储 | 数据库迁移, auth.ts | 大 |
| P1 | H-06 | 移动端使用加密存储 | local_storage.dart → flutter_secure_storage | 中 |
| P1 | H-07 | 移动端API改用HTTPS | api_constants.dart, Nginx配置 | 中 |
| P2 | M-01 | 添加CSRF防护 | app.ts | 中 |
| P2 | M-02 | 生产环境隐藏错误详情 | auth.ts, error.ts | 小 |
| P2 | M-03 | 审查所有动态SQL拼接 | 多个controller | 中 |
| P2 | M-04 | 增强密码策略 | constants.ts, validators.ts | 小 |
| P2 | M-05 | 实现Refresh Token机制 | jwt.ts, auth.ts | 大 |
| P2 | M-06 | 上传文件增加内容验证 | upload.ts | 中 |
| P2 | M-07 | 实现操作审计日志系统 | 新增模块 | 大 |
| P2 | M-08 | Docker MySQL设置强密码 | docker-compose.yml | 小 |

---

## 🏨 酒店行业实际运用开发建议

### 1. 数据安全与隐私保护（酒店行业核心要求）

**1.1 住客个人信息保护**
- 对身份证号、手机号等PII字段进行数据库层加密（AES-256）
- 实现数据脱敏功能，前台查看住客信息时默认脱敏显示
- 实现"被遗忘权"功能，退房后一定期限内自动匿名化住客数据

**1.2 入住记录留存**
- 入住记录独立加密存储，设置合规的数据保留策略（如6个月）
- 到期自动归档，符合《旅馆业治安管理办法》要求

### 2. IoT设备安全（行业差异化竞争力）

**2.1 设备固件安全**
- 实现固件签名验证机制，防止恶意固件注入
- 支持OTA固件更新时的回滚机制
- 设备启动时验证固件完整性（Secure Boot概念）

**2.2 设备通信安全**
- 实现MQTT Topic级别的ACL（访问控制列表）
- 为每台设备颁发唯一的X.509证书，实现双向TLS认证
- 实现设备心跳异常检测，设备失联时自动触发安全告警

**2.3 边缘计算安全**
- 在楼控节点实现离线自治能力，网络中断时仍能执行基本安全策略
- 边缘节点与云端通信使用VPN或专用通道

### 3. 门禁系统安全（酒店安全核心）

**3.1 RFID门禁增强**
- 实现RFID卡UID加密（当前为明文比对），使用滚动码防止卡片克隆
- 门禁验证增加防重放机制（Nonce + 时间戳）
- 异常开门事件实时告警（如非入住时段开门、暴力破门检测）
- 实现门禁与视频监控联动，异常开门时自动抓拍

**3.2 数字钥匙安全**
- 移动端数字钥匙（手机开门）使用TEE/SE安全元件存储密钥
- 蓝牙开门使用加密通道，防止中继攻击

### 4. 支付安全（金融合规）

**4.1 支付流程安全**
- 余额支付增加支付密码/指纹验证
- 实现支付限额和异常交易检测
- 支付记录不可篡改（使用区块链或哈希链技术）
- 对接正规支付渠道（微信支付/支付宝），不自行处理支付敏感信息

**4.2 财务合规**
- 所有金额操作记录操作日志，支持财务审计
- 实现日结/月结自动对账功能

### 5. 系统架构安全

**5.1 网络隔离**
- IoT设备网络与办公网络物理/逻辑隔离
- 实现VLAN划分：设备网段、管理网段、住客Wi-Fi网段
- 部署防火墙，限制设备只能访问必要的端口

**5.2 高可用与灾备**
- 数据库主从复制，确保业务连续性
- 关键服务（门禁、消防）实现本地缓存和离线模式
- 定期自动备份，备份数据加密存储

**5.3 安全运维**
- 实现统一的运维审计堡垒机
- 所有管理操作通过VPN访问
- 部署SIEM（安全信息和事件管理）系统

### 6. 合规认证建议

对于酒店行业的实际部署，建议逐步获取以下认证：
- **等保2.0二级/三级**：酒店信息系统通常需要等保二级，涉及支付的可能需要三级
- **PCI DSS**：如果处理信用卡支付
- **ISO 27001**：信息安全管理体系认证
- **GDPR合规**：如果服务国际旅客

### 7. 比赛加分建议

在比赛答辩中，以下安全特性可以作为亮点展示：

1. **端到端加密的IoT通信链路**：设备→MQTT(TLS)→后端→前端全链路加密
2. **零信任架构设计**：每个请求都经过认证和授权，不信任内网
3. **数据分类分级保护**：身份证号加密存储、手机号脱敏显示、操作日志留存6个月
4. **离线安全自治**：网络中断时门禁仍可本地验证
5. **安全态势感知**：实时监控异常行为（异常登录、设备失联、暴力破门）
6. **合规性声明**：明确展示对《网络安全法》《数据安全法》《个人信息保护法》的合规措施

---

## 📊 总结

本次安全审计共发现 **5个严重漏洞、7个高危漏洞、8个中危漏洞、5个低危问题**，涵盖了IoT通信安全、认证授权、数据保护、移动端安全等多个维度。

**最紧急需要修复的3个问题**：
1. **MQTT匿名连接+明文传输** — 这是IoT酒店系统最核心的安全短板
2. **密码重置无身份验证** — 任何人可以重置他人密码接管账户
3. **服务器IP和数据库密码硬编码在源码中** — 源码泄露即等于生产环境完全失控

项目已有的安全基础设施（JWT认证、输入验证/净化器、Helmet安全头、速率限制、设备签名验证）说明团队有安全意识，但在**IoT通信安全**和**敏感数据保护**这两个酒店行业最关键的领域存在明显短板。

**建议修复优先级**：
- **立即修复**：C-01到C-05严重问题
- **一周内修复**：H-01到H-07高危问题
- **一个月内修复**：M-01到M-08中危问题
- **长期改进**：L-01到L-05低危问题

通过系统性的安全整改，本项目将在行业应用赛道中形成显著的安全竞争力优势。