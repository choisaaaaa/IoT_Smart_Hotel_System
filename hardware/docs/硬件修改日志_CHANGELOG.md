# 硬件修改日志 (CHANGELOG)

> 本文档记录了项目硬件组在架构设计、组件选型与引脚分配过程中的重要修改与决策结论，用于追溯历史版本变更与设计背景。

---

## [2026-04-09] 三端构建通过与联调准备完成

### 1. 构建与依赖修复
- **三端构建状态**：`room_terminal`、`front_desk_terminal`、`floor_controller` 均已完成独立构建通过。
- **组件依赖解析修复**：修复了 `json` 组件名在 ESP-IDF v6.0 下不可解析的问题，`cJSON` 依赖统一切换为 Component Manager 方式。
- **依赖声明落地**：在三端 `main` 组件中新增 `idf_component.yml`，统一声明 `espressif/cjson` 依赖；移除错误的 `REQUIRES json`。

### 2. 最小联调验证准备
- **MQTT 模拟脚本对齐**：三端模拟脚本已覆盖上线消息、心跳/遥测上报、指令回执三类基础联调路径。
- **Topic/字段核对结果**：已完成三端 MQTT Topic 与 JSON 字段静态一致性核对，满足当前通讯规范的最小联调要求。

### 3. 协议文档同步
- **通讯规范更新项**：补充 `incoming_call` 与 `hangup_call` 命令类型说明，用于客房端语音通话状态切换。

---

## [2026-04-08] 硬件概念冻结与实施整备阶段

### 1. 架构文档与实施概念完善
- **拓扑与交互时序**：在《硬件架构与设计基线_v1.0.md》中新增了基于 Mermaid 的“全局硬件网络拓扑图”与“AI管家语音交互时序图”，直观展示了全终端直连 MQTT 架构及语音大模型处理生命周期。
- **设备部署形态**：明确了三端设备的物理部署概念：前台端（桌面控制台/摆件）、楼控端（弱电箱/吊顶暗装）、客房端（标准86底盒+门外副面板）。
- **采购与造价核算**：更新了《硬件打样采购清单_v1.1.md》，补充了核心元器件的市场预估单价，并核算出单客房核心物料 BOM 成本压缩在 130 元以内的极高商业落地可行性。
- **文档规范化**：将原口语化的“对话日志”正式重构为本《硬件修改日志_CHANGELOG.md》，统一为第三方工程视角。

### 2. HAL 层控制逻辑重构 (按键与继电器)
- **执行器控制 (`hal_actuators`)**：基于 `global_config.h` 引脚映射，重构了 4 路继电器（灯光、插座、窗帘、门锁）的统筹控制逻辑。确立了低电平有效且初始化默认拉高断开的强电安全策略。
- **交互输入 (`hal_interactive`)**：统筹规划了三端共 6 个物理交互按键（客房 PTT/SOS/场景、前台消音/广播、楼控复位）。采用极简 GPIO 轮询与开启内部上拉的策略，避免过度设计，保障了代码在原型阶段的绝对稳定性与可维护性。

---

## [历史记录] 架构重构与组件库初始化

### 1. 架构与工程重构
- **分层架构确立**：确立并实施了 Driver → HAL → Service 的三层解耦架构。新外设驱动统一纳入 `common_components` 公共组件库。
- **通信规范对齐**：客房端、前台端、楼控端的 `main` 业务逻辑已与后端 MQTT/JSON 通信规范完成骨架对齐。
- **引脚分配策略**：统一在 `global_config.h` 中维护 ESP32-S3 冻结引脚表，彻底规避了 S3 的内部 Flash/PSRAM 冲突引脚及高风险启动引脚。所有终端的按键、继电器引脚已分配完毕并下沉至组件库配置中。

## 2. 组件库变更 (common_components)
- **新增驱动支持**：已完成 SG90、OLED(SSD1306)、RC522、电位器、LDR、NTC、DHT11、MQ-2、EC11、RD-03(OT2/UART)、红外发射/接收、RGB、有源蜂鸣器、继电器、W25Q64、INMP441、LMD2718、NS4168、PAM8403 的底层驱动封装。
- **组件移除与废弃**：
  - 矩阵键盘（4x4 Keypad）因原型调试复杂度过高，已从组件库及系统方案中移除。
  - 循迹模块（线巡/红外反射）因与智能酒店主线业务无关，明确不纳入系统方案。
- **音频链路方案**：确立 LMD2718（数字麦克风）+ NS4168（数字功放）为主线音频方案，PAM8403 降级为备选兼容方案。

## 3. 核心外设驱动修改明细
- **电位器 (Potentiometer)**：
  - 新增 `driver_potentiometer` 组件，实现模拟量输入（ADC）读取，提供百分比与原始值读取 API。
- **OLED 显示屏 (0.96" SSD1306)**：
  - `driver_oled` 组件由 Mock 模拟实现重构为基于 ESP-IDF `esp_lcd` 的真实 I2C 驱动。
  - 集成 5x7 ASCII 字模，引脚与分辨率宏统一定义于 `global_config.h`。
- **毫米波雷达 (RD-03 / S3KM1110)**：
  - 针对 RD-03 模块拆分为两个独立组件以适应不同接入需求：
    - `driver_rd03_simple`：基于 OT2 引脚的极简 GPIO 高低电平读取。
    - `driver_rd03_uart`：基于 UART 协议的被动收包与数据帧解析。

## 4. 后续演进规划
- 待所有硬件器件型号与 BOM 表最终登记完成后，进行最后一轮针对引脚与电源域的集中配置与校验。
- 预留 I2C/SPI 等共用总线的冲突处理与多设备挂载逻辑优化。
- 逐步推进量产级安全（TLS 加密）与网络容灾（断线重连、离线降级）机制的落地。
