# 硬件联调验收清单

> 合并说明（2026-04-10）：本清单已并入 `07_硬件系统完成度与验收总览_2026-04-10.md`。  
> 本文件保留用于历史验收记录。

> 版本：v1.0  
> 日期：2026-04-09  
> 适用范围：`room_terminal`、`front_desk_terminal`、`floor_controller`  
> 验收目标：确认硬件端已达到“可联调稳定态”

---

## 1. 构建与依赖验收

- [x] `room_terminal` 独立构建通过
- [x] `front_desk_terminal` 独立构建通过
- [x] `floor_controller` 独立构建通过
- [x] `cJSON` 依赖通过 Component Manager 解析成功
- [x] 无 `REQUIRES json` 遗留错误声明

---

## 2. Topic 与消息格式验收

- [x] 设备状态主题可用：`hotel/device/status/{device_type}/{device_id}`
- [x] 传感器主题可用：`hotel/device/data/{sensor_type}/{device_id}`
- [x] 指令下发主题可用：`hotel/device/command/{device_type}/{device_id}`
- [x] 指令回执主题可用：`hotel/device/command/result`
- [x] 安防事件主题可用：`hotel/security/event`
- [x] JSON 字段与《02_硬件层通讯规范.md》一致

---

## 3. 三端基础链路验收

### 3.1 客房端（Room）

- [x] 上线消息发布成功
- [x] 周期数据上报成功
- [x] `light_on` 指令可执行并回执
- [x] `incoming_call` 指令可执行并回执
- [x] `hangup_call` 指令可执行并回执

### 3.2 楼控端（Floor）

- [x] 上线消息发布成功
- [x] 周期数据上报成功
- [x] `light_on` 指令可执行并回执
- [x] `light_off` 指令可执行并回执
- [x] 楼道报警按键事件上报：`floor_alarm_pressed`
- [x] `floor_reset` 命令可执行并回执（由前台电脑/后台触发）

### 3.3 前台端（Front Desk）

- [x] 上线消息发布成功
- [x] 心跳消息周期发布成功
- [x] 状态上报链路可用
- [x] 前台按键业务事件上报（消音/广播）
- [x] 前台下行命令触发链路可用（按键触发 `incoming_call` / `hangup_call`）
- [x] 前台回执订阅与日志处理可用（`hotel/device/command/result`）

---

## 4. 配置与容灾验收

- [x] 三端设备标识支持 NVS 优先读取（失败回退默认值）
- [x] MQTT Broker URI 支持 NVS 优先读取（失败回退默认值）
- [x] 网络断开时进入降级门控（停止高频上报）
- [x] 网络恢复后可继续上线与上报
- [x] MQTT 指数退避重连（1/2/4/8...上限60s，重连后自动恢复订阅）

---

## 5. 文档一致性验收

- [x] `01_硬件架构与设计基线_v1.0.md` 已同步当前阶段结论
- [x] `03_硬件制图与实施交付手册_原型验证分板版_v1.0.md` 可作为制图输入
- [x] `02_硬件层通讯规范.md` 已同步 `incoming_call` / `hangup_call`
- [x] `15_硬件修改日志_CHANGELOG.md` 已记录本阶段关键变更
- [x] `MQTT联调测试报告_2026-04-09.md` 已形成对外报告

---

## 6. 阶段结论

- 当前结论：**硬件端已达到“可联调稳定态”**。  
- 风险边界：尚未完成量产级能力（TLS、长时压测、外设在环 HIL 全覆盖）。  
- 下一验收门槛：完成 `service_mqtt` 退避重连 + 24h 压测报告后，进入“量产前稳定性评审态”。

