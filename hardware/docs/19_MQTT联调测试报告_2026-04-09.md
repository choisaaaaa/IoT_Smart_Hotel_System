# MQTT 联调测试报告（硬件组）

> 合并说明（2026-04-10）：本报告已并入 `07_硬件系统完成度与验收总览_2026-04-10.md`。  
> 本文件保留用于历史脚本联调追溯。

> 日期：2026-04-09  
> 范围：`room_terminal`、`floor_controller`、`front_desk_terminal`  
> 方式：Node.js 脚本模拟设备端 MQTT 行为（无外设齐套场景）

---

## 1. 测试环境

- Broker：`mqtt://8.134.166.69:1883`
- 账号：`iot_user`
- 客户端脚本：
  - `hardware/test/mqtt_simulator/test_room_301.js`
  - `hardware/test/mqtt_simulator/test_floor_03.js`
  - `hardware/test/mqtt_simulator/test_front_desk_01.js`

---

## 2. 覆盖项与结果

### 2.1 三端基础链路

- [x] MQTT 连接成功
- [x] 设备上线消息发送成功
- [x] 周期遥测/心跳消息发送成功

### 2.2 下行命令与回执链路

- Room 端验证通过：
  - `light_on`（`command_id=2001`）
  - `air_on` / `air_off`（业务流回归）
  - `curtain_open` / `curtain_close`（业务流回归）
  - `door_unlock` / `door_lock`（业务流回归）
  - `incoming_call`（`command_id=2002`）
  - `hangup_call`（`command_id=2003`）
- Floor 端验证通过：
  - `light_on`（`command_id=3001`）
  - `light_off`（`command_id=3002`）
  - `broadcast_start` / `broadcast_stop`（占位业务流回归）
  - `floor_reset`（占位业务流回归）
- Front 端验证通过：
  - 回执订阅链路 `hotel/device/command/result`
  - 按键触发事件上报（脚本模拟）
  - 下发 `incoming_call` / `hangup_call` 至 `room_301`
- [x] 回执主题 `hotel/device/command/result` 返回成功

### 2.3 事件上报链路

- [x] Room 安防事件 `hotel/security/event`（`door_open`）上报成功

---

## 3. 当前结论

- 硬件侧 MQTT 最小联调链路已打通，可进入后端联合联调阶段。
- 当前属于“无外设齐套”预稳定阶段；外设动作真实性需在 HIL 阶段补测。
- 三端业务流第二轮回归（命令扩展、前台主动控制、楼控事件）已完成脚本级验证。

---

## 4. 后续建议

1. 后端侧校验 `hotel/device/command/result` 的消费与入库完整性。  
2. 在 `service_mqtt` 增加退避重连策略并开展 24h 长稳压测。  
3. 外设到齐后执行 HIL 用例，完成量产前硬件实测闭环。  

