# 业务流联调验收报告

> 合并说明（2026-04-10）：本报告已并入 `07_硬件系统完成度与验收总览_2026-04-10.md`。  
> 本文件保留用于历史联调追溯。

> 日期：2026-04-09  
> 范围：`room_terminal`、`front_desk_terminal`、`floor_controller`  
> 结论：三端业务流主链路已达到可联调验收通过

---

## 1. 验收目标

- 验证三端业务命令执行链路完整性（接收、执行、回执、状态更新）。
- 验证前台主动控制链路（按键事件上报、下发命令、回执监听）。
- 验证楼控扩展占位命令与楼道报警事件链路可用性。

---

## 2. 验收项与结果

### 2.1 客房端业务命令

- [x] `light_on` / `light_off`
- [x] `air_on` / `air_off`
- [x] `curtain_open` / `curtain_close`
- [x] `door_unlock` / `door_lock`
- [x] `incoming_call` / `hangup_call`
- [x] 回执主题 `hotel/device/command/result` 返回 `success/failed` 结构完整
- [x] 执行后状态上报链路可用

### 2.2 前台端主动控制

- [x] 按键事件上报：`front_clear_pressed` / `front_broadcast_pressed`
- [x] 下发房间命令：`incoming_call` / `hangup_call`
- [x] 订阅并处理回执：`hotel/device/command/result`

### 2.3 楼控端业务链路

- [x] 基础命令：`light_on` / `light_off`
- [x] 扩展占位命令：`broadcast_start` / `broadcast_stop` / `floor_reset`
- [x] 楼道报警按键事件上报：`floor_alarm_pressed`
- [x] 楼控运行态上报：走廊灯状态字段
- [x] 复位入口约束：楼控物理按钮用于报警，`floor_reset` 由前台电脑/后台命令触发

---

## 3. 关键一致性检查

- [x] Topic 与《02_硬件层通讯规范.md》一致
- [x] 设备 ID 命名一致：`front_desk_01` / `room_301` / `floor_03`
- [x] 命令字段一致：`command_id`、`command_type`、`timestamp`
- [x] 三端均保留网络门控，断网时不执行高频上报

---

## 4. 验收结论

三端主业务流闭环已形成，满足“可联调稳定态”验收要求。  
当前可进入后端联合联调与演示阶段；量产前仍需执行 TLS、HIL 与 24h 长稳压测。

