# 下一阶段执行清单（Phase 3）

> 说明：本清单用于“构建修复完成后”的独立阶段推进，避免与本轮依赖/构建修复混在同一提交中。

---

## 1. main.c 业务缝合（独立提交）

- 将三端 `main.c` 与 HAL/Service API 完整接通，清理占位逻辑。
- 保持消息 Topic 与字段严格对齐《02_硬件层通讯规范.md》。
- 对关键控制路径（灯光、门锁、群控、通话状态）补充失败分支日志。

## 2. FreeRTOS 任务隔离（独立提交）

- 拆分任务：
  - 按键轮询任务
  - 传感器采样任务
  - 音频处理任务
- 统一任务优先级和周期注释，避免并发行为不可控。

## 3. 网络容灾与配置收敛（独立提交）

- 补齐 WiFi/MQTT 断线重连状态流转。
- 统一 NVS 读取优先级（NVS > 默认值）。

## 4. 联调验收（独立提交）

- 使用 `test/mqtt_simulator` 完成三端联调脚本验收。
- 固化“上线消息、心跳/遥测、回执”三类验收清单。

