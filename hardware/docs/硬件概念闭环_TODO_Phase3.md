# 硬件概念闭环与实机联调 TODO 计划 (Phase 3)

> **目标**：将当前零散的组件库与架构设计，缝合为能够在电脑上“空跑”的完整 IoT 固件产品，实现“仅待烧录”的巅峰状态。

## 1. 缝合三端 `main.c` (业务逻辑干跑)
- [x] **客房端 (Room Terminal)**：接入 `hal_actuators` 与 `hal_interactive`，完成 MQTT 指令解析、执行与回执流程收敛。
- [x] **前台端 (Front Desk)**：完成上线/心跳发布路径收敛，补齐任务化骨架。
- [x] **楼控端 (Floor Controller)**：完成群控指令执行与回执收敛，拆分环境采样上报任务。

## 2. 引入 FreeRTOS 任务隔离 (并发防撞)
- [x] **按键轮询任务 (`button_poll_task`)**：前台端已预留骨架任务。
- [x] **传感器采集任务 (`sensor_poll_task`)**：客房/楼控端已独立任务化并统一周期常量。
- [x] **音频处理任务 (`audio_task`)**：客房端已独立 `voice_task`，仅处理通话态音频。

## 3. 网络容灾与状态机 (断线重连)
- [x] **WiFi 断线状态门控**：三端已完成 `s_network_ready` 门控接入。
- [ ] **MQTT 退避重连机制**：待在 `service_mqtt` 中补充指数退避策略。
- [x] **离线降级模式**：三端已在断网时停止高频上报并进入降级日志路径。

## 4. 彻底消灭硬编码 (NVS 存储)
- [x] **NVS 配置读取**：三端已接入设备标识与 MQTT Broker URI 的 NVS 读取。
- [x] **默认后备机制**：已实现 NVS 读取失败时回退 `global_config.h` 默认值。

## 5. 本地 Node.js 脚本联调 (终极验证)
- [x] **MQTT 联调环境可用**：已完成脚本侧 MQTT 联调（当前使用远端 Broker）。
- [x] **编写 Mock Server 脚本**：三端模拟脚本已覆盖上线、遥测、回执和下行命令验证。
- [x] **链路联调实测**：已完成三端脚本实测与命令回执验证，链路可用。

---

## 当前状态总结（2026-04-09）

- Phase 3 的核心“底层业务闭环”已完成。
- 下一步重点：在 `service_mqtt` 层补齐退避重连策略与长稳压测脚本。
